# Auditoría CompanyUsers — seguimiento

> **Documento vivo / tracker.** Se actualiza al cerrar cada hallazgo.
> **Creado:** 2026-06-05 · **Reauditado:** 2026-06-06 · **Estado:** 🟢 Cerrado (F1 ✅ PR-A · F2/F5 ✅ PR-B · F6 ✅ PR-C · F4 ✅ PR-D · R1 ✅ PR-E · F3 ➖ won't-fix · F7 ✅ escindido a [auditoría de provisioning](company-user-provisioning-audit.md)) · **Owner:** equipo backend
> **Alcance:** dominio CompanyUsers completo — `CompanyUsersController` (`api/v1/company/users`, 8 endpoints) + `Features/CompanyUsers/` (Create/Update/Patch/Deactivate/Reactivate/Get/GetList/ResetInvitation + Common) + `UserCompanyRepository` + authz (`AuthorizeResource` + `ICompanyUserAuthorizationService` + `IFieldPermissionService`) + `CompanyUserETag`.
> **Dimensiones:** seguridad · performance · arquitectura, contra `AGENTS.md` (§8, §17) y `docs/technical/overview/project-foundation.md` (§11).

---

## 1. Veredicto

Controlador **maduro y consciente de seguridad** — el más sensible auditado (Auth/Identity/RBAC). **Sin vulnerabilidades crítico/alto confirmadas** tras verificación adversarial:

- **Aislamiento de tenant VERIFICADO sólido:** toda mutación resuelve la membresía con `FindByUserPublicIdAsync(companyPublicId=JWT, userId)` y distingue 404 vs `TenantMismatch` (403); el tenant sale del JWT, no de un parámetro. **No hay IDOR cross-company.**
- **Sin asignación de roles cross-tenant:** `IamRole : TenantEntity` → el filtro global EF lo acota al tenant; `GetRolesByPublicIdsAsync` no usa `IgnoreQueryFilters` → solo devuelve roles del tenant del caller. *(La alarma "CRITICAL privilege escalation" del análisis inicial se degradó a una pregunta de diseño intra-tenant — ver F3.)*
- **Last-admin protegido** (update + deactivate), **PATCH whitelist** que re-despacha el Update (sin bypass), **field-level permissions** en lectura y escritura, **weak-ETag sólido** (hashea perfil+status+set de roles ordenado; rota; honra `W/"…"` y `*`), **two-layer authz superset** verificado (filtro `[AuthorizeResource]` ⊇ gate del handler).

Lo accionable: **1 N+1 (perf), rate-limiting ausente en endpoints de email/search, una pregunta de diseño de techo de privilegios, enumeración cross-tenant en la invitación, y enrolado en guardrails.**

**Estado a 2026-06-06:** todos los hallazgos de código resueltos (F1·PR-A, F2/F5·PR-B, F6·PR-C, F4·PR-D); F3 cerrado como ➖ won't-fix (modelo RBAC intencional); sólo queda F7 como observación para una auditoría de provisioning separada.

**Reauditoría 2026-06-06:** doble pase — (A) anti-regresión: los 5 fixes de código (F1/F2/F4/F5/F6) siguen presentes y verificados en el código actual; (B) pase adversarial fresco sobre los 8 endpoints + repo + authz + ETag + validators. Re-confirmados los cruces críticos: aislamiento de tenant, **sin asignación de roles cross-tenant** (`GetRolesByPublicIdsAsync` verificado sin `IgnoreQueryFilters` → acotado por el filtro global de `IamRole : TenantEntity`), rate-limiting, validación de entrada, last-admin guard, weak-ETag, PATCH whitelist sin bypass, tokens CSPRNG, audit en todas las mutaciones. **Sin nuevas brechas de seguridad.** Único hallazgo nuevo: **R1 (perf, P3, latente) — resuelto en la misma reauditoría (PR-E).**

## 2. Leyenda de estado

⬜ pendiente · 🟡 en progreso · ✅ resuelto · ⏸️ diferido · ➖ descartado

| Severidad | P1 crítico | P2 alto | P3 medio/bajo |
|---|---|---|---|
| Conteo | 0 | 3 | 5 |

*(P3 incluye R1, hallazgo añadido en la reauditoría 2026-06-06 y resuelto en PR-E.)*

---

## 3. Hallazgos accionables

| # | Dim | Sev | Estado | Hallazgo | Evidencia (`file:line`) | Fix propuesto |
|---|-----|-----|--------|----------|-------------------------|---------------|
| **F1** | PERF | P2 | ✅ | **N+1 en el listado con `includeAllowedActions=true`**: el handler itera la página llamando `IsLastActiveAdministratorAsync` por usuario, y cada llamada re-ejecuta `GetActiveAdministratorUserIdsAsync` (varias queries) → ~N× por página (default 20). | `GetCompanyUsers.cs:46-72`; `UserCompanyRepository.cs:199-202,338-402` | Calcular el set de admins activos **una vez por página** y evaluar `isLastActiveAdministrator` en memoria (mismo patrón batch que CompetencyFramework F1). Sin el flag, el listado ya es eficiente (3-4 queries, batched). <br>**✅ Resuelto (PR-A · 2026-06-06):** `GetActiveAdministratorUserIdsAsync(companyPublicId)` promovido de privado a `IUserCompanyRepository`; el handler lo invoca **una vez por página** y evalúa `isLastActiveAdministrator` en memoria (`Count == 1 && Contains(user.Id)`), espejo de `IdentityAccess/Users/UserAdministration.cs:216`. Anclado por `Handle_WhenListingWithAllowedActions_ShouldResolveAdministratorsOncePerPage` (1 resolución/página, red→green verificado). |
| **F2** | SEC/PERF | P2 | ✅ | **Endpoints de email/search sin rate-limiting**: `POST /company/users` (invita, **envía email**) y `POST /{id}/reset-invitation` (**re-envía email**) no tienen `[EnableRateLimiting]` → abuso/email-bomb/enumeración. `GET` lista/search tampoco. Los endpoints de auth (register/login/invite-accept) **sí** están limitados (5/min/IP). | `CompanyUsersController.cs:74,228,35`; `Program.cs` (sin política CompanyUsers) | `CompanyUserRateLimitPolicies` (Invite tight ~5-10/min user+tenant; Search 120/min) + registro en `Program.cs` + `[EnableRateLimiting]` + guardrail `CompanyUserRateLimitingGovernanceTests`. Estándar: AGENTS.md §8/§17.3, foundation §11.4 ("rate limiting en endpoints sensibles"). <br>**✅ Resuelto (PR-B · 2026-06-06):** `CompanyUserRateLimitPolicies` (Invite 10/min + Search 120/min, partición user+tenant, config-overridable) registradas en `Program.cs`; `[EnableRateLimiting(Invite)]` en Create + ResetInvitation (los e-mail senders), `[EnableRateLimiting(Search)]` en List. Guardrail `CompanyUserRateLimitingGovernanceTests` (todo `[HttpPost]` declara política canónica + ambas cableadas; red→green). Integración: `CompanyUsers_Invite_ShouldRateLimit` (429 al 11º) + `CompanyUsers_Search_ShouldRateLimit` (429 al 121º). |
| **F3** | SEC | P2 | ➖ | **Sin techo de privilegios al asignar roles** (pregunta de diseño): con `RBAC_USERS.Update` + permiso de campo `Role`, un user-admin puede asignar **cualquier rol del tenant (incl. admin)**, sin chequeo de "solo roles ≤ los que ya tienes". Incluye auto-escalación (editarse a sí mismo). **VERIFICADO: NO hay exposición cross-tenant** (`IamRole` tenant-scoped). | `UpdateCompanyUser.cs:82-95`; `CreateCompanyUser.cs` (mismo patrón) | **➖ Won't-fix (decisión de producto · 2026-06-06):** es el modelo RBAC intencional — el permiso "gestionar usuarios" (`RBAC_USERS.Update`/`Create` + field-perm `Role`) habilita asignar cualquier rol **del tenant**, como en la mayoría de sistemas RBAC. La delegación acotada ("techo de privilegios" / roles solicitados ⊆ roles delegables del caller) **no** es un requisito actual; el riesgo está acotado intra-tenant (sin exposición cross-tenant, verificado). Si en el futuro se quiere delegación acotada, reabrir como feature de roadmap, no como deuda. |
| **F4** | SEC | P3 | ✅ | **Enumeración cross-tenant en la invitación**: `POST create` devuelve el código distinto `company_users.user_in_another_company` cuando el email pertenece a **otra** empresa → un admin autenticado descubre la existencia cross-tenant de un email. (El caso misma-empresa `user_already_in_company` es aceptable.) | `CreateCompanyUser.cs:99-114`; `CompanyUserErrors.cs:42-50` | Para el caso cross-tenant, responder genérico (p. ej. invitación aceptada/encolada) sin revelar pertenencia a otra empresa. Foundation §11.4 ("respuestas genéricas para evitar enumeración"). Contexto: el caller ya es admin autenticado → severidad acotada. <br>**✅ Resuelto (PR-D · 2026-06-06):** la rama cross-tenant de `CreateCompanyUser` ahora devuelve `UserAlreadyInCompany` (idéntico al caso misma-empresa) en vez de `UserAssignedToAnotherCompany` → las dos respuestas son **indistinguibles** y el admin no puede sondear la existencia cross-tenant de un email. Nota de diseño: un código genérico *distinto* sólo habría renombrado el oráculo (nuevo→201 vs cross-tenant→código-único); la indistinguibilidad real exige colapsar en una respuesta ya existente. El símbolo `UserAssignedToAnotherCompany` **se conserva** para `CompanyUserProvisioningService` (sync system-driven, no atacable). Anclado en `Handle_WhenEmailBelongsToAnotherCompany_ShouldReturnSameCodeAsSameCompany` (red→green). Build 0/0, unit 1609/0, integración CompanyUsers 16/16. |
| **F5** | GUARDRAIL | P3 | ✅ | **Sin enrolar en guardrails estructurales**: no está en `OpenApiContractGuardrailsTests.Families[]` (aunque el controller ya tiene `[Tags("Company Users")]`+`[SwaggerOperation]`), ni en `PaginationRangeGuardrailsTests` (sin `[Range]` en el boundary; el handler valida `InclusiveBetween(1,100)`), ni en un guardrail de rate-limit. | `OpenApiContractGuardrailsTests.cs:39-66`; `CompanyUsersController.cs:46` | Enrolar la familia CompanyUsers en el guardrail OpenAPI + `[Range(1, MaxPageSize)]` en `pageSize` + su guardrail de paginación (drift-proof, como CompetencyFramework F6). La exclusión de `AuthorizationPolicyConventionGovernance` **es by-design** (handler-gated `[AuthorizeResource]`, documentado en el controller). <br>**✅ Resuelto (PR-B · 2026-06-06):** familia `CompanyUsers` enrolada en `OpenApiContractGuardrailsTests.Families`; `List` refactorizado a params explícitos con `[Range(1, CompanyUserValidationRules.MaxPageSize)]` (mismo estilo que el resto de controllers) + validador sincronizado a la constante (`MaxPageSize`); nuevo `CompanyUserPaginationGuardrailsTests` (drift-proof, red→green). `AuthorizationPolicyConventionGovernance` se deja intacto (handler-gated, by-design). |
| **F6** | ARCH | P3 | ✅ | **`reset-invitation` no adjunta `WeakETag`** en el `User` de la respuesta, a diferencia de `create` (que hace `with { WeakETag = Compute(...) }`) → el cliente no puede mutar inmediato sin un GET. Inconsistencia de contrato. | `ResetInvitation.cs:152` vs `CreateCompanyUser.cs:211-213` | Adjuntar `WeakETag` al `User` en la respuesta de reset-invitation (espejar `create`), o omitirlo consistentemente en ambas. <br>**✅ Resuelto (PR-C · 2026-06-06):** la respuesta de `ResetInvitation` ahora hace `Filter(response, …) with { WeakETag = CompanyUserETag.Compute(response) }`, espejo exacto de `create` (ETag computado de la proyección **sin filtrar** → independiente de field-perms del caller). Anclado en `Handle_WhenResettingInvitation_…` (assert `WeakETag` no-vacío + igual al recomputado; red→green por construcción — antes era `null`). Build 0/0, unit 1608/0. |
| **F7** | ARCH | P3 (obs) | ✅ | **`CompanyUserProvisioningService.SyncRoleAssignmentsForPositionSlot` asigna roles sin field-auth/audit** del flujo CompanyUsers. Es un flujo **distinto** (provisión por position-slot, system-driven) → fuera del alcance del controller CompanyUsers. | `CompanyUserProvisioningService.cs` | Observación: verificar que el bypass es intencional (sync system-driven con su propia auditoría de position-slot). Si no, alinear. Revisar en una auditoría aparte de provisioning. <br>**✅ Escindido (2026-06-06):** auditoría dedicada en [`company-user-provisioning-audit.md`](company-user-provisioning-audit.md). Veredicto: sin crit/high; el "cross-tenant" es falso positivo (ambos llamadores con tenant ambiente del JWT). 4 hallazgos P3 (PV1 gap latente de tenant, PV2 audit por-usuario, PV3 N+1, PV4 email-en-transacción). Fuera del alcance de este tracker. |
| **R1** | PERF | P3 | ✅ | **N+1 + sync-over-async en la rama de *fallback* de `GetActiveAdministratorUserIdsAsync`** (hallazgo de la reauditoría 2026-06-06). La ruta principal (admins vía IAM) ya estaba batcheada en 1 query; el fallback (cuando no resuelve ningún admin IAM — linkage incompleto/datos legacy) iteraba `activeMemberUserIds` (lista ya materializada) ejecutando `dbContext.IamRoles.Any(...)` **síncrono por cada miembro** → N round-trips bloqueantes. Latente (el fallback rara vez se alcanza), pero F1 lo invoca 1×/página en el listado → un tenant en régimen de fallback pagaría el N+1 por página. | `UserCompanyRepository.cs:404-415` (numeración previa al fix) | Batchear como la ruta IAM: 1 query set-based con los `RoleId` de membresía distintos → set de roles administrativos → match en memoria. <br>**✅ Resuelto (PR-E · 2026-06-06):** el fallback ahora hace **una** query (`membershipRoleIds.Contains(role.Id) && PermissionAssignments.Any(admin)`) + filtrado en memoria (`administrativeRoleIds.ToHashSet()`). Anclado por `CompanyUserAdministratorResolutionIntegrationTests` (provider real, fuerza el branch sin IamUser seeded; 2 admins vía rol-de-membresía resueltos + 1 no-admin excluido; estilo PV3). Build 0/0, unit 1633/0, integración CompanyUser 18/18. |

---

## 4. Descartado / ya cumple (verificado — no son hallazgos)

| Tema | Resolución |
|---|---|
| ➖ "CRITICAL privilege escalation cross-tenant" | **Falso positivo:** `IamRole` es tenant-scoped (filtro global EF), `GetRolesByPublicIdsAsync` no lo ignora → imposible asignar roles de otro tenant. Lo intra-tenant es F3 (diseño). |
| ➖ F3 — techo de privilegios en asignación de roles | **Won't-fix (decisión de producto 2026-06-06):** el modelo RBAC intencional permite que "gestionar usuarios" asigne cualquier rol **del tenant**; sin delegación acotada por ahora. Riesgo acotado intra-tenant (sin exposición cross-tenant, verificado). Reabrir como feature de roadmap si se requiere delegación acotada. |
| ➖ IDOR / aislamiento de tenant | Verificado sólido (membresía scoped al JWT; 404 vs TenantMismatch). |
| ➖ Weak-ETag "lossy / last-writer-wins" | **Incorrecto:** el `If-Match` mismatch devuelve `409 CONCURRENCY_CONFLICT` (previene el sobre-escrito). Weak-ETag computado = decisión deliberada (opción c; los 3 agregados no tienen token). |
| ➖ Transacción de 3 agregados "ventana de inconsistencia" | Es **un solo** `SaveChangesAsync` (atómico). Sin commit parcial. |
| ➖ Handler-gated `[AuthorizeResource]` (no `[AuthorizationPolicySet]`) | By-design, documentado en el controller; excluido de `GovernedFamilyRegex` a propósito. |
| ➖ Soft-delete vía deactivate (sin hard DELETE) | By-design (auditoría/compliance/re-invitación; revoca refresh tokens; last-admin guard). |
| ➖ `If-Match: *` wildcard | Manejado correctamente (`CompanyUserETag.Matches` honra `*`). |
| ➖ `search` `FirstName.ToUpper()` con null | EF traduce a SQL (null→null, sin NRE); no se evalúa client-side. |
| ✅ Cumple | Last-admin guard (update+deactivate), PATCH whitelist→re-dispatch Update, field-perms en lectura+escritura, two-layer authz superset, `[Tags]`+`[SwaggerOperation]` en los 8 endpoints, POST→201+Location, paginación acotada (validator), `AsNoTracking`, proyección SQL-side, split por operación limpio (sin god-file), sin código muerto. |

---

## 5. Plan de PRs sugerido

| PR | Hallazgos | Tema |
|---|---|---|
| **PR-A** ✅ | F1 | Perf: batch del set de admins en el listado (mayor impacto). **Hecho 2026-06-06.** |
| **PR-B** ✅ | F2 + F5 | Hardening: rate-limit de invite/reset/search + `[Range]` + enrolar guardrails (drift-proof). **Hecho 2026-06-06.** |
| **PR-C** ✅ | F6 | Consistencia: `WeakETag` en reset-invitation. **Hecho 2026-06-06.** |
| **PR-D** ✅ | F4 | Privacidad: respuesta genérica (colapsar cross-tenant en `user_already_in_company`). **Hecho 2026-06-06.** |
| **PR-E** ✅ | R1 | Perf: batchear la rama de fallback de `GetActiveAdministratorUserIdsAsync` (1 query set-based, elimina N+1 + sync-over-async) + test de integración del branch. **Hecho 2026-06-06 (reauditoría).** |
| **Decisión** ➖ | F3 | Producto: **resuelto won't-fix** — se acepta el modelo "user-admin asigna cualquier rol del tenant" (RBAC intencional, sin exposición cross-tenant). **2026-06-06.** |
| **Aparte** ✅ | F7 | Escindido a la [auditoría de provisioning](company-user-provisioning-audit.md) (position-slot role-sync) — **cerrada** (PV1/PV2/PV3 ✅, PV4 ➖). No queda trabajo pendiente en este tracker. |

---

## 6. Bitácora

| Fecha | Cambio |
|---|---|
| 2026-06-05 | Auditoría inicial (4 agentes: seguridad/perf/arquitectura/estándares + verificación adversarial). Hallazgo clave: la alarma "CRITICAL privilege escalation" se **degradó** a pregunta de diseño intra-tenant (F3) tras verificar que `IamRole` es tenant-scoped → sin exposición cross-tenant. 7 hallazgos accionables (0 P1, 3 P2, 4 P3); 8 temas descartados (falsos positivos / by-design). Todos ⬜ pendientes. |
| 2026-06-06 | **PR-A (F1) ✅ resuelto.** Eliminado el N+1 del listado con `includeAllowedActions=true`: el set de administradores activos se resolvía 1 vez por usuario (≈N×, default 20). `GetActiveAdministratorUserIdsAsync(companyPublicId)` se expuso en `IUserCompanyRepository` (impl. `UserCompanyRepository` ya delegaba ahí desde `IsLastActiveAdministrator/HasAnyActiveAdministrator`); `GetCompanyUsers` ahora lo invoca **una sola vez por página** y evalúa el flag en memoria (patrón espejo de `IdentityAccess/Users/UserAdministration.cs`). Diff mínimo, contrato de wire intacto. +2 tests unit (once-per-page con contador de llamadas + último-admin en ambas direcciones; red→green verificado revirtiendo al loop N+1 → contador 2≠1). Build 0/0, unit 1600/0, integración CompanyUsers 14/14 ✅ (docker Postgres `:5433`). |
| 2026-06-06 | **PR-B (F2 + F5) ✅ resuelto.** **F2 rate-limiting:** nuevas `CompanyUserRateLimitPolicies` (Invite 10/min + Search 120/min, partición user+tenant, config-overridable vía `RateLimiting:CompanyUsers:*`) registradas en `Program.cs`; `[EnableRateLimiting(Invite)]` en Create + ResetInvitation (los que envían e-mail), `[EnableRateLimiting(Search)]` en List. **F5 guardrails:** familia `CompanyUsers` enrolada en `OpenApiContractGuardrailsTests.Families`; `List` refactorizado a params explícitos con `[Range(1, CompanyUserValidationRules.MaxPageSize)]` (estilo del resto de controllers, contrato de wire intacto) + validador sincronizado a `MaxPageSize`. Nuevos guardrails `CompanyUserRateLimitingGovernanceTests` + `CompanyUserPaginationGuardrailsTests` (drift-proof; ambos red→green verificados). `AuthorizationPolicyConventionGovernance` intacto (handler-gated, by-design). Build 0/0, unit 1608/0, integración CompanyUsers 16/16 ✅ (incl. `CompanyUsers_Invite/Search_ShouldRateLimit`). |
| 2026-06-06 | **PR-C (F6) ✅ resuelto.** Consistencia de contrato: la respuesta de `reset-invitation` ahora adjunta el `WeakETag` al `User` (`Filter(response, …) with { WeakETag = CompanyUserETag.Compute(response) }`), espejo exacto de `create`. El ETag se computa de la proyección **sin filtrar** (independiente de los field-perms del caller, como documenta `CompanyUserETag`), de modo que el cliente puede mutar inmediatamente tras la re-invitación sin un GET previo. Anclado en `Handle_WhenResettingInvitation_ShouldRevokePreviousTokenAndIssueNewOne` (assert `WeakETag` no-vacío + igual al recomputado; red→green por construcción — antes la propiedad era `null`). Diff mínimo (1 línea de producción), contrato de wire enriquecido sin breaking change. Build 0/0, unit 1608/0. |
| 2026-06-06 | **F3 ➖ won't-fix + PR-D (F4) ✅ resuelto** (tras decisión de producto/privacidad). **F3:** se acepta el modelo RBAC — "gestionar usuarios" habilita asignar cualquier rol del tenant; sin techo de privilegios (riesgo acotado intra-tenant, sin exposición cross-tenant). Si se quisiera delegación acotada → roadmap, no deuda. **F4 (PR-D):** la rama cross-tenant de `CreateCompanyUser` devuelve ahora `UserAlreadyInCompany` en vez de `UserAssignedToAnotherCompany` → respuesta **indistinguible** del caso misma-empresa, cerrando la enumeración cross-tenant de emails por un admin autenticado. Un código genérico distinto sólo habría renombrado el oráculo; por eso se colapsa en una respuesta existente. El símbolo `UserAssignedToAnotherCompany` se conserva para `CompanyUserProvisioningService` (sync system-driven, no atacable). +1 test unit `Handle_WhenEmailBelongsToAnotherCompany_ShouldReturnSameCodeAsSameCompany` (red→green). Build 0/0, unit 1609/0, integración CompanyUsers 16/16. **Auditoría cerrada para acción; sólo queda F7 (observación) → auditoría de provisioning aparte.** |
| 2026-06-06 | **Reauditoría + PR-E (R1) ✅ resuelto.** Doble pase a petición: (A) anti-regresión — los 5 fixes de código (F1/F2/F4/F5/F6) verificados presentes en el código actual (rate-limit en List/Create/ResetInvitation + `Program.cs`, `[Range]` + ambos guardrails drift-proof, colapso cross-tenant a `UserAlreadyInCompany`, `WeakETag` en reset-invitation, batch de admins 1×/página); (B) pase adversarial fresco sobre los 8 endpoints + repo + authz + ETag + validators. Re-confirmado **sin asignación de roles cross-tenant** (`GetRolesByPublicIdsAsync` sin `IgnoreQueryFilters`) y aislamiento de tenant sólido. **Sin nuevas brechas de seguridad.** Único hallazgo nuevo **R1 (perf, P3, latente)**: la rama de *fallback* de `GetActiveAdministratorUserIdsAsync` ejecutaba `dbContext.IamRoles.Any(...)` síncrono por cada miembro activo (N+1 + sync-over-async) dentro de un `Where` en memoria. **PR-E:** batcheado a **una** query set-based (`membershipRoleIds.Contains(role.Id) && PermissionAssignments.Any(admin)`) + match en memoria, espejo de la ruta IAM. Nuevo `CompanyUserAdministratorResolutionIntegrationTests` (provider real; sin IamUser seeded → fuerza el branch; 2 admins vía rol-de-membresía resueltos + 1 no-admin excluido). Observaciones no-accionables: email-antes-de-commit (consistente con la decisión PV4 ya aceptada); 403 TenantMismatch vs 404 en mutaciones (input es GUID, no enumerable). Build 0/0, unit 1633/0, integración CompanyUser 18/18. |
