# Backlog de requerimientos — seguimiento de desarrollo

| | |
|---|---|
| **Propósito** | **Fuente única de verdad** del estado de los requerimientos pendientes y en desarrollo. El asistente NO debe confiar en su memoria para saber qué sigue: este archivo manda. |
| **Regla de oro** | Se actualiza **antes de continuar** con la siguiente unidad de trabajo (no al final de la sesión). Si el archivo y la memoria/contexto se contradicen, **gana este archivo**. |
| **Creado** | 2026-07-04 |

---

## Protocolo de uso (para el loop y para cada sesión nueva)

1. **Al iniciar** una sesión o iteración del loop: leer este archivo. El requerimiento activo es el primero en 🟡; si no hay ninguno, tomar el primero 🔴 (orden = prioridad).
2. Ejecutar la **"Próxima acción"** del requerimiento activo (es autocontenida: no depende de recordar la conversación).
3. **Al cerrar cada unidad de trabajo** (un PR completado con suite verde): marcar el checkbox, actualizar "Próxima acción" y agregar una línea a la **bitácora** con fecha y resultado (incluir hash de commit / número de PR si existe).
4. Un requerimiento pasa a 🟢 solo cuando: todos sus PRs están mergeados, la suite completa está verde, `openapi.yaml` sin drift, guía FE publicada y los pendientes de despliegue quedaron documentados/ejecutados.
5. Si algo bloquea (falta insumo del cliente, decisión de negocio, infraestructura): estado ⏸️ con el motivo y qué se necesita para destrabar; pasar al siguiente requerimiento 🔴 si lo hay.
6. Los requerimientos nuevos se agregan al final con la **plantilla** de la última sección (el orden en el archivo define la prioridad; se puede reordenar).
7. Cada PR se desarrolla contra la rama del requerimiento; convención de commits del repo: **sin trailer de co-autoría de IA**.

**Estados:** 🔴 PENDIENTE · 🟡 EN DESARROLLO · 🟢 COMPLETADO · ⏸️ BLOQUEADO

---

## REQ-001 — Vacaciones e incapacidades

| | |
|---|---|
| **Estado** | 🟢 COMPLETADO (PR-1…PR-10 ✅ en `feature/vacaciones-incapacidades`; guía FE publicada; openapi sin drift). **Pendiente de merge a master + ejecución del checklist de despliegue** (infra: migraciones, storage, load-template, quincenas/clínicas — abajo). |
| **Análisis de negocio** | [`docs/business/analisis-vacaciones-incapacidades-empleado.md`](business/analisis-vacaciones-incapacidades-empleado.md) — D-01…D-27 + P-01…P-18 ratificadas; Anexo A.2/A.3/A.4 **confirmados** (2026-07-04) |
| **Plan técnico** | [`docs/technical/plan-tecnico-vacaciones-incapacidades.md`](technical/plan-tecnico-vacaciones-incapacidades.md) — §0 aclaraciones · §3 arquitectura · §7 orden de PRs |
| **Guía FE** | ✅ [`docs/technical/guia-integracion-frontend-vacaciones-incapacidades.md`](technical/guia-integracion-frontend-vacaciones-incapacidades.md) (publicada en PR-10) |
| **Rama** | `feature/vacaciones-incapacidades` (crear desde `master`) |
| **Línea base de tests** | ✅ verificada 2026-07-08: unit 2153 · integración 472/472 (los 2 tests PDF exigen el contenedor `gotenberg` arriba — `docker compose up -d gotenberg`) |
| **Alcance (síntesis)** | Incapacidades con motor de días+montos ISSS (tramos por riesgo, tope patronal 9/año, prórrogas, auto-registro `EN_REVISION`, constancia obligatoria) · lactancia con horarios · fondo anual de vacaciones **por empleado** (ley+beneficio) · plan anual · solicitudes con decisión RRHH y devolución total/parcial · maestros por empresa (clínicas sin semilla, riesgos/tipos con plantilla SV, asuetos, periodos de planilla por empresa) · bandejas/exports (planilla + provisión Finanzas) · saldos en perfil · integración liquidación |

### Checklist de PRs (fuente: plan técnico §7 — detalle por PR en el plan)

**Ola 1 — configuración + incapacidades**
- [x] **PR-1** — Maestros por empresa + `LeaveTemplateSeeder` (A.2/A.3) + `load-template` + TPH `clinic-sectors` (M1) — plan §3.1 ✅ 2026-07-08
- [x] **PR-2** — TPH de estados + 5 ActionTypes (`-9485…-9489`) + 4 permisos/gates + preferencias (+10 columnas) + `restDayOfWeek` en plaza + openapi temprano (M2) — plan §3.2/§3.3 ✅ 2026-07-08
- [x] **PR-3** — Dominio incapacidades + lactancia + EF + guards unitarios (M3) — plan §3.4 ✅ 2026-07-08
- [x] **PR-4** — Motor `IncapacityCalculationRules` + data provider + **suite A.4 en verde (gate de la ola)** — plan §3.5 ✅ 2026-07-08
- [x] **PR-5** — Incapacidades end-to-end (CRUD, confirmación/cierre/anulación, prórrogas, adjuntos `IncapacityDocument`, saldos, asientos, autoservicio) — plan §3.6-3.8 ✅ 2026-07-08
- [x] **PR-6** — Lactancia end-to-end + bandeja/export de incapacidades (filtro default `REGISTRADA`) — plan §3.9 ✅ 2026-07-08 (**cierra la Ola 1**)

**Ola 2 — vacaciones**
- [x] **PR-7** — Fondo: periodos + generación masiva idempotente + detalle/provisión + **saldos del perfil** (M4) — plan §3.10 ✅ 2026-07-08
- [x] **PR-8** — Solicitudes: crear (Art. 178) + decisión (FIFO, anti-self) + devoluciones total/parcial (LIFO) + asientos — plan §3.4/§3.7 ✅ 2026-07-08
- [x] **PR-9** — Plan anual + calendario + bandeja/export de solicitudes + export de provisión — plan §3.7/§3.9 ✅ 2026-07-08
- [x] **PR-10** — Integración liquidación (pendientes → `VACACION_PROPORCIONAL`) + suite E2E + `openapi.yaml` final + **guía FE** — plan §3.11 ✅ 2026-07-08 (**cierra REQ-001**)

### Pendientes de despliegue (código listo; acciones de INFRA/OPERACIÓN del equipo al desplegar — documentadas en la guía FE §14)
- [ ] Migraciones M1–M4 aplicadas (`AddLeaveConfigurationMasters` · `AddLeaveStatusCatalogsPermissionsAndPreferences` · `AddPersonnelFileIncapacitiesAndLactation` · `AddVacationFundAndRequests`). **PR-10 no agrega migración** (la integración con liquidación es solo lógica; drift verificado por `OpenApiContractGuardrails` verde).
- [ ] `Storage:Purposes:IncapacityDocument` en appsettings **base** + contenedor `clarihr-incapacity-documents` aprovisionado (config faltante → 422 en el alta con constancia).
- [ ] `load-template` ejecutado en el tenant productivo (riesgos/tipos/asuetos del año) + los 6 permisos asignados a los roles.
- [ ] La empresa carga su calendario de **periodos de planilla** (no hay plantilla — cada empresa define sus quincenas) y sus clínicas (maestro inicia vacío).

### Próxima acción
> **REQ-001 COMPLETADO (PR-10, 2026-07-08).** Todos los PRs ✅, guía FE publicada, openapi sin drift, integración con liquidación retrocompatible y E2E verde. Falta solo el **merge a master** (orquestador) y la ejecución del **checklist de despliegue** (infra/operación — arriba). **Arrancar REQ-002 (Gestión de tiempo compensatorio)**: es el siguiente 🔴 en orden y su dependencia («después de REQ-001 mergeado») queda satisfecha al mergear esta rama. Al arrancar REQ-002, seguir su «Próxima acción» (crear `feature/tiempo-compensatorio` desde `master`, anotar línea base, PR-1 §3.1-3.3) y recordar los dos «ojos» ya anotados allí (nombres reales de la línea de liquidación `UnitsOrDays`/`UnitsOverridden`/`OverrideAmount` — **confirmados en la práctica en este PR-10**; y el corte `IsSystemCalculated=true` del `HORAS_EXTRAS_PENDIENTES`).

### Ola 1 (referencia)
> **Ola 1 COMPLETA y CERTIFICADA** (integración **492/492** verde, 2026-07-08). El fondo de PR-7 quedó encima. Como el dominio de vacaciones NO se construyó en PR-3 (solo incapacidades+lactancia), PR-7 crea el **dominio de vacaciones completo** (`PersonnelFileVacationPeriod` con filtered-unique `(tenant, personnel_file_id, period_year) WHERE is_active`; `PersonnelFileVacationRequest` + `VacationRequestAllocation` + `VacationReturn`; `Domain/Leave/VacationPlan.cs` + `VacationPlanLine`; consts `VacationRequestStatuses` con sets `DecisionTargets`/`Returnable`) + configs EF + **migración M4 `AddVacationFundAndRequests`** (todas las tablas de vacaciones de una vez), pero **solo cablea el vertical del FONDO**: periodos CRUD (`PersonnelFileVacationPeriodsController`: crear/editar solo-sin-consumo/borrar-soft), generación masiva idempotente (`POST /companies/{id}/vacation-periods/generate`, resumen creados/omitidos/errores por fila, elegibilidad Art. 177 ≥1 año), detalle del fondo + provisión Finanzas (`GET vacation-fund` + export `pendientes × diario × 1.30`), y **`VacationDaysAvailable` del perfil** (§3.10, sustituir el null en `PersonnelFileEmployeeRepository:~2213`, gotcha member-init). `VacationRules` puro: `PeriodBounds(year, useAnniversary, primaryPlazaStartDate)` (año bisiesto/29-feb), `AvailableDays(period, consumptions)`, `IsEligible(seniority≥1año)`. Los handlers de solicitudes/plan son PR-8/PR-9 (las tablas ya quedan en M4). Molde: sub-recurso de expediente = incapacidades (PR-5); permisos `View/ManageVacations` (ya sembrados en PR-2). openapi a mano + tests (generación masiva idempotente 2ª corrida=0, fondo con provisión, VacationDaysAvailable poblado).


### Bitácora
- **2026-07-08 (l)** — **REQ-001 CERTIFICADO DEFINITIVAMENTE**: suite COMPLETA de integración **500/500 verde** (31 min, tras PR-10 que tocó el módulo crítico de liquidación → cero regresión confirmada). Tip de la rama `1384f3a`. REQ-001 queda 🟢 COMPLETADO end-to-end; listo para merge a master. Se arranca REQ-002 (tiempo compensatorio) sobre la misma rama (su dependencia REQ-001 — asuetos/`restDayOfWeek`/periodos/plantillas — ya está construida).
- **2026-07-08 (k)** — **PR-10 COMPLETADO — CIERRA REQ-001** (integración con liquidación + suite E2E + openapi final + guía FE). **(A) Integración liquidación (RF-019, quirúrgica y retrocompatible)**: nuevo `ISettlementRepository.GetPendingVacationDaysAsync(long personnelFileId, ct)` + impl que **delega** en un helper compartido nuevo `Infrastructure/PersonnelFiles/VacationFundQueries.GetAvailableEnjoymentDaysAsync` — **misma derivación EXACTA** que ya poblaba `VacationDaysAvailable` del perfil (PR-7); `PersonnelFileEmployeeRepository.ComputeVacationDaysAvailableAsync` se **refactorizó para llamar al mismo helper** (cero divergencia por construcción). Cadena de datos: campo nuevo `PendingVacationDays decimal?` (default `null`) en `SettlementCalculationContext` (lo puebla `GetCalculationContextAsync`) → propagado al `SettlementCalculationInput` (mismo default) por `SettlementCalculationSupport.Recalculate` → en `BuildSuggestedSpecs` la `EngineSpec(VACACION_PROPORCIONAL)` se reemplaza por un `LineSpec` con `UnitsOrDays: input.PendingVacationDays > 0 ? input.PendingVacationDays : null`. Efecto: **con fondo** → la sugerencia usa los días pendientes; **sin fondo (null/0)** → cae en `DaysSinceAnniversary` (comportamiento anterior intacto). El **override del liquidador** (`UnitsOverridden`/`SetUnitsOrDays`, canal `BuildSpecsFromExisting`) **NO se tocó** y sobrevive los recálculos; la **regeneración** (que limpia líneas por diseño) vuelve a leer el fondo. **Nombres reales confirmados**: `UnitsOrDays`/`UnitsOverridden`/`OverrideAmount` (el plan §3.11 decía `UnitsOrFactorUsed`/`IsOverridden` — INCORRECTO). SIN migración, SIN cambio de contrato de la API de liquidación (`pendingVacationDays` es input interno del motor). **(B) Tests**: 2 unit nuevos en `SettlementCalculationRulesTests` (fondo 12 días → units 12 / 0 → fallback aniversario 106) + 2 E2E nuevos en `ApiIntegrationTests.LeaveSettlement.cs` (con fondo → units == pendientes + override sobrevive recalc + regenerate re-lee el fondo; sin fondo → `DaysSinceAnniversary` computado del `plazaStartDate`/`retirementDate` de la respuesta). **(C) openapi.yaml FINAL**: verificado válido (374 paths / 701 schemas) y con TODOS los endpoints del módulo presentes (maestros, catálogos, incapacidades+balance+documentos, lactancia, fondo+generate+export, solicitudes, plan, calendario, bandejas/exports, load-template, preferencias, `restDayOfWeek`); guardrail `OpenApiContractGuardrails` verde (sin drift — PR-10 no cambia superficie HTTP). **(D) Guía FE** publicada: `docs/technical/guia-integracion-frontend-vacaciones-incapacidades.md` (molde retiro; 14 secciones: RBAC 6 permisos, catálogos+máquinas de estado, maestros+load-template, preferencias+restDayOfWeek, incapacidades con autoservicio/constancia, lactancia, fondo, solicitudes FIFO/LIFO, plan+calendario, bandejas/exports, saldos+RF-019, tabla de errores, adopción por empresa, notas de despliegue; nota salario/30 = montos referenciales). **Suites: build `CLARIHR.slnx` 0 errores/0 warnings · unit `CLARIHR.Application.UnitTests` 2343/2343 (2341 base + 2 nuevos) · integración dirigida `--filter Settlement` 6/6 (4 liquidación existentes SIN regresión + 2 E2E nuevos) + `OpenApiContractGuardrails` 1/1 verde.** Sin correr la suite COMPLETA (la corre el orquestador). Archivos: `VacationFundQueries.cs` (new), `ISettlementRepository.cs`, `SettlementRepository.cs`, `PersonnelFileEmployeeRepository.cs`, `SettlementCalculation.Rules.cs`, `Settlements.Handlers.cs`, `SettlementCalculationRulesTests.cs`, `ApiIntegrationTests.LeaveSettlement.cs` (new), guía FE (new). Siguiente: merge de REQ-001 + arrancar REQ-002.
- **2026-07-08 (j)** — **PR-9 COMPLETADO** (plan anual + calendario + bandeja/export de solicitudes). **(A) Plan anual** (`Application/Features/PersonnelFiles/Vacations/VacationPlans.cs`/`.Handlers.cs`): 3 comandos (Add/Update/Annul) + 2 queries (list?year= / by-id) sobre el dominio ya construido en PR-7 (`Domain/Leave/VacationPlan.cs`, sin cambios); `VacationPlansController` **company-scoped** con `[AuthorizationPolicySet(ViewVacations, ManageVacations)]` (writes Manage, reads View — sin autoservicio) + If-Match. **Warnings ≠ errores** (D-15/aclaración №9): POST/PUT devuelven `warnings[]` POR LÍNEA — `VACATION_PLAN_WARNING_DATE_RULE` (reusa `VacationRules.ValidateRequestDates` como advertencia, no 422) y `VACATION_PLAN_WARNING_INSUFFICIENT_FUND` (fondo del empleado, acumulado por empleado vía nuevo `GetPlanEmployeeContextsAsync` = restDay plaza + disponible enjoyment); mensajes bilingües vía `IBackendMessageLocalizer`. **Bloqueos reales**: solape de líneas del mismo empleado → 422 `VACATION_PLAN_LINE_OVERLAP` (pre-check puro `VacationPlanRules.HasOverlappingLines` ANTES del guard de dominio para 422 limpio, no 500); empleado ajeno a la empresa → 422 `VACATION_PLAN_EMPLOYEE_INVALID`; plan no VIGENTE → 422 `VACATION_PLAN_STATE_RULE_VIOLATION`; no encontrado → 404 `VACATION_PLAN_NOT_FOUND`. El plan **NO genera asiento** (indicativo); auditoría vía `AuditEventTypes.VacationPlanSaved` (nuevo, patrón `VACATION_PERIODS_GENERATED`). **(B) Calendario** (`VacationsCalendar.cs`/`.Handlers.cs`): `GET /companies/{id}/vacations/calendar?year=` → goces de solicitudes `APROBADA/DEVUELTA_PARCIAL/DEVUELTA` (reusa el set `VacationRequestStatuses.ConsumesFund`) que solapan el año + líneas de planes `VIGENTE` del año, por empleado. **(C) Bandeja + export de solicitudes** (`VacationRequestsBandeja.cs`/`.Handlers.cs` + `VacationsReportingController`, molde PR-6): `POST /companies/{id}/vacation-requests/query` (paginado + `StatusCounts` sobre todos los estados; filtros empleado/estado/rango; **gate per-handler `EnsureCanViewVacationsAsync`, NO policy-set en el POST**) + `GET …/vacation-requests/export?format=xlsx|csv|json` (rate-limited) → `GoceVacacionesExportRow` (props ES) fijo a los 3 estados de goce con periodos de origen «año: días». **Export de provisión del fondo (`FondoProvisionExportRow` / `GET …/vacation-fund/export`) YA existía de PR-7 — verificado, NO duplicado.** **(D)** Repo (`IPersonnelFileVacationRepository`/impl, +8 métodos: AddPlan/GetPlanEntity/GetPlanResponses/GetPlanResponse/GetPlanEmployeeContexts/GetCalendar/QueryRequests/GetGoceExportRows) — reusa `LoadNetConsumptionAsync`, `GetHolidaysInRangeAsync`, `VacationRequestSupport`. 6 códigos resx EN/ES nuevos (`VACATION_PLAN_NOT_FOUND`/`_LINE_OVERLAP`/`_STATE_RULE_VIOLATION`/`_EMPLOYEE_INVALID` + warnings `_WARNING_INSUFFICIENT_FUND`/`_WARNING_DATE_RULE`); openapi.yaml a mano (+6 paths → 374, +12 schemas → 701, YAML válido, refs OK). **SIN migración** (dominio+tablas ya en M4; MigrationSeeding verde confirma sin drift). Gotcha resuelto: el `CreatedAtAction` del plan requiere clave de ruta `companyPublicId` (no `companyId`) — la `PublicContractRouteConvention` reescribe `{companyId}`→`{companyPublicId}`. **Suites: build 0 errores/0 warnings · unit 2341/2341 (2336 base + 5 `VacationPlanRulesTests`) · integración dirigida — Vacation 6/6 (4 PR-7/8 + 2 PR-9: plan con warnings fondo/asueto sin bloquear + solape→422 + empleado inválido→422 + anulación→estado; calendario año = plan VIGENTE + goce APROBADA, excluye SOLICITADA; bandeja StatusCounts + filtro; export xlsx 200) + guardrails MigrationSeeding/PublicContract/OpenApi/AllowedActions 16/16 verde.** Sin correr la suite COMPLETA (la corre el orquestador). Siguiente: PR-10 (liquidación + E2E + guía FE — cierra REQ-001).
- **2026-07-08 (i)** — **PR-8 COMPLETADO** (solicitudes de vacaciones + devolución total/parcial). **(A) Reglas puras** (`Vacations/VacationRules.cs`): `ValidateRequestDates` (Art. 178 RN-27 → devuelve códigos de violación, 422 en solicitud / warnings en plan PR-9), `SuggestFifoAllocations` (consume el periodo más antiguo con saldo primero), `SuggestLifoReturn` (deshace la última asignación primero) + records `VacationDateRulePreferences`/`VacationPeriodAvailability`/`VacationPeriodOutstanding` + consts de código Art. 178. **(B) Repo** (`IPersonnelFileVacationRepository`/impl, +8 métodos): `GetRequestEntityAsync` (tracked con Include Allocations/Returns), `AddRequest`, `GetRequestResponsesAsync`/`GetRequestResponseAsync` (map in-memory + `VacationPeriodRef` lookup interno→publicId/año; distribución de devolución deserializada de jsonb), `HasOverlappingRequestAsync` (live = SOLICITADA/APROBADA/DEVUELTA_PARCIAL), `GetHolidaysInRangeAsync`, `GetPrimaryPlazaRestDayAsync`, `ResolveEnjoymentPeriodInternalIdsAsync`, `GetPeriodRefLookupAsync`. **(C) Vertical** (`VacationRequests.cs`/`.Handlers.cs`): 4 comandos + 2 queries. **Crear** (gate nuevo `LoadForCreateOwnOrManageVacationRequestAsync` = Manage OR isSelf, molde incapacidad; requester=expediente propio en autoservicio): Art. 178 (asueto/restDay plaza→pref→domingo), solape con solicitud viva → `VACATION_REQUEST_OVERLAP`, solape con incapacidad activa (**reusa `IncapacityRepository.HasOverlappingIncapacityAsync` — implementado, NO diferido**) → `VACATION_INCAPACITY_OVERLAP`, disponibilidad de fondo RN-10 → `VACATION_FUND_INSUFFICIENT`. **Decisión** (`PATCH …/decision`, Manage-only + anti-self `VACATION_DECISION_SELF_FORBIDDEN` 403): aprobar con `allocations` editables (default FIFO), **re-verifica el fondo DENTRO de la tx** (carrera→`VACATION_FUND_INSUFFICIENT`), Σ==RequestedDays / periodo inválido→`VACATION_ALLOCATION_MISMATCH`, asiento `GOCE_VACACIONES=-9488`; o rechazar. **Cancelación** (`PATCH …/cancellation`, Manage OR isSelf, solo SOLICITADA). **Devolución** (`POST …/returns`, Manage-only + anti-self, solo `Returnable`): total/parcial, distribución LIFO editable (`VacationFundMath.NetConsumedByPeriod` para el outstanding por periodo en orden de asignación), tope `NetConsumedDays`→`VACATION_RETURN_EXCEEDS_CONSUMED`, auto `DEVUELTA_PARCIAL`→`DEVUELTA`, asiento `DEVOLUCION_VACACIONES=-9489`. Guards del dominio (PR-7) usados sin cambios. Controller `PersonnelFileVacationRequestsController` (`[AuthorizationPolicySet(View/ManageVacations)]`, SIN `[ResourceActions]`, If-Match). **(D)** 10 códigos error EN/ES (`VACATION_FUND_INSUFFICIENT`/`_REQUEST_OVERLAP`/`_INCAPACITY_OVERLAP`/`_START_ON_HOLIDAY_FORBIDDEN`/`_START_ON_REST_DAY_FORBIDDEN`/`_END_ON_HOLIDAY_FORBIDDEN`/`_ALLOCATION_MISMATCH`/`_DECISION_SELF_FORBIDDEN`/`_STATE_RULE_VIOLATION`/`_RETURN_EXCEEDS_CONSUMED`) + `VacationErrors.ForDateViolation` mapper; openapi.yaml a mano (+5 paths → 368, +8 schemas → 689, YAML válido). **Saldo del perfil** (`VacationDaysAvailable`) cuadra por construcción (derivado alloc−return). **SIN migración** (dominio+tablas ya en M4; `MigrationSeeding` verde confirma sin drift). **Suites: build 0 errores/0 warnings · unit 2336/2336 (2325 base + 11 VacationRulesTests: 5 Art.178 + 3 FIFO + 3 LIFO) · integración dirigida — Vacations 4/4 (2 PR-7 + 2 PR-8: FIFO 2 periodos→aprobar→devolución parcial LIFO→devolución total→saldo restaurado + asientos GOCE/DEVOLUCION; Art. 178 asueto/restDay→422, autoservicio crea, decidir-propia→403, fondo insuficiente al aprobar→422) + guardrails/Incapacit/Lactation 29/29 verde**. Sin correr la suite COMPLETA (la corre el orquestador). Siguiente: PR-9 (plan anual + calendario + bandeja/export).
- **2026-07-08 (h)** — **PR-7 COMPLETADO — ABRE LA OLA 2** (fondo de vacaciones). **(A) Dominio de vacaciones COMPLETO** (no existía; PR-3 solo hizo incapacidades+lactancia): `Domain/PersonnelFiles/PersonnelFileVacations.cs` (`VacationPeriodSources` MANUAL/GENERACION_MASIVA; `VacationRequestStatuses` con sets `DecisionTargets`/`Cancellable`/`Returnable`/`ConsumesFund`; `PersonnelFileVacationPeriod` [grants ley/beneficio, bounds derivadas, `GeneratesEnjoymentDays`/`UsedAnniversary`/`SourceCode`, guards `Create`/`UpdateGrants`/`Deactivate`, saldos derivados no-columnas]; `PersonnelFileVacationRequest` + hijas `VacationRequestAllocation`/`VacationReturn` con guards `Approve`(Σ==RequestedDays)/`Reject`/`Cancel`/`Return`(LIFO, acumulado≤consumido, auto-DEVUELTA) — **construidos, cableados SOLO donde el fondo los necesita; el vertical de solicitudes es PR-8**) + `Domain/Leave/VacationPlan.cs` (`VacationPlan`+`VacationPlanLine`, guard no-solape por empleado, para PR-9). **(B) Migración M4 `AddVacationFundAndRequests`** (6 tablas: `personnel_file_vacation_periods` con **índice único filtrado** `uq_pf_vacation_periods__tenant_file_year_active (tenant_id, personnel_file_id, period_year) WHERE is_active`, `personnel_file_vacation_requests`, `vacation_request_allocations`, `vacation_returns`, `vacation_plans`, `vacation_plan_lines`; CHECKs de fechas/días; jsonb `distribution_json`), drift limpio. **(C) Reglas puras** `Vacations/VacationRules.cs` (`PeriodBounds` aniversario/calendario **año-bisiesto/29-feb safe**, `AvailableDays`, `IsEligible` Art.177 ≥1año) + `VacationFundMath` (`DailySalary`=salario/30, `Provision`=pend×diario×1.30, `NetConsumedByPeriod` reduce alloc−return). **(D) Vertical del FONDO cableado**: `IPersonnelFileVacationRepository`/impl (period CRUD, consumo por periodo, salario base batch molde data-provider, provisión export, candidatos+idempotencia generación, anchor plaza-principal/hire) + feature (`VacationPeriods`, `VacationFundDetail`, `VacationPeriodsMassGeneration` handlers) + 2 controllers (`PersonnelFileVacationPeriodsController` `[AuthorizationPolicySet(View/ManageVacations)]` GET/POST/PUT/DELETE + GET vacation-fund; `VacationPeriodsAdministrationController` `[Authorize]`-only per-handler gate POST generate + GET vacation-fund/export). Gates nuevos `LoadForManageVacationsAsync`/`LoadCompletedEmployeeForVacationReadAsync`. **(E) Saldos del perfil (§3.10)**: `VacationDaysAvailable` poblado en `PersonnelFileEmployeeRepository` (gemelo de `ComputeDisabilityDaysAvailableAsync`, fuera de la proyección, vía `VacationRules.AvailableDays`; null sin fondo). **(F)** ActionType nuevo de auditoría `VACATION_PERIODS_GENERATED` (catálogo+All); 3 códigos error EN/ES (`VACATION_PERIOD_DUPLICATE`/`VACATION_PERIOD_HAS_CONSUMPTION`/`VACATION_ELIGIBILITY_NOT_MET`); openapi.yaml a mano (+5 paths, +8 schemas → 363 paths/681 schemas, YAML válido). **Decisiones**: (1) salario diario del export = resolución batch del salario base (molde `LeaveCalculationDataProvider`) — implementado, NO diferido; (2) consumo simulado en integración vía seed directo de request APROBADA+allocation empujando `AmbientTenantContext` (InternalsVisibleTo) — así el assert "editar/borrar periodo con consumo→422" corre en PR-7; (3) FIFO/LIFO suggest de `VacationRules` diferidos a PR-8 (el fondo no los usa). **Suites: build 0 errores/0 warnings · unit 2325/2325 (2304 base + 21: VacationRulesTests 7 + VacationDomainTests 14) · migración sin drift · integración dirigida — Vacations 2/2 + guardrails (MigrationSeeding/PublicContract/AllowedActions/OpenApi) 16/16 + Incapacit/Lactation/Vacation 13/13 verde.** Sin correr la suite COMPLETA (la corre el orquestador). Siguiente: PR-8 (solicitudes).
- **2026-07-04** — Análisis de negocio creado, ratificado (D-01…D-27, P-01…P-18) y confirmado el Anexo A (riesgos/asuetos/golden cases). Plan técnico escrito (PR-1…PR-10). Confirmaciones finales: quincenas por empresa (sin plantilla), clínicas sin catálogo inicial (campo opcional). Docs sin commit. Desarrollo no iniciado.
- **2026-07-08 (g2)** — **OLA 1 CERTIFICADA**: suite COMPLETA de integración **492/492 verde** (30 min, contra Postgres :5432 con Gotenberg arriba). Confirma PR-1…PR-6 sin regresiones. La Ola 1 (configuración + incapacidades + lactancia) queda lista para merge. Se arranca la Ola 2 (vacaciones) desde PR-7.
- **2026-07-08 (g)** — **PR-6 COMPLETADO — CIERRA LA OLA 1** (lactancia end-to-end + bandeja/export de incapacidades). **(A) Lactancia**: feature folder `Lactation/` (`LactationPeriods.cs`/`.Handlers.cs` con `LactationWriteSupport` de pre-validación de horarios) + `IPersonnelFileLactationRepository`/impl (resuelve SOLO el `IncapacityType` activo con code `LACTANCIA`) + `PersonnelFileLactationController` (`[AuthorizationPolicySet(View/ManageIncapacities)]`, SIN `[ResourceActions]`) + `LactationContracts.cs`. Endpoints: `GET/POST /personnel-files/{id}/lactation-periods`, `GET/PUT /…/{id}` (el PUT reemplaza datos+horarios vía triple `ReplaceSchedules([])→UpdatePeriod→ReplaceSchedules(new)` para mover el periodo sin chocar con los horarios previos), `PATCH /…/{id}/annulment`. **Solo RRHH** (gate `LoadForManageIncapacitiesAsync`; D-18 sin autoservicio); lecturas `View OR isSelf` (reusa `LoadCompletedEmployeeForIncapacityReadAsync`). Asiento `LACTANCIA=-9487` en la misma tx al crear; auditoría doble-SaveChanges. Errores de horario **pre-validados** (contención→`LACTATION_SCHEDULE_OUT_OF_RANGE`, solape→`LACTATION_SCHEDULE_OVERLAP`) para 422 limpio antes del guard del dominio (defensa en profundidad); tipo≠LACTANCIA→`LACTATION_TYPE_INVALID`; estado→`LACTATION_STATE_RULE_VIOLATION`. **(B) Bandeja+export incapacidades**: `IncapacitiesReportingController` (`POST /companies/{id}/incapacities/query` con `StatusCounts` + `GET …/incapacities/export?format=xlsx|csv|json` rate-limited; **gate per-handler `EnsureCanViewIncapacitiesAsync`, NO policy-set en el POST**) + `IncapacitiesBandeja.cs`/`.Handlers.cs` + 2 métodos nuevos en `IPersonnelFileIncapacityRepository`. **Filtro default `status=REGISTRADA`** (excluye EN_REVISION del insumo de planilla) documentado en summary+comentario; **StatusCounts se calculan SIN el filtro de estado** (cubren todos los estados aunque los items sean REGISTRADA). `IncapacidadExportRow` (props en español) con días+montos por sección, **% por tramo aplanado de `TrancheDetailJson` (deserializado in-memory)**, base mensual/diaria, tipo de planilla + periodo (label+fechas), `utilizaFondo`; join a `PersonnelFileEmployeeProfiles` para el código de empleado; **plaza = `AssignedPositionPublicId` (no hay snapshot de nombre — diferido)**. Localización EN/ES de los 4 códigos nuevos (`LACTATION_*`); `PERSONNEL_FILE_EXPORT_FORMAT_INVALID` reutilizado. openapi.yaml a mano (+5 paths, +8 schemas; YAML válido, 358 paths/673 schemas). Tests: `ApiIntegrationTests.Lactation.cs` (2 round-trips: lactancia alta+horarios→asiento LACTANCIA, out-of-range 422, overlap 422, PUT reemplaza, anulación; bandeja REGISTRADA+EN_REVISION→StatusCounts correctos, default excluye EN_REVISION, export xlsx 200+content-type+filas, format inválido 400). **Suites: build 0 errores · unit 2304/2304 · round-trips nuevos+PR-5 (`--filter Lactation|Incapacit`) 11/11 verde.** Sin migración (dominio ya en M3). RBAC ya anticipaba lactancia/bandeja en la descripción de `View/ManageIncapacities` (PR-2) → 0 permisos nuevos. **Pendiente orquestador: correr la suite COMPLETA de integración (30 min) para el verde global de la ola.** Siguiente: PR-7 (Ola 2 — fondo de vacaciones).
- **2026-07-08 (f)** — **PR-5 COMPLETADO** (incapacidades end-to-end): feature folder `Incapacities/` (8 handlers + `IncapacityWriteSupport`/`IncapacityCalculationSupport` + `IncapacityBalanceRules` puro) + `IPersonnelFileIncapacityRepository`/impl + 2 controllers (`PersonnelFileIncapacitiesController`, `…IncapacityDocumentsController`, `[AuthorizationPolicySet(View/ManageIncapacities)]`, SIN `[ResourceActions]`) + `IncapacityContracts.cs`. Endpoints: `GET/POST/PUT` incapacidad, `PATCH confirmation/closure/annulment`, `POST extensions` (prórroga), `GET incapacity-balance?year=`, y sub-recurso documentos (`GET/POST/DELETE` + `read-url`). Cálculo INLINE en cada escritura vía `ILeaveCalculationDataProvider`→motor→`ApplyCalculation` (indefinida difiere hasta el cierre); **anti-race del tope R-T2** (re-lee consumo del año DENTRO de la tx, solo `REGISTRADA`); gates autoservicio (`LoadForCreateOwnOrManageIncapacityAsync`: Manage OR isSelf→origen AUTOSERVICIO→EN_REVISION; lecturas View OR isSelf 403 sin enmascarar; anti-self al confirmar la propia → 403 `INCAPACITY_CONFIRM_SELF_FORBIDDEN`); **constancia = alta-con-adjunto en un POST** (`FilePurpose.IncapacityDocument` + `Storage:Purposes` en appsettings, contenedor `clarihr-incapacity-documents`; obligatoria si `IncapacityRequiresDocument ?? true` → 422); asientos `INCAPACIDAD`/`PRORROGA_INCAPACIDAD` en la misma tx; `DisabilityDaysAvailable` del perfil poblado por `IncapacityBalanceRules` (cuadra con `incapacity-balance` por construcción). **Hueco RN-14 cerrado por el orquestador tras el subagente**: `INCAPACITY_OVERLAP` (repo `HasOverlappingIncapacityAsync` + guard en crear/editar/prórroga; la prórroga contigua no se auto-marca; +test de integración). 14 códigos de error EN/ES + `INCAPACITY_OVERLAP`. openapi a mano (11 paths + 12 schemas). Sin migración (el `FilePurpose` es enum de config). **Suites: unit 2304/2304 · integración 489/489 (subagente) + Incapacity_Overlapping verde**. Siguiente: PR-6 (cierra Ola 1).
- **2026-07-08 (e)** — **PR-4 COMPLETADO** (gate de la Ola 1 en verde): motor puro `IncapacityCalculation.Rules.cs` (`IncapacityCalculationRules.Calculate` — 7 pasos, sin reloj ni BD; validado y sin corrección necesaria vs el borrador) + `ILeaveCalculationDataProvider`/`LeaveCalculationDataProvider` (salario base = patrón exacto `SettlementRepository:87-124` — códigos `IsBaseSalary` del país + fallback `SALARIO_BASE`, plaza referida→principal, conceptos nivel-empleado solo si principal; RestDay plaza→pref→domingo; asuetos del rango; `EmployerCapRemaining` solo `REGISTRADA` por año calendario; `ChainOffsetDays` saltando anuladas) + DI cableado + resx `INCAPACITY_WARNING_CAP_EXHAUSTED` (EN/ES). El bug del intento previo era solo un `using CLARIHR.Domain.Common` faltante (los enums SÍ existen; los "nombres inexistentes" eran falsa alarma). **Suite dorada `IncapacityCalculationRulesTests` 16/16 (A.4-1…A.4-7 + 9 bordes §6)**; A.4-1 exacto ($20 diario, patrono $45, ISSS $15). **Suites: unit 2290/2290 · integración smoke 3/3 (arranque del contenedor con el nuevo DI + M3)**. Sin migración (PR-4 no toca esquema). Siguiente: PR-5.
- **2026-07-08 (d)** — **PR-4 INICIADO, NO completado** (sesión cortada por límite de créditos). Se redactó el motor puro `IncapacityCalculation.Rules.cs` (7 pasos, ~16 KB — parte difícil hecha) + la abstracción `ILeaveCalculationDataProvider`, pero: (1) `LeaveCalculationDataProvider` (Infra) **no compila** — referencia `CompensationNature`/`CompensationCalculationType` con nombres inexistentes al resolver el `SALARIO_BASE`; verificar los nombres reales de esos enums/consts en el código de compensación antes de reusarlos; (2) faltan el **registro DI** del provider, la clave resx `INCAPACITY_WARNING_CAP_EXHAUSTED` (EN/ES) y **toda la suite dorada A.4** (el gate real del PR). Los 3 archivos parciales se **retiraron del árbol** para dejar la rama limpia en el tip de PR-3 (verde). Borrador preservado fuera de git (scratchpad de la sesión — efímero); lo más seguro es **regenerar PR-4 desde el plan §3.5** con el fix del nombre de enum. La rama `feature/vacaciones-incapacidades` está en `da02086` (PR-3), sin push. Próxima acción sigue siendo **PR-4**.
- **2026-07-08 (c)** — **PR-3 COMPLETADO** (mismo commit): dominio de incapacidades + lactancia en archivos propios (`PersonnelFileIncapacity.cs` con `IncapacityStatuses` + predicado central `CountsAsRegistered` (R-T6) + `IncapacityOrigins` + record `IncapacityCalculationSnapshot` + entidad completa [trío solicitante, FK dura riesgo + 6 snapshots, cadena `ExtendsIncapacityId` autoreferente Restrict, desglose `numeric(18,2)` + `TrancheDetailJson` jsonb, guards Create origen→estado / ApplyCalculation única vía / Confirm / CloseIndefinite / Annul / OverrideComputableDays con nota] + `PersonnelFileIncapacityDocument` espejo medical-claim; `PersonnelFileLactation.cs` con `ReplaceSchedules` contención/no-solape y `UpdatePeriod` que protege horarios) · 4 configs EF + 4 DbSets + CHECKs de fechas/conteos · migración `AddPersonnelFileIncapacitiesAndLactation` (4 tablas), drift limpio · `riskAllowsIndefinite` viaja como parámetro de validación (no se persiste). **Suites: unit 2274/2274 (70 nuevos de dominio) · integración dirigida 18/18 (MigrationSeeding aplica M3 + guardrails)** — PR solo-dominio sin endpoints: el gate completo de integración corre en PR-5 (primer PR con superficie HTTP). Siguiente: PR-4 (motor, gate de la ola).
- **2026-07-08 (b)** — **PR-2 COMPLETADO** (mismo commit que esta bitácora): 2 TPH de estados sembrados (`incapacity-statuses` `-9850…-9852` EN_REVISION/REGISTRADA/ANULADA · `vacation-request-statuses` `-9853…-9858` SOLICITADA/APROBADA/RECHAZADA/ANULADA/DEVUELTA_PARCIAL/DEVUELTA — receta 8 toques) · 5 ActionTypes `-9485…-9489` (sort 160-200, continúa tras LIQUIDACION) · 4 permisos `PersonnelFiles.ViewIncapacities/ManageIncapacities/ViewVacations/ManageVacations` (codes+XML docs, gates fail-closed default-interface + overrides en `PersonnelFileAuthorizationService`, policies **authn-only las 4** — hay autoservicio en ambas familias, molde medical-claims — provisioning tuples, governance list) · `CompanyPreference` +10 columnas anulables + `SetLeavePolicies` (PUT-only; el PATCH sigue scalar-only currency/timezone — precedente dashboard/ayuda) · `restDayOfWeek` (`DayOfWeek?`→smallint, int 0-6 en wire) en Create/Update/**JSON Patch** del assignment + response (el patch applier re-valida rango y hace passthrough para no borrar el valor — sin proyección EF: el Map del assignment es in-memory) · migración `AddLeaveStatusCatalogsPermissionsAndPreferences` (11 AddColumn + 2 CreateTable + 3 InsertData), drift limpio · resx +6 validation.message · **openapi.yaml actualizado A MANO** (+22 paths, +29 schemas, estilo cost-centers; general-catalogs enumera las 3 wire keys nuevas; preferencias y assignment actualizados; guardrails OpenApi 87/87). **Suites: unit 2204/2204 · integración 486/486** (Gotenberg arriba). Siguiente: PR-3.
- **2026-07-08** — Rama `feature/vacaciones-incapacidades` creada; docs commiteados (`547696a`). **PR-1 COMPLETADO** (mismo commit que esta bitácora): 6 entidades `Domain/Leave/` (MedicalClinic, IncapacityRisk+Parameter con guard `ReplaceParameters` de tramos contiguos, IncapacityType, CompanyHoliday, PayrollPeriodDefinition) · TPH `clinic-sectors` sembrado `-9860…-9862` (receta 8 toques) · 5 maestros governed end-to-end espejo CostCenters (familia `[ResourceActions]`, If-Match, activate/inactivate, SIN delete físico) con permisos nuevos **`LeaveConfiguration.Read/Admin`** (módulo PersonnelFiles) · `LeaveTemplateSeeder` idempotente (A.2: 5 riesgos con tramos; D-08: 6 tipos incl. LACTANCIA; A.3: 11 asuetos/año con computus de Semana Santa) + hook en `CompanyProvisioningService` + `POST companies/{id}/leave-configuration/load-template?year=` · migración `20260708063820_AddLeaveConfigurationMasters` (7 tablas + seed TPH), drift limpio · resx EN/ES (18 códigos + 2 validation.message) · governance actualizado (`AuthorizationPolicyConventionGovernanceTests` + registry + ProblemDetails 403). **Suites: unit 2171/2171 (2153 base + 18 dominio) · integración 484/484 (472 base + 12 nuevos `ApiIntegrationTests.Leave.cs`; los 2 tests PDF requieren Gotenberg en Docker)**. Hallazgos operativos: (1) el harness de integración NO pasa por `CompanyProvisioningService` (seeder directo con `Company.Create`) → el hook de plantilla se prueba resolviendo `ILeaveTemplateSeeder` del factory; (2) primer uso de `DateOnly` en el dominio — Npgsql lo mapea nativo a `date`, vigilar serialización en DTOs; (3) validación de guards del PUT de riesgos ANTES de abrir la transacción para no convertir 422 en 500. Siguiente: PR-2.

---

## REQ-002 — Gestión de tiempo compensatorio

| | |
|---|---|
| **Estado** | 🟢 COMPLETADO (PR-1…PR-6 ✅ en `feature/vacaciones-incapacidades`; guía FE publicada; openapi sin drift; migración M3 sin drift; retrocompatibilidad de liquidación probada). **Pendiente de merge a master + ejecución del checklist de despliegue** (infra: migraciones M1-M3, storage/contenedor, paso de adopción, confirmar P-15 — abajo). |
| **Análisis de negocio** | [`docs/business/analisis-tiempo-compensatorio-empleado.md`](business/analisis-tiempo-compensatorio-empleado.md) — **D-01…D-21 ratificadas** (D-05/D-06/D-12/D-17 ajustadas; D-19 liquidación · D-20 adjunto · D-21 costura horas-extras nuevas) + **P-01…P-14 respondidas**; única pendiente **P-15** (tarifa de valoración al liquidar — segunda ronda, NO bloqueante: default parametrizable 1.00) |
| **Plan técnico** | [`docs/technical/plan-tecnico-tiempo-compensatorio.md`](technical/plan-tecnico-tiempo-compensatorio.md) — **escrito 2026-07-05**: §0 aclaraciones (12) · §3 arquitectura · §3.11 integración liquidación (5 toques quirúrgicos verificados contra código) · §7 orden de PRs (PR-1…PR-6) |
| **Guía FE** | ✅ [`docs/technical/guia-integracion-frontend-tiempo-compensatorio.md`](technical/guia-integracion-frontend-tiempo-compensatorio.md) (publicada en PR-6) |
| **Rama** | `feature/vacaciones-incapacidades` (misma rama de REQ-001 — NO se crea `feature/tiempo-compensatorio`) |
| **Línea base de tests** | heredada de REQ-001: unit 2343 · integración 500/500. Tras PR-1: unit 2343/2343. Tras PR-2: **unit 2389/2389** (+46). Tras PR-3/PR-4/PR-5: unit 2389/2389 · `CompensatoryTime` 8/8. **Tras PR-6: unit `CLARIHR.Application.UnitTests` 2395/2395** (2389 base + 6 nuevos del motor: golden A.4-10 + tarifa 2× + retrocompat + horas editadas + override + bases ISSS/AFP/Renta) · integración dirigida `~Settlement` **9/9** (6 existentes de liquidación SIN regresión + 3 E2E nuevos) · `~CompensatoryTime` **11/11** (8 módulo + 3 E2E) · guardrails `MigrationSeeding`/`OpenApiContractGuardrails` 3/3 · migración M3 sin drift; suite COMPLETA de integración NO corrida (la corre el orquestador — certificación obligatoria porque toca el motor de liquidación) |
| **Alcance (síntesis)** | Fondo de tiempo compensatorio **por empleado** (ledger derivado en **horas**: saldo = acreditado − debitado, nunca negativo, **sin caducidad**) · acreditaciones declarativas de RRHH (fecha trabajada, horas × factor del tipo con snapshot, detalle + autorizado-por + **documento de autorización de jefatura PDF obligatorio** — nuevo FilePurpose; costura `overtimeRecordPublicId` al futuro módulo de horas extras) · ausencias con verificación **transaccional** de fondo y sugerencia de horas (días × horas estándar default 8, excluyendo descanso/asuetos de REQ-001; imputación opcional a periodo de planilla) · estado de cuenta con saldo corrido (RRHH + autogestión lectura) + saldo aditivo en perfil · maestro por empresa de tipos (operación ACREDITA/DEBITA/AMBAS + factor; **inicia VACÍO, sin plantilla** — A.2 es referencia de adopción) · **integración F1 con liquidación**: saldo → línea automática `HORAS_EXTRAS_PENDIENTES` (horas × salario/30/horas-día × tarifa default 1.00, editable, snapshot) · bandeja + exports (movimientos y saldos) · asientos `ACREDITACION_TIEMPO_COMPENSATORIO`/`GOCE_TIEMPO_COMPENSATORIO` · F2: solicitud en línea, política de pago/caducidad al retiro, elegibilidad por plaza, vínculo activo con horas extras |
| **Dependencias** | Desarrollo **después de REQ-001** (asuetos, `restDayOfWeek`, periodos de planilla, solapes cruzados); la **integración con liquidación NO depende de REQ-001** (módulo mergeado, PR #56); seeds en bloque ≤ -9865 (trampa: -9490…-9496 ocupados por `ACTION_STATUS_CATALOG`); storage: 1 purpose nuevo + contenedor |

### Checklist de PRs (fuente: plan técnico §7 — detalle por PR en el plan)
- [x] **PR-1** — Configuración (M1): maestro `CompensatoryTimeType` governed **sin semilla** + 2 TPH (`-9865/-9866`, `-9867…-9869`) + 2 ActionTypes (`-9870/-9871`) + 2 permisos/gates + 4 columnas de preferencias + `FilePurpose.CompensatoryTimeDocument` + openapi temprano — plan §3.1-3.3 ✅ 2026-07-08
- [x] **PR-2** — Dominio + reglas (M2): 3 entidades (crédito+documento+ausencia, costura `overtimeRecordPublicId`) + `CompensatoryTimeRules` puro + **golden A.4 en verde (gate)** + `ICompensatoryTimeRepository` (agregación + `pg_advisory_xact_lock`) — plan §3.4-3.6 ✅ 2026-07-08
- [x] **PR-3** — Acreditaciones end-to-end: CRUD + **adjunto obligatorio en el POST** (patrón nuevo) + sub-recurso documentos + asientos + anti-descubierto bajo lock — plan §3.7-3.8 ✅ 2026-07-08
- [x] **PR-4** — Ausencias + fondo: verificación transaccional + test de carrera + sugerencia (asuetos/`restDayOfWeek` REQ-001) + solapes + statement + **saldo en perfil** — plan §3.7/§3.9 ✅ 2026-07-08
- [x] **PR-5** — Bandeja + exportaciones (movimientos y saldos) con rate limits — plan §3.9 ✅ 2026-07-08
- [x] **PR-6** — Integración liquidación (contexto→input→spec→case; **seed `-9837` volteado a `IsSystemCalculated=true` vía migración M3** → la vía manual queda cerrada gratis, sin guard §3.11.5; solo plaza principal; retrocompatible) + suite E2E + `openapi.yaml` final + **guía FE** — plan §3.11 ✅ 2026-07-08 (**cierra REQ-002**)

### Pendientes de despliegue (checklist §9 del plan)
- [ ] Migraciones **M1-M3** aplicadas (`AddCompensatoryTimeConfiguration` · `AddPersonnelFileCompensatoryTime` · `AddCompensatoryTimeSettlementConcept`). **M3** voltea `HORAS_EXTRAS_PENDIENTES` (seed `-9837`) a `is_system_calculated=true` (`UpdateData`; habilita la línea automática de liquidación y cierra su vía manual).
- [ ] `Storage:Purposes:CompensatoryTimeDocument` (PDF) en appsettings **base** + contenedor `clarihr-compensatory-time-documents` aprovisionado (D-20)
- [ ] **Paso de adopción por empresa**: crear los tipos de tiempo compensatorio (maestro inicia vacío — referencia A.2), configurar las 4 preferencias (horas/día, tope, adjunto obligatorio, tarifa) y cargar saldos iniciales con su acta adjunta
- [ ] Confirmar **P-15** (tarifa de valoración al liquidar; default 1.00) con el negocio antes del despliegue

### Próxima acción
> **REQ-002 COMPLETADO (PR-6, 2026-07-08).** Los 6 PRs ✅, guía FE publicada, openapi sin drift, migración M3 (`AddCompensatoryTimeSettlementConcept`) sin drift, integración con liquidación retrocompatible (6 tests de liquidación existentes SIN regresión) y E2E verde. Falta solo el **merge a master** (orquestador) + el **checklist de despliegue** (infra/operación — arriba) y **correr la suite COMPLETA de integración** (toca el motor de liquidación — certificación obligatoria de REQ-002, la corre el orquestador). **Arrancar REQ-003 (Otras transacciones de personal)**: es el siguiente 🔴 en orden y su dependencia («después de REQ-001 y REQ-002 mergeados») queda satisfecha al mergear esta rama. Al arrancar REQ-003, seguir su «Próxima acción» (crear `feature/otras-transacciones-personal` desde `master`, anotar línea base, verificar IDs de seed libres ≤ -9875 respetando REQ-002 `-9865…-9871`, PR-1 §3.1-3.3) y recordar sus dos «ojos» ya anotados (mutador `Annul()` nuevo en `PersonnelFilePersonnelAction`; asientos reutilizan `AMONESTACION=-9477`/`SUSPENSION=-9478`).

### Bitácora
- **2026-07-08** — **REQ-002 CERTIFICADO DEFINITIVAMENTE**: suite COMPLETA de integración **511/511 verde** (34 min, tras PR-6 que volteó el seed `-9837` y tocó el motor de liquidación → cero regresión confirmada). REQ-002 queda 🟢 COMPLETADO end-to-end; listo para merge. Se arranca REQ-003 (otras transacciones de personal) sobre la misma rama (dependencias REQ-001+REQ-002 completas).
- **2026-07-08** — **PR-6 COMPLETADO — CIERRA REQ-002** (integración con liquidación + suite E2E + openapi final + guía FE). **(A) Resolución del gotcha (como el orquestador verificó): NO hay flag de línea** — el corte `SettlementCalculation.Rules.cs:382` (`if (spec.IsManual || !concept.IsSystemCalculated)`) checa el flag del **CATÁLOGO**. **Se volteó el seed `-9837 HORAS_EXTRAS_PENDIENTES` a `IsSystemCalculated=true`** (`GlobalCatalogSeedData.cs:1021`, param 11) + fixture `SettlementCalculationRulesTests.cs:37` a `true`; migración **M3 `20260708212452_AddCompensatoryTimeSettlementConcept`** = `UpdateData` de `is_system_calculated` (Up→true / Down→false), `has-pending-model-changes` vacío. El **guard anti-duplicado §3.11.5 NO se implementó** (redundante): al ser de sistema, `AddSettlementManualLineCommandHandler` (`Settlements.Handlers.cs:~625`) ya rechaza la línea manual con `SETTLEMENT_CONCEPT_INVALID` (probado E2E). Swagger limpiado (quitado `HORAS_EXTRAS_PENDIENTES` de los ejemplos de concepto manual en `SettlementContracts.cs:84` y `SettlementsController.cs:~328`). **Tests de settlements tocados por el flip: solo el fixture unit `SettlementCalculationRulesTests.cs:37`** (grep confirmó que ningún test agregaba `HORAS_EXTRAS_PENDIENTES` como línea manual esperando éxito). **(B) 4 toques del motor (molde EXACTO de VACACION_PROPORCIONAL, PR-10 REQ-001)**: contexto `CompensatoryTimeContext?(PendingHours, StandardDailyHours, RateFactor)` en `SettlementCalculationContext` (`ISettlementRepository.cs`), resuelto en `GetCalculationContextAsync` (`SettlementRepository.cs`) **solo si la plaza es la principal** (helper `IsPrincipalPlazaAsync` = IsPrimary entre activas→StartDate más antiguo, idéntico a `PersonnelFileVacationRepository`) **y `GetBalanceAsync > 0`**; preferencias `CompensatoryTimeStandardDailyHours ?? 8` / `CompensatoryTimeSettlementRateFactor ?? 1.00` leídas junto al `CurrencyCode`. **`ICompensatoryTimeRepository` inyectado en `SettlementRepository`** (sin ciclo de DI: CompensatoryTimeRepository→Incapacity/Vacation repos, ninguno vuelve a Settlement). Input `CompensatoryTimeInput?` (espejo) mapeado en `SettlementCalculationSupport.Recalculate` (`Settlements.Handlers.cs`). Spec condicional en `BuildSuggestedSpecs` (`input.CompensatoryTime is { PendingHours: > 0 }` → `EngineSpec(HorasExtrasPendientes)`). `case HorasExtrasPendientes` en `ComputeIncomeLine`: `units = spec.UnitsOrDays ?? PendingHours ?? 0`, `base = CompensatoryTimeRules.HourlyRate(derived.DailySalary, stdHours ?? 8)`, `calculated = SettlementAmount(units, base, factor ?? 1)`, detail `"{units} h × {base}/h × factor {factor}"`. ISSS/AFP/Renta automáticas (concepto Ingreso, afecta las 3). **Retrocompatible: sin saldo/plaza-no-principal/módulo-sin-datos → contexto null → ni spec ni case → suite de liquidación intacta.** SIN cambio de contrato de la API de liquidación (input interno del motor). **(C) Tests**: **6 unit nuevos** en `SettlementCalculationRulesTests` (golden A.4-10 saldo 12 h/diario 20→base 2.50/monto **30.00**; tarifa 2.00→60.00; sin fondo→sin línea + aditividad; horas editadas 10→25.00; override 100 sobrevive; bases ISSS/AFP/Renta = 443.26 con la línea). **3 E2E nuevos** en `ApiIntegrationTests.CompensatoryTimeSettlement.cs`: (1) saldo 12 h → línea automática $30.00 + **manual→422 SETTLEMENT_CONCEPT_INVALID** + editar horas 10→$25.00 (sobrevive recalc) + regenerate→$30.00; (2) sin saldo→sin línea; (3) multi-plaza→línea solo en la plaza principal. **(D) openapi.yaml**: verificado válido (392 paths / 723 schemas) con TODOS los endpoints del módulo (maestro+activate/inactivate, créditos+documentos+read-url+annulment, ausencias+sugerencia+annulment, statement, movements query, exports movimientos+saldos); PR-6 no cambia superficie HTTP → guardrail `OpenApiContractGuardrails` verde. **(E) Guía FE** publicada: `docs/technical/guia-integracion-frontend-tiempo-compensatorio.md` (12 secciones: RBAC 2 permisos, catálogos+estados, maestro vacío+adopción, 4 preferencias, acreditaciones con adjunto obligatorio, ausencias+verificación de fondo, estado de cuenta, bandeja/exports, saldo perfil + integración liquidación [línea editable+regenerate+manual bloqueado], tabla de errores, flujo/adopción, notas de despliegue incl. M3 + P-15). **Suites: build `CLARIHR.slnx` 0 errores/0 warnings · unit `CLARIHR.Application.UnitTests` 2395/2395 (2389 base + 6 nuevos) · integración dirigida `~Settlement` 9/9 (6 liquidación existentes SIN regresión + 3 E2E nuevos) · `~CompensatoryTime` 11/11 (8 módulo + 3 E2E) · `MigrationSeeding`+`OpenApiContractGuardrails` 3/3 · migración M3 sin drift.** Sin correr la suite COMPLETA (la corre el orquestador). Archivos: `GlobalCatalogSeedData.cs`, `SettlementCalculationRulesTests.cs`, `ISettlementRepository.cs`, `SettlementCalculation.Rules.cs`, `Settlements.Handlers.cs`, `SettlementRepository.cs`, `SettlementContracts.cs`, `SettlementsController.cs`, migración M3 (new), `ApiIntegrationTests.CompensatoryTimeSettlement.cs` (new), guía FE (new). Siguiente: merge de REQ-001+REQ-002 + arrancar REQ-003.
- **2026-07-08** — **PR-5 COMPLETADO** (Bandeja + exportaciones de movimientos y saldos) sobre `feature/vacaciones-incapacidades`. **(A) Feature** (`Application/Features/PersonnelFiles/CompensatoryTime/CompensatoryTimeBandeja.cs`/`.Handlers.cs`, molde `IncapacitiesBandeja`): 3 queries (`QueryCompensatoryTimeMovementsQuery` + `ExportCompensatoryTimeMovementsQuery` + `ExportCompensatoryTimeBalancesQuery`) con validadores (PageNumber>0, PageSize 1-100, ToDate≥FromDate) + 3 handlers **con gate per-handler `EnsureCanViewCompensatoryTimeAsync`** (NO policy-set en el POST) + constantes `CompensatoryTimeMovementOperations` (ACREDITACION/AUSENCIA — NO son los codes ACREDITA/DEBITA/AMBAS del maestro). **(B) Controller** `CompensatoryTimeReportingController` (`[ApiController]`+`[Authorize]`, SIN `[AuthorizationPolicySet]`): `POST /companies/{id}/compensatory-time-movements/query` (rate-limit `Search`) + `GET …/compensatory-time-movements/export` + `GET …/compensatory-time-balances/export` (ambos rate-limit `Export`, 413 vía `ReportExportDeliveryService.SynchronousReadLimit`, `PersonnelFileErrors.ExportFormatInvalid`). Contrato `Api/Contracts/PersonnelFiles/CompensatoryTimeReportingContracts.cs` (`QueryCompensatoryTimeMovementsRequest`). **(C) Repo** (`ICompensatoryTimeRepository` +3: `QueryMovementsAsync`/`GetMovementExportRowsAsync`/`GetBalanceExportRowsAsync`): créditos+ausencias proyectados a un `MovementRow` común (crédito → +HoursCredited, FechaTrabajada, HorasTrabajadas, Factor; ausencia → −HoursDebited, StartDate/EndDate, join a `PayrollPeriodDefinitions` por `PayrollPeriodPublicId` para etiqueta+fechas del periodo imputado), left-join a `PersonnelFileEmployeeProfiles` para `EmployeeCode` (molde incapacidades); tablas heterogéneas → filtros a nivel BD, combinación+orden(fecha desc)+paginado en memoria. **StatusCounts sobre TODOS los estados** (GroupBy por tabla, respetando filtros no-estado, mergeados). **(D) Semántica de anulados**: `statusCode` explícito → ese estado exacto (permite pedir ANULADA); si no, `includeAnnulled` (default false) decide → **default excluye ANULADA** (`ResolveItemStatusFilter`). **(E) Export rows ES** (§3.9): `MovimientoTiempoCompensatorioExportRow` (Empleado, CodigoEmpleado, Operacion, Tipo, FechaInicio, FechaFin, HorasTrabajadas, Factor, Horas±, Detalle, AutorizadoPor, Estado, PeriodoPlanilla+Inicio/Fin, FechaRegistro) y `SaldoTiempoCompensatorioExportRow` (Empleado, CodigoEmpleado, TotalAcreditado, TotalDebitado, SaldoDisponible, UltimoMovimiento) — el saldo agrega SOLO REGISTRADA (cuadra con `GetBalanceAsync`/`CompensatoryTimeRules.Balance` por construcción; solo empleados con movimiento vigente). **(F) openapi.yaml a mano** (+3 paths → 392, +3 schemas → 723 [`QueryCompensatoryTimeMovementsRequest`/`CompensatoryTimeMovementListItemResponse`/`CompensatoryTimeMovementBandejaResponse`]; los export rows no llevan schema — el endpoint devuelve archivo, como incapacidades; YAML válido, refs OK). **SIN migración** (solo lectura sobre el dominio de M2) y **SIN códigos resx nuevos** (reusa `PERSONNEL_FILE_EXPORT_FORMAT_INVALID` → `BackendMessageLocalizationTests` intacto). **Decisiones**: (1) el `CompensatoryTimeReportingController` es empresa/company-scoped SIN autoservicio (bandeja/exports son insumo RRHH; el estado de cuenta por-empleado con rama self ya vive en el `…StatementController` de PR-4); (2) combinación en memoria de las 2 tablas (heterogéneas; el `operationCode` restringe a una sola cuando viaja); (3) StatusCounts cubren ambos estados aunque los items por defecto excluyan ANULADA. **Suites: build `CLARIHR.slnx` 0 errores/0 warnings · unit `CLARIHR.Application.UnitTests` 2389/2389 (sin unit nuevos — los validadores/handlers los cubre el DI + el round-trip) · integración dirigida `CompensatoryTime` 8/8 (7 de PR-3/PR-4 + 1 nuevo: bandeja default StatusCounts REGISTRADA=2/ANULADA=1 con anulados excluidos, filtro ANULADA explícito, includeAnnulled=3, filtro operación AUSENCIA con periodo imputado, export movimientos xlsx 200+content-type + json con periodo y signo, export saldos json 10/4/6, format inválido→400) + guardrails `OpenApiContractGuardrails`/`RateLimit`/`AuthorizationPolicyConvention`/`AllowedActionsCoverage`/`MigrationSeeding`/`PublicContract` 22/22 verde.** Sin correr la suite COMPLETA de integración (la corre el orquestador). Sin commit (lo hace el orquestador). Siguiente: PR-6 (integración liquidación + E2E + openapi final + guía FE — cierra REQ-002).
- **2026-07-08** — **PR-4 COMPLETADO** (Ausencias + fondo + estado de cuenta + saldo en perfil + carrera) sobre `feature/vacaciones-incapacidades`. **(A) Feature** (`Application/Features/PersonnelFiles/CompensatoryTime/`): `CompensatoryTimeAbsences.cs`/`.Handlers.cs` (5 handlers Add/Update/Annul + GetList/GetById + `GetAbsenceHoursSuggestion`; support `CompensatoryTimeAbsenceWriteSupport` = resolución de tipo DEBITA/AMBAS + imputación/solapes + asiento GOCE) y `CompensatoryTimeStatement.cs`/`.Handlers.cs` (estado de cuenta paginado). Contratos `Api/Contracts/PersonnelFiles/CompensatoryTimeAbsenceContracts.cs`. **(B) Endpoints**: `PersonnelFileCompensatoryTimeAbsencesController` (`GET/POST /personnel-files/{id}/compensatory-time-absences`, `GET/PUT /{absencePublicId}`, `PATCH /{absencePublicId}/annulment`, `GET /absence-hours-suggestion?start=&end=`) y `PersonnelFileCompensatoryTimeStatementController` (`GET /compensatory-time-statement` con filtros fromDate/toDate/tipo/estado/includeAnnulled + paginación page/pageSize [Range guardrail]); ambos `[AuthorizationPolicySet(ViewCompensatoryTime, ManageCompensatoryTime)]` SIN `[ResourceActions]`, If-Match, `xxxPublicId`. **(C) Verificación transaccional del fondo (R-T1, el gate)**: crear/editar ausencia toma `AcquireFundLockAsync` DENTRO de la tx ANTES de re-leer `GetBalanceAsync`; débito > saldo → 422 `COMPENSATORY_TIME_BALANCE_INSUFFICIENT` (plano — molde `VACATION_FUND_INSUFFICIENT`, `ProblemDetails` sólo expone code/traceId/details); editar re-verifica contra el saldo que EXCLUYE el débito viejo; anular ausencia restaura horas (no reduce saldo → sin riesgo, lock igual por consistencia). **Carrera probada E2E**: 2 POST concurrentes (`Task.WhenAll`) de 8h contra fondo 8 en fechas NO solapadas → **exactamente 1×201 + 1×422 BALANCE_INSUFFICIENT**, fondo drena a 0 (nunca negativo). **(D) Sugerencia**: `CompensatoryTimeRules.SuggestAbsenceHours` con asuetos del rango (`GetHolidaysInRangeAsync`, delega en el repo de vacaciones REQ-001) + restDay plaza (`GetPrimaryPlazaRestDayAsync`) → preferencia `CompanyRestDayOfWeek` → degradado (null, sin default domingo); horas estándar `CompensatoryTimeStandardDailyHours ?? 8`. **(E) Solapes (RN-05)**: ausencia propia (`HasOverlappingAbsenceAsync`, REGISTRADA) → 422 `..._ABSENCE_OVERLAP`; **cross-module aislados en `CheckCrossModuleOverlapAsync`** que REUSA `IncapacityRepository.HasOverlappingIncapacityAsync` → 422 `..._INCAPACITY_OVERLAP` y `VacationRepository.HasOverlappingRequestAsync` (SOLICITADA/APROBADA/DEVUELTA_PARCIAL) → 422 `..._VACATION_OVERLAP` (los 3 implementados; el `CompensatoryTimeRepository` inyecta ambos repos de REQ-001). **(F) Imputación opcional** a periodo de planilla: `PayrollPeriodExistsAsync(tenant, publicId, IsActive)` → 422 `..._PAYROLL_PERIOD_INVALID` si viaja e inválido. **(G) Asiento** `GOCE_TIEMPO_COMPENSATORIO=-9871` en la tx al crear (vigencias = start/end). **(H) Saldo en perfil**: `CompensatoryTimeHoursAvailable` (decimal?) aditivo en `PersonnelFileEmployeeProfileResponse` + poblado en `PersonnelFileEmployeeRepository.ComputeCompensatoryTimeHoursAvailableAsync` (compute post-proyección, gotcha member-init; null sin movimientos; misma agregación `CompensatoryTimeRules.Balance` que el statement → cuadra por construcción). **(I) Estado de cuenta**: `GetStatementPageAsync` fetch filtrado crédito+ausencia → `BuildStatement` sobre el SET COMPLETO (saldo corrido con offset acumulado, R-T9) → slice de página; totales `totalCredited`/`totalDebited`/`availableBalance` sobre el set filtrado; anulaciones excluidas por defecto; RETIRADO bloquea toda escritura (`EMPLOYEE_PROFILE_RETIRED_LOCKED`, aclaración №9). **(J) resx EN/ES**: 5 códigos nuevos (`COMPENSATORY_TIME_BALANCE_INSUFFICIENT`/`_ABSENCE_OVERLAP`/`_INCAPACITY_OVERLAP`/`_VACATION_OVERLAP`/`_PAYROLL_PERIOD_INVALID`); type/state/operation-mismatch reusan los códigos de PR-3 (una entrada resx cada uno); validadores SIN `.WithMessage` → cero `validation.message.*` nuevos. **(K) openapi.yaml a mano** (+5 paths → 389, +7 schemas → 720; `compensatoryTimeHoursAvailable` en el schema de perfil; YAML válido). **SIN migración** (dominio de ausencias ya en M2). **Decisiones**: (1) el 422 de fondo insuficiente es PLANO (molde PR-3/vacaciones; `saldoDisponible`/`horasFaltantes` no viajan porque el `Error`/ProblemDetails no soporta extensions numéricas); (2) statement por-empleado (bandeja empresa/exports = PR-5); (3) los 3 solapes cross-module implementados vía delegación a los repos ya probados de REQ-001, con test de integración del solape propio (ausencia); los cross-module descansan en la cobertura de REQ-001. **Suites: build `CLARIHR.slnx` 0 errores/0 warnings · unit `CLARIHR.Application.UnitTests` 2389/2389 (sin unit nuevos — golden A.4 cubre reglas) · integración dirigida `CompensatoryTime` 7/7 (incluida la carrera: 1×201 + 1×422) + guardrails `AuthorizationPolicyConvention`/`PublicContractGuardrails`/`AllowedActionsCoverage`/`MigrationSeeding`/`OpenApiContractGuardrails` 13/13 verde.** Sin correr la suite COMPLETA de integración (la corre el orquestador). Sin commit (lo hace el orquestador). Siguiente: PR-5 (bandeja + exportaciones).
- **2026-07-08** — **PR-3 COMPLETADO** (Acreditaciones end-to-end + adjunto obligatorio en el POST + sub-recurso documentos + asiento + anti-descubierto bajo lock) sobre `feature/vacaciones-incapacidades`. **(A) Feature** (`Application/Features/PersonnelFiles/CompensatoryTime/`, molde exacto incapacidades PR-5): `CompensatoryTimeCredits.cs`/`.Handlers.cs` (5 handlers: Add/Update/Annul + GetList/GetById; `CompensatoryTimeCreditWriteSupport` = resolución de documento + cálculo `ResolveCreditedHours` + `ValidateDeclarative` fecha≤hoy/rango + journal) y `CompensatoryTimeCreditDocuments.cs`/`.Handlers.cs` (list/byId/read-url/add/delete, espejo `IncapacityDocuments`). Namespace `CLARIHR.Application.Features.PersonnelFiles` (como incapacidades); record `CompensatoryTimeTypeRef` para el snapshot. **(B) Cálculo**: `HoursCredited = CompensatoryTimeRules.CreditedHours(hoursWorked, factor)` con snapshot del factor vía `ApplyCreditedHours`; override manual (`hoursCreditedOverride` + `overrideNote` obligatoria → 422 `COMPENSATORY_TIME_OVERRIDE_NOTE_REQUIRED`; factor 2.00×4h→8.00 verificado E2E). **(C) Adjunto obligatorio (patrón nuevo)**: el POST lleva `authorizationFilePublicId`; con `CompensatoryTimeCreditRequiresDocument ?? true` es obligatorio (`COMPENSATORY_TIME_DOCUMENT_REQUIRED`), gate de purpose (existe→Active→mismo tenant→`Purpose==CompensatoryTimeDocument`; ajeno→`COMPENSATORY_TIME_DOCUMENT_PURPOSE_INVALID`), documento creado en la MISMA tx; `AuthorizerFilePublicId` del crédito = PublicId del archivo resuelto. **(D) Validaciones**: tipo activo (`COMPENSATORY_TIME_TYPE_INVALID`) + operación (DEBITA→`COMPENSATORY_TIME_TYPE_OPERATION_MISMATCH`, solo ACREDITA/AMBAS); `workDate ≤ hoy` vía `IDateTimeProvider` (`COMPENSATORY_TIME_WORK_DATE_IN_FUTURE`); rango horario (`COMPENSATORY_TIME_TIME_RANGE_INVALID`); tope `CompensatoryTimeMaxBalanceHours` (`COMPENSATORY_TIME_MAX_BALANCE_EXCEEDED`). **(E) Anti-descubierto bajo lock (R-T1)**: los 3 escritores (crear/editar/anular) toman `AcquireFundLockAsync(tenant, expediente)` DENTRO de la tx y re-leen `GetBalanceAsync` antes de mutar; editar-a-la-baja o anular que dejaría el saldo negativo → 422 `COMPENSATORY_TIME_BALANCE_WOULD_GO_NEGATIVE` (crear que rebasa el tope → `..._MAX_BALANCE_EXCEEDED`). **Anti-descubierto probado E2E** sembrando el débito directamente en BD (`PersonnelFileCompensatoryTimeAbsence` REGISTRADA, helper `SeedCompensatoryDebitAsync`, patrón PR-7 de REQ-001, ya que el endpoint de ausencias es PR-4): fondo 12+3−8=7, anular el crédito de 12 → 422; anular el de 3 (7−3=4≥0) → 200. **(F) Asiento** `ACREDITACION_TIEMPO_COMPENSATORIO` en la tx al crear (`PersonnelFilePersonnelAction.Create(..., "APLICADA", isSystemGenerated:true)`). **(G) Gates**: escrituras Manage-only (`LoadForManageCompensatoryTimeAsync` nuevo en la COMMAND base — trampa: la READ-query base es una subclase, la primera colocación equivocada rompió el build; F1 sin rama self, D-01); lecturas View OR self (`LoadCompletedEmployeeForCompensatoryTimeReadAsync` en la read-query base). **(H) Perfil RETIRADO** → 422 en toda escritura vía error compartido nuevo `EMPLOYEE_PROFILE_RETIRED_LOCKED` en `PersonnelFileErrors` + `ICompensatoryTimeRepository.IsProfileRetiredAsync` (query a `PersonnelFileEmployeeProfiles`, status == `RETIRADO`); reusable por PR-4. **(I) Guard de uso del maestro** `COMPENSATORY_TIME_TYPE_IN_USE` ya NO diferido: `CompensatoryTimeTypeRepository.IsInUseAsync` ahora resuelve el id interno y checa créditos+ausencias REGISTRADA. **(J) Controllers** `PersonnelFileCompensatoryTimeCreditsController` (`GET/POST` + `GET/PUT /{id}` + `PATCH /{id}/annulment`) y `PersonnelFileCompensatoryTimeCreditDocumentsController` (`GET/POST /documents`, `GET/DELETE /documents/{docId}`, `GET /documents/{docId}/read-url`), ambos `[AuthorizationPolicySet(ViewCompensatoryTime, ManageCompensatoryTime)]` SIN `[ResourceActions]`, If-Match, DELETE→`parentConcurrencyToken`. **(K) resx EN/ES**: 11 códigos del módulo (`COMPENSATORY_TIME_TYPE_INVALID`/`_OPERATION_MISMATCH`/`_WORK_DATE_IN_FUTURE`/`_TIME_RANGE_INVALID`/`_DOCUMENT_REQUIRED`/`_DOCUMENT_PURPOSE_INVALID`/`_OVERRIDE_NOTE_REQUIRED`/`_MAX_BALANCE_EXCEEDED`/`_BALANCE_WOULD_GO_NEGATIVE`/`_ANNULMENT_REASON_REQUIRED`/`_STATE_RULE_VIOLATION`) + `EMPLOYEE_PROFILE_RETIRED_LOCKED`. **(L) openapi.yaml a mano** (+6 paths → 384, +7 schemas → 713; YAML válido). **Decisiones**: (1) validadores SIN `.WithMessage` (mensajes por defecto FluentValidation) → cero claves `validation.message.*` nuevas; (2) los 422 `MAX_BALANCE_EXCEEDED`/`BALANCE_WOULD_GO_NEGATIVE` son errores planos (el molde `VACATION_FUND_INSUFFICIENT` tampoco lleva extensions numéricas — `ProblemDetails` solo expone `code`/`traceId`/`details`); (3) input de negocio compartido crear/editar + campos de adjunto SOLO en el comando de creación (más limpio que el molde incapacidad que arrastra el adjunto en el PUT). **SIN migración** (dominio en M2). **Suites: build `CLARIHR.slnx` 0 errores/0 warnings · unit `CLARIHR.Application.UnitTests` 2389/2389 (línea base PR-2, sin unit nuevos — el golden A.4 ya cubre las reglas) · integración dirigida `ApiIntegrationTests.CompensatoryTime` 3/3 (HR round-trip+8 guards, preferencia-off, anti-descubierto) + guardrails `AllowedActionsCoverage`/`MigrationSeeding`/`OpenApiContractGuardrails`/`AuthorizationPolicyConvention` 7/7 verde.** Sin correr la suite COMPLETA de integración (la corre el orquestador). Sin commit (lo hace el orquestador). Siguiente: PR-4 (ausencias + fondo + estado de cuenta + saldo en perfil + carrera).
- **2026-07-08** — **PR-2 COMPLETADO — GATE DE LA OLA EN VERDE** (Dominio + reglas puras + M2 + golden A.4) sobre `feature/vacaciones-incapacidades`. **(A) Dominio** (`Domain/PersonnelFiles/PersonnelFileCompensatoryTime.cs`, molde `PersonnelFileIncapacity`): const `CompensatoryTimeStatuses` (REGISTRADA/ANULADA + set `Vigentes` + predicado `IsVigente`); **`PersonnelFileCompensatoryTimeCredit`** (FK expediente + `BindToPersonnelFile`; FK dura `CompensatoryTimeTypeId` + `TypeNameSnapshot`; `WorkDate` DateOnly, `StartTime?`/`EndTime?` TimeOnly coherentes si ambas viajan; `HoursWorked` numeric(5,2)>0; `FactorApplied`/`HoursCredited` escritos SOLO por `ApplyCreditedHours(factor, hours, isOverridden, note)` — nota obligatoria si override; `WorkDetail`(500)/`AuthorizedByText`(200) req; `AuthorizerFilePublicId?`/`AssignedPositionPublicId?`; **`OvertimeRecordPublicId?`** costura D-21 sin FK; anulación `AnnulmentReason`/`AnnulledByUserId`/`AnnulledUtc`; guards `Create`/`Update`(solo REGISTRADA)/`Annul`(motivo obligatorio, solo REGISTRADA)); **`PersonnelFileCompensatoryTimeCreditDocument`** (espejo exacto del incapacity doc, `CreditId`+`BindToCredit`, doc-type opcional); **`PersonnelFileCompensatoryTimeAbsence`** (FK expediente; tipo+snapshot; `StartDate`/`EndDate` inicio≤fin, futuras OK; `HoursDebited` numeric(6,2)>0; `Reason`(500) req; `PayrollPeriodPublicId?` opcional; guards `Create`/`Update`/`Annul`). La validación fecha≤hoy y el invariante de saldo NO son guards de dominio (van en reglas+handler bajo lock — PR-3/PR-4). **(B) Reglas puras** (`Application/Features/PersonnelFiles/CompensatoryTime/CompensatoryTimeRules.cs`, **público** para que Infra lo consuma; molde `IncapacityCalculationRules`, `Round2` AwayFromZero): `CreditedHours(worked, factor)`, `Balance(cred, deb)`, `BuildStatement(movements[], openingBalance=0)` (orden fecha+`CreatedUtc`, saldo corrido, ANULADA marcado+excluido, totales), `SuggestAbsenceHours(start, end, restDay?, holidays, stdHours)` (excluye restDay+asuetos; degradado sin calendario = días×horas), `ValidateDebit`/`MaxAnnullable`/`MaxCreditable(balance, cap?)`, `HourlyRate(dailySalary, stdHours)`, `SettlementAmount(hours, rate, factor)` (D-19, consumidos por liquidación en PR-6). Records `CompensatoryTimeMovement`/`...StatementLine`/`...Statement`/`...DebitCheck`. **(C) Repositorio** (`Abstractions/PersonnelFiles/ICompensatoryTimeRepository.cs` + impl `Infrastructure/PersonnelFiles/CompensatoryTimeRepository.cs`, registrado en DI): `GetBalanceAsync` (Σ créditos − Σ débitos REGISTRADA, delega en `Rules.Balance` — única fuente) + `GetStatementAsync` (proyección común crédito/ausencia concat + `BuildStatement`) + `AcquireFundLockAsync` (`pg_advisory_xact_lock`; **default no-op en la interfaz** para fakes; class id `0x43544644` "CTFD", key `tenantKey ^ (int)personnelFileId`). Bandeja/export/solapes **diferidos a PR-3/PR-4/PR-5** (no declarados aún). **(D) Configs EF** (`Persistence/Configurations/PersonnelFiles/PersonnelFileCompensatoryTimeConfiguration.cs`, 3 clases; nombres ≤63 chars con prefijo `pf_comp_time_*`): CHECKs `hours_worked>0`/`hours_credited>0`/`factor_applied>0` (créditos), `hours_debited>0`/`start_date<=end_date` (ausencias); índices `(tenant, file, status)`+`(tenant, work_date)` créditos y `(tenant, file, status)`+`(tenant, file, start, end)` ausencias; FKs personnel_file (Cascade) + tipo (Restrict) + doc-type (Restrict); 3 DbSets. **(E) Migración M2** `20260708193657_AddPersonnelFileCompensatoryTime`: 3 CreateTable (`personnel_file_compensatory_time_credits`/`..._credit_documents`/`..._absences`) + 5 CHECKs + índices; **drift limpio** (`has-pending-model-changes` vacío). (Dos índices auto-FK con nombre truncado `~` a 63 chars — patrón Npgsql ya presente en 6 migraciones previas.) **(F) Golden A.4 (GATE)** `tests/.../CompensatoryTimeRulesTests.cs` (18 casos) + batería de dominio `CompensatoryTimeDomainTests.cs` (28 casos): A.4-1/3 (crédito 3h→3.00, acumulación 8+4→12, débito 8→4, débito 8 vs saldo 3→faltante 5.00), A.4-4 (factor 2.00×4h→8.00 + Round2 con céntimos), A.4-5 (`MaxAnnullable`), A.4-7 (tope 40/saldo 38→2.00; sin tope→null), A.4-8 (sugerencia lun-mié asueto martes descanso domingo→16h; degradado→24h), A.4-10 (`HourlyRate(20,8)=2.50`, `SettlementAmount(12,2.50,1.00)=30.00`, tarifa 2.00→60.00), `BuildStatement` (orden/saldo corrido/anulados/offset). **Suites: build `CLARIHR.slnx` 0 errores/0 warnings · unit `CLARIHR.Application.UnitTests` 2389/2389 (2343 base + 46 nuevos) · migración M2 sin drift.** Sin correr la suite COMPLETA de integración (la corre el orquestador). Sin commit (lo hace el orquestador). Siguiente: PR-3 (acreditaciones end-to-end + adjunto obligatorio en el POST).
- **2026-07-08** — **PR-1 COMPLETADO** (Configuración, M1) sobre la rama `feature/vacaciones-incapacidades` (misma rama, sin crear `feature/tiempo-compensatorio`). **(A) Maestro governed `CompensatoryTimeType`** (`Domain/Leave/CompensatoryTimeType.cs` + consts `CompensatoryTimeOperations` ACREDITA/DEBITA/AMBAS, molde `IncapacityType`/`MedicalClinic`): `Code`/`Name`/`OperationCode`/`CreditFactor` (numeric(5,2)>0, default 1.00, guard en ctor)/`SortOrder`; feature CRUD (`Features/Leave/CompensatoryTimeTypes.cs`/`.Handlers.cs`), repo (`ICompensatoryTimeTypeRepository`/impl), controller governed (`companies/{id}/compensatory-time-types` + `/{id}` con activate/inactivate, `[AuthorizationPolicySet(LeaveConfigurationPolicies.Read/Manage)]` + `[ResourceActions(COMPENSATORY_TIME_TYPES)]`, `ISupportsAllowedActions` en los DTOs), registrado en `AllowedActionsRegistry`. Reusa `ILeaveConfigurationAuthorizationService` (permisos `LeaveConfiguration.Read/Admin`, sin permisos de maestro nuevos). Índice único **filtrado** `uq_compensatory_time_types__tenant_code_active WHERE is_active` + CHECK `credit_factor > 0`. **Guard de uso `COMPENSATORY_TIME_TYPE_IN_USE` DIFERIDO**: `IsInUseAsync` devuelve `false` (las tablas crédito/ausencia son M2/PR-2). **(B) 2 TPH** receta 8 toques: `compensatory-time-statuses` (`REGISTRADA=-9865`/`ANULADA=-9866`) y `compensatory-time-operations` (`ACREDITA=-9867`/`DEBITA=-9868`/`AMBAS=-9869`) — idx de operations acortado a `__active_sort` (el `__country_active_sort` pasaba 63 chars). **(C) 2 ActionTypes** en ACTION_TYPE_CATALOG: `ACREDITACION_TIEMPO_COMPENSATORIO=-9870`, `GOCE_TIEMPO_COMPENSATORIO=-9871` (sort 210/220). **(D) 2 permisos** `PersonnelFiles.ViewCompensatoryTime` (policy authn-only `.Combine`) / `ManageCompensatoryTime` (policy RequireAssertion fallback Admin/ManageAdministration — HR-only Fase 1): codes+policies, gates default fail-closed en la interfaz + overrides en `PersonnelFileAuthorizationService`, tuplas provisioning, `Program.cs`, lista canónica del governance test. **Gates de handler (`LoadForManage…`, `LoadCompletedEmployeeFor…Read`) NO en PR-1** (PR-3/PR-4; ningún guardrail los exigió). **(E) Preferencias +4 columnas** anulables `CompensatoryTimeStandardDailyHours`(4,2, null=8)/`CompensatoryTimeMaxBalanceHours`(6,2, null=sin tope)/`CompensatoryTimeCreditRequiresDocument`(bool?, null=sí)/`CompensatoryTimeSettlementRateFactor`(5,2, null=1.00) + `SetCompensatoryTimePolicies` (valida >0) + PUT admin + response. **(F) `FilePurpose.CompensatoryTimeDocument`** + bloque `Storage:Purposes` en `appsettings.json` base (solo PDF, contenedor `clarihr-compensatory-time-documents`; Development.json no lo requiere — igual que IncapacityDocument). **(G) Migración M1** `20260708190420_AddCompensatoryTimeConfiguration`: 3 CreateTable (`compensatory_time_types` + CHECK + 2 TPH) + 3 InsertData (2 status, 3 operation, 2 ActionTypes) + 4 AddColumn en `company_preferences`; **drift limpio** (`has-pending-model-changes` vacío). **(H) resx** EN/ES: `COMPENSATORY_TIME_TYPE_NOT_FOUND`/`_CODE_CONFLICT`/`_IN_USE` + `validation.message.operation_code_must_be_acredita_debita_or_ambas`. **(I) openapi.yaml a mano**: +4 paths (378) y +5 schemas (706) del maestro, +4 campos de preferencias en request/response, wire keys nuevas en la descripción de general-catalogs; YAML válido. **Suites: build `CLARIHR.slnx` 0 errores/0 warnings · unit `CLARIHR.Application.UnitTests` 2343/2343 · guardrails integración dirigidos (AllowedActionsCoverage + MigrationSeeding + OpenApiContractGuardrails) 5/5 · guardrails unit (GeneralCatalogKeyMap + OpenApi) 96/96 · migración sin drift.** Sin correr la suite COMPLETA de integración (la corre el orquestador). Sin commit (lo hace el orquestador). Siguiente: PR-2 (dominio + reglas + golden A.4).
- **2026-07-05** — Análisis de negocio creado en borrador (validación contra código + GAP): greenfield total verificado (solo existen los conceptos aislados `HORAS_EXTRA` de compensación y `HORAS_EXTRAS_PENDIENTES` de liquidación — este último es la costura F2 para el saldo al retiro); D-01…D-18 propuestas, P-01…P-14 abiertas; plantilla de tipos y seeds tentativos en anexos. Sin commit. Pendiente: ratificación del negocio.
- **2026-07-05** — **RATIFICADO por el negocio**: todas las D aceptadas y P-01…P-14 respondidas. Ajustes de alcance F1 respecto del borrador: (1) **el saldo al retiro se paga** — la liquidación agrega automáticamente la línea `HORAS_EXTRAS_PENDIENTES` con el saldo (D-19, RF-013; tarifa default 1.00 → P-15 segunda ronda); (2) **adjunto de autorización de jefatura (PDF) obligatorio** al acreditar (D-20, RF-012 — nuevo FilePurpose + storage + contenedor); (3) el **maestro de tipos inicia vacío**, sin plantilla ni load-template (D-05 ajustada; A.2 pasa a catálogo de referencia); (4) **costura `overtimeRecordPublicId`** hacia el futuro módulo de horas extras (D-21). Documento reescrito como RATIFICADO (D-01…D-21). Sin commit. Siguiente: plan técnico.
- **2026-07-05** — **Plan técnico escrito** (`plan-tecnico-tiempo-compensatorio.md`, PR-1…PR-6) sobre exploración quirúrgica del código: integración liquidación verificada (línea `PersonnelFileSettlementLine` con nombres reales `UnitsOrDays`/`UnitsOverridden`/`OverrideAmount`; `HORAS_EXTRAS_PENDIENTES` hoy solo-manual seed `-9837L` — se convierte en línea de sistema condicional vía spec + `case` nuevo en `ComputeIncomeLine`, catálogo sin cambios; contexto en un viaje vía `SettlementCalculationContext`; **solo plaza principal** anti doble pago; guard anti-duplicado manual); anti-carrera del saldo con `pg_advisory_xact_lock` (precedente PositionSlots/Companies); adjunto obligatorio en POST = **patrón nuevo verificado sin precedente** (gate de purpose espejo medical-claims); corrección documentada al plan de REQ-001 (§0.10). Checklist de PRs poblado. Sin commit. Desarrollo espera merge de REQ-001.

---

## REQ-003 — Otras transacciones de personal (reconocimientos, amonestaciones y disponibilidad de tiempos)

| | |
|---|---|
| **Estado** | 🟢 COMPLETADO (PR-1…PR-6 ✅ en `feature/vacaciones-incapacidades`; guía FE publicada; openapi sin drift). **Pendiente de merge a master + ejecución del checklist de despliegue** (infra: migraciones M1-M2, 2 storage purposes + contenedores, `load-template` — abajo). |
| **Análisis de negocio** | [`docs/business/analisis-otras-transacciones-personal.md`](business/analisis-otras-transacciones-personal.md) — **D-01…D-18 ratificadas sin ajustes** + **P-01…P-14 respondidas** (P-14: «no hay multas» → sin advertencia legal en F1; capacidad de descuento intacta; plantilla de causas sin conceptos) |
| **Plan técnico** | [`docs/technical/plan-tecnico-otras-transacciones-personal.md`](technical/plan-tecnico-otras-transacciones-personal.md) — **escrito 2026-07-05**: §0 aclaraciones (12) · §3 arquitectura · §3.11 consulta de disponibilidad (contrato + `activeSources[]`) · §7 orden de PRs (PR-1…PR-6) |
| **Guía FE** | ✅ [`docs/technical/guia-integracion-frontend-otras-transacciones-personal.md`](technical/guia-integracion-frontend-otras-transacciones-personal.md) (publicada en PR-6) |
| **Rama** | `feature/vacaciones-incapacidades` (REQs acumulados sobre la misma rama) |
| **Línea base de tests** | unit 2395 (post-REQ-002); tras PR-1 → **unit 2395** (PR-1 no añade tests unitarios propios); tras PR-2 → **unit 2451/2451** (2395 base + 56: 36 `PersonnelTransactionRulesTests` golden A.4 + 20 `PersonnelTransactionDomainTests`); migración M2 sin drift; **tras PR-3 → unit 2451/2451** (sin cambio — PR-3 es integración) + **integración dirigida `~Recognition` 6/6 verde** (`ApiIntegrationTests.PersonnelTransactions.cs`); sin migración; **tras PR-4 → unit 2451/2451** (sin cambio — PR-4 es integración) + **integración dirigida `~DisciplinaryAction` 6/6 verde + `~Recognition` 6/6 sin regresión**; sin migración; **tras PR-5 → unit 2451/2451** (sin cambio — PR-5 es integración; localización EN/ES verde) + **integración dirigida `~Recognition|~DisciplinaryAction` 13/13 verde** (11 PR-3/PR-4 sin regresión + 2 PR-5: bandejas StatusCounts + default excluye anulados + export xlsx; insumo 2 filas por efecto + revocada excluida + rango faltante 422) + `~OpenApi` 1/1; sin migración |
| **Alcance (síntesis)** | Reconocimientos y amonestaciones como entidades del expediente con **flujo de una decisión** (`EN_REVISION → APLICADA/RECHAZADA` + `ANULADA`/revocación; anti-autoaprobación **doble**; `Authorize*` excluye Admin) · amonestación con **suspensión sin goce** (rango de fechas si el tipo tiene `appliesSuspension`; sin cambio automático de estado — `SUSPENDIDO=-9101` queda manual) y **descuento documental** (monto + concepto de egreso del catálogo país, snapshot desde la causa; export insumo planilla — sin escribir ledgers) · 3 **maestros por empresa** con plantilla (tipos de reconocimiento / tipos de amonestación / causas) · asientos automáticos reutilizando `AMONESTACION=-9477`/`SUSPENSION=-9478` + `RECONOCIMIENTO` nuevo · adjuntos (2 purposes) · bandejas/exports · **consulta de disponibilidad de tiempos** por fuentes conectables (F1: suspensiones + fin de contratos temporales vía `ContractTypeCatalogItem.IsTemporary` + `EndDate` de plaza; REQ-001/REQ-002 conectan vacaciones/incapacidades/permisos) con permiso dedicado `ViewTimeAvailability` y payload mínimo |
| **Dependencias** | Secuenciar **después de REQ-001 y REQ-002** (patrón de plantilla de maestros nace en REQ-001 PR-1 + 3 fuentes de la consulta); seeds en bloque **≤ -9875** (REQ-002 propone -9865…-9871; trampa vigente: -9490…-9496 `ACTION_STATUS_CATALOG`); storage: 2 purposes + contenedores |

### Checklist de PRs (fuente: plan técnico §7 — detalle por PR en el plan)
- [x] **PR-1** — Configuración (M1): 3 maestros governed + `EmployeeRelationsTemplateSeeder` + `load-template` + TPH `personnel-transaction-statuses` (`-9875…-9878`) + ActionType `RECONOCIMIENTO` (`-9879`) + 7 permisos/policies/gates (2 `Authorize*` sin Admin) + 2 `FilePurpose` + appsettings base + openapi temprano — plan §3.1-3.3 ✅ 2026-07-08
- [x] **PR-2** — Dominio + reglas (M2): 4 entidades + **mutador `Annul()` en `PersonnelFilePersonnelAction`** + configs EF + índices/CHECKs + `PersonnelTransactionRules` puro con **golden A.4 en verde (gate)** + repositorio (solapes + lock advisory) — plan §3.4-3.6 ✅ 2026-07-08
- [x] **PR-3** — Reconocimientos end-to-end: CRUD `EN_REVISION` + decisión (Authorize, anti-self doble) + anulación/revocación con asiento + adjuntos + rama self solo-`APLICADA` — plan §3.7-3.10 ✅ 2026-07-08
- [x] **PR-4** — Amonestaciones end-to-end: suspensión (flag del tipo, solape bajo lock) + descuento (snapshot al aplicar) + doble asiento (`AMONESTACION`/`SUSPENSION`) + revocación integral — plan §3.7-3.10 ✅ 2026-07-08
- [x] **PR-5** — Bandejas + exportaciones por familia + **insumo de planilla** (efectos DESCUENTO/SUSPENSION del rango) con rate limits — plan §3.9 ✅ 2026-07-08
- [x] **PR-6** — Consulta de disponibilidad de tiempos (query + export + `activeSources[]`, 2 fuentes F1: suspensiones + `IsTemporary`+`EndDate`) + suite E2E + `openapi.yaml` final + **guía FE** — plan §3.11 ✅ 2026-07-08 (**cierra REQ-003**)

### Pendientes de despliegue (checklist §9 del plan)
- [ ] Migraciones M1-M2 aplicadas
- [ ] `Storage:Purposes:RecognitionDocument` y `Storage:Purposes:DisciplinaryActionDocument` en appsettings **base** + contenedores `clarihr-recognition-documents`/`clarihr-disciplinary-action-documents` aprovisionados
- [ ] `load-template` de los 3 maestros ejecutado en el tenant productivo (causas **sin** concepto de descuento — P-14; cada empresa asocia conceptos si su marco lo permite)

### Próxima acción
> **REQ-003 COMPLETADO (PR-6, 2026-07-08).** Todos los PRs ✅, guía FE publicada, openapi sin drift (426 paths / 764 schemas), consulta de disponibilidad con las 2 fuentes F1 + E2E verde. Falta solo el **merge a master** (orquestador) y la ejecución del **checklist de despliegue** (infra/operación — arriba: migraciones M1-M2, 2 `Storage:Purposes` base + contenedores, `load-template`, 7 permisos a roles). **Arrancar REQ-004 (Tablero de gráficos e indicadores de acciones de personal)**: es el siguiente 🔴 en orden y **no depende de REQ-001…003** (read-only sobre módulos existentes). Al arrancar REQ-004, seguir su «Próxima acción» (crear `feature/tablero-acciones-personal` desde `master`, anotar línea base, sembrar `payroll-types` con los **IDs re-ubicados `-9890…-9895`** —⚠️ los tentativos `-9520…-9525` COLISIONAN con `ECONOMIC_AID_TYPE_CATALOG`—, PR-1 §3.1/§4) y recordar: la limpieza de `payroll_type_code` es destructiva (registrar conteo previo); el contrato es 100% aditivo (dashboard PR #52 intacto); `sections[]` en metadata es el espejo de `activeSources[]` de esta consulta de disponibilidad.

### Bitácora
- **2026-07-08** — **PR-6 COMPLETADO — CIERRA REQ-003** (consulta de disponibilidad de tiempos + suite E2E + openapi final + guía FE) sobre `feature/vacaciones-incapacidades` (molde `EmployeeRelationsReportingController`). **(A) Feature** `Application/Features/PersonnelFiles/PersonnelTransactions/TimeAvailability.cs`/`.Handlers.cs`: wire types (`TimeAvailabilityRowResponse` payload mínimo P-10 · `TimeAvailabilityQueryResponse` con `categoryCounts` + `activeSources[]`), consts `TimeAvailabilityCategories` (`SUSPENSION`/`FIN_CONTRATO_TEMPORAL` + `ActiveSources` fija) y `TimeAvailabilitySourceModules` (`EMPLOYEE_RELATIONS`/`EMPLOYMENT`), `DisponibilidadTiempoExportRow` (ES), errores `TIME_AVAILABILITY_RANGE_REQUIRED`/`_RANGE_INVALID` (ambos 422). Handler `TimeAvailabilitySupport` compartido (query+export): gate `EnsureCanViewTimeAvailabilityAsync` → valida rango (`ResolveWindow`: null→RangeRequired, `BuildAvailabilityWindow` inválido→RangeInvalid) → selecciona fuentes por `categoryCodes` (vacío=ambas) → concatena → ordena (startDate asc, empleado, referencePublicId) → pagina; `categoryCounts` inicializa las categorías INCLUIDAS a 0. **(B) Controller** `TimeAvailabilityController` (`[Authorize]`, SIN `[AuthorizationPolicySet]` — gate per-handler como las bandejas; rate-limit Search/Export): `POST /companies/{companyId}/time-availability/query` + `GET …/time-availability/export?format=…&startDate=…&endDate=…&personnelFilePublicId=…&categoryCodes=…&orgUnitPublicId=…`; request DTO `TimeAvailabilityQueryRequest` en `EmployeeRelationsReportingContracts.cs`. **(C) 2 fuentes F1 = 2 métodos del repo** (`IPersonnelTransactionRepository`+impl): `GetSuspensionAvailabilityRowsAsync` = amonestaciones `APLICADA` cuyo bloque de suspensión **intersecta** el rango (`suspStart ≤ window.End && suspEnd ≥ window.Start`, fechas reales, `days=SuspensionDays`, `statusCode=APLICADA`, `referencePublicId`=amonestación, left-join profile+PositionSlot); `GetTemporaryContractEndRowsAsync` = resuelve país del tenant → junta los `NormalizedCode` de contract-types `IsTemporary=true` → asignaciones activas con ese código y `EndDate ∈ rango` (`upper(contract_type_code)=ANY(codes)`, dos pasos para evitar función en join-key; `end_date` timestamptz comparado contra bounds UTC-midnight inclusivos), fila `startDate=endDate=EndDate`, `days=1`, `statusCode=VIGENTE`, `referencePublicId`=plaza. Filtros empleado (publicId) + orgUnit (source-1 vía EXISTS asignación activa; source-2 vía `assignment.OrgUnitPublicId`). **(D) openapi.yaml a mano**: +2 paths (→ **426**) + 3 schemas (`TimeAvailabilityQueryRequest`/`RowResponse`/`QueryResponse` → **764**); YAML válido, refs OK, guardrail `OpenApiContractGuardrails` verde. **(E) resx EN/ES** 2 códigos nuevos (`TIME_AVAILABILITY_RANGE_REQUIRED`/`_RANGE_INVALID`); es-SV cae en fallback a es (paridad EN/es únicamente, como el resto del módulo). **(F) Guía FE** publicada: `docs/technical/guia-integracion-frontend-otras-transacciones-personal.md` (11 secciones: RBAC 7 permisos [2 `Authorize*` sin Admin], catálogos+2 máquinas de estado, maestros+load-template, reconocimientos [una decisión+anti-self doble+revocación+self solo-APLICADA], amonestaciones [suspensión+descuento+doble asiento+snapshot], bandejas/exports+**insumo de planilla**, coexistencia con el asiento manual RN-16, **consulta de disponibilidad** [contrato estable + `activeSources[]` + degradación + fuentes futuras REQ-001/002 como costura], tabla de errores, flujo/adopción, notas de despliegue). **Decisiones de diseño**: (1) `activeSources` = las 2 fijas de F1 SIEMPRE (representa «familias conectadas», no «con datos»); (2) el orden y la paginación se hacen en el handler tras concatenar las 2 fuentes (rango acotado = conjunto chico), conectar REQ-001/002 = método nuevo del repo + categoría en `ActiveSources` sin tocar el contrato; (3) el `export` reusa `ReportExportDeliveryService` (rows sin schema OpenAPI, igual que las demás exportaciones del módulo); (4) el 422 de rango se valida EN EL HANDLER (no FluentValidation) para no volverlo 400. **SIN migración, SIN seed** (PR-6 solo lectura derivada). **Suites: build `CLARIHR.slnx` 0 errores/0 warnings · unit `CLARIHR.Application.UnitTests` 2451/2451 (sin cambio — PR-6 es integración; `BuildAvailabilityWindow`/`RangesOverlap` ya cubiertos por los golden A.4-6 de PR-2) · integración dirigida `~Recognition|~DisciplinaryAction|~TimeAvailability|~PersonnelTransactions` 15/15 verde (14 PR-3/4/5 SIN regresión + 1 E2E nuevo) + `~OpenApi` 1/1.** El E2E nuevo (A.4-6): rango 2026-06-01..15 con suspensión aplicada 05-28→06-03 (intersección parcial) → fila SUSPENSION con fechas reales+days 7+referencePublicId; plaza PLAZO_FIJO `EndDate=06-10` → FIN_CONTRATO_TEMPORAL days=1 VIGENTE; plaza INDEFINIDO `EndDate=06-12` → NO aparece; `activeSources`=2; `categoryCounts` {SUSPENSION:1, FIN_CONTRATO_TEMPORAL:1}; `categoryCodes=["SUSPENSION"]` excluye la otra; rango faltante→422 `_RANGE_REQUIRED`; rango incoherente→422 `_RANGE_INVALID`; sin `ViewTimeAvailability`→403; export xlsx 200 + json 2 filas con Fuente EMPLOYEE_RELATIONS/EMPLOYMENT. Suite COMPLETA de integración NO corrida (la corre el orquestador). Archivos nuevos: `TimeAvailability.cs`/`.Handlers.cs`, `TimeAvailabilityController.cs`, guía FE; tocados: `IPersonnelTransactionRepository.cs`+impl (2 métodos + 2 row classes), `EmployeeRelationsReportingContracts.cs`, 2 resx, openapi.yaml, `ApiIntegrationTests.PersonnelTransactions.cs`. **REQ-003 → 🟢 COMPLETADO.** Siguiente: merge de REQ-001+REQ-002+REQ-003 + arrancar REQ-004.
- **2026-07-08** — **PR-5 COMPLETADO — bandejas + exportaciones + insumo de planilla** sobre `feature/vacaciones-incapacidades` (molde `IncapacitiesReportingController`/`CompensatoryTimeReportingController`). **(A) Controller** `EmployeeRelationsReportingController` (`[Authorize]`, **SIN `[AuthorizationPolicySet]`** — gate per-handler; 5 endpoints): `POST …/recognitions/query` + `GET …/recognitions/export` (gate `EnsureCanViewRecognitionsAsync`) · `POST …/disciplinary-actions/query` + `GET …/disciplinary-actions/export` + `GET …/disciplinary-actions/payroll-input/export` (gate `EnsureCanViewDisciplinaryActionsAsync`); rate limits `Search` (queries) / `Export` (exports) + 413 (límite síncrono). **(B) Feature** `PersonnelTransactionsBandeja.cs`/`.Handlers.cs`: **bandejas por familia** (`RecognitionBandejaResponse`/`DisciplinaryActionBandejaResponse` con `StatusCounts` sobre TODOS los estados aunque los items **excluyan `ANULADA` por defecto** — `includeAnnulled`/`statusCode` explícito para incluirlos, molde comptime; filtros empleado/tipo/(causa)/estado/rango de fechas del hecho·falta) + **exports ES** (`ReconocimientoExportRow`: Empleado, CodigoEmpleado, Tipo, FechaHecho, Detalle, Monto, Moneda, Estado, RegistradoPor, DecididoPor, FechaDecision, FechaRegistro · `AmonestacionExportRow`: + Causa, Detalle, TieneDescuento, MontoDescuento, ConceptoDescuento (snapshot), Moneda, SuspensionDesde/Hasta/Dias) + **insumo de planilla** (`InsumoPlanillaExportRow`: Empleado, CodigoEmpleado, Efecto, Causa, ConceptoDescuento?, Monto?, Moneda?, FechaInicio, FechaFin, Dias?). **(C) Insumo (RF-012)**: rango OBLIGATORIO (filtra por **IncidentDate** = fecha de la falta, coherente con la bandeja disciplinar); solo amonestaciones `APLICADA` con efecto → **una fila por efecto** (`DESCUENTO`: monto+concepto snapshot+moneda, FechaInicio=FechaFin=IncidentDate; `SUSPENSION_SIN_GOCE`: rango+`SuspensionDays`) — una amonestación con descuento Y suspensión → **2 filas**; **revocadas (ANULADA) NO viajan** por construcción (filtro `StatusCode==APLICADA`, RN-14/RN-15). Rango faltante/incoherente → **422 `PERSONNEL_TRANSACTION_RANGE_REQUIRED`** validado EN EL HANDLER (no FluentValidation, para 422 no 400). **(D) Repo** `IPersonnelTransactionRepository`/impl +5 métodos (`QueryRecognitionsAsync`/`GetRecognitionExportRowsAsync`/`QueryDisciplinaryActionsAsync`/`GetDisciplinaryActionExportRowsAsync`/`GetPayrollInputRowsAsync`): member-init projections (molde `FilteredIncapacities`) con join a `PersonnelFiles`+`PersonnelFileEmployeeProfiles` (código) y a los maestros `RecognitionType`/`DisciplinaryActionType`/`DisciplinaryActionCause` (código/publicId); StatusCounts vía GroupBy sobre el filtro sin-estado; scoping por **query-filter global de tenant** (no filtro explícito de company, igual que incapacidades/comptime); `MaxRows+1` para el 413. **(E) Contratos** `EmployeeRelationsReportingContracts.cs` (`QueryRecognitionsRequest`/`QueryDisciplinaryActionsRequest`). **(F) resx EN/ES** 1 código nuevo `PERSONNEL_TRANSACTION_RANGE_REQUIRED` (`PERSONNEL_FILE_EXPORT_FORMAT_INVALID` reutilizado para formato inválido). **(G) openapi.yaml a mano**: +5 paths (→ **424**) + 6 schemas (Recognition/DisciplinaryAction ListItem+Bandeja+Query → **761**); YAML válido, guardrail `OpenApiContractGuardrails` verde. **SIN migración, SIN seed** (PR-5 solo reporting). **Decisiones de diseño**: (1) los export rows NO llevan schema OpenAPI (se sirven como descarga, igual que comptime); (2) el insumo filtra por IncidentDate (no por solape de suspensión) — la fecha de la falta ancla toda la amonestación, coherente con la bandeja; (3) default de bandejas = excluir ANULADA (task) vs incapacidades que default a REGISTRADA — cada familia con su semántica documentada. **Suites: build `CLARIHR.slnx` 0 errores/0 warnings · unit `CLARIHR.Application.UnitTests` 2451/2451 (sin cambio — PR-5 es integración; localización EN/ES verde) · integración dirigida `~Recognition|~DisciplinaryAction` 13/13 verde (11 PR-3/PR-4 SIN regresión + 2 PR-5) + `~OpenApi` 1/1.** (2 PR-5: bandejas de reconocimientos+amonestaciones con StatusCounts {APLICADA/EN_REVISION/ANULADA} + default excluye anulados + includeAnnulled=3 + export xlsx 200/content-type + export json 2 filas · insumo con amonestación APLICADA descuento $25 + suspensión 3 días → **2 filas** DESCUENTO[Monto 25/USD/concepto snapshot]+SUSPENSION_SIN_GOCE[Dias 3/2026-05-10→05-12], una revocada de otro empleado EXCLUIDA, rango faltante→422). Suite COMPLETA de integración NO corrida (la corre el orquestador). Archivos nuevos: `PersonnelTransactionsBandeja.cs`/`.Handlers.cs`, `EmployeeRelationsReportingContracts.cs`, `EmployeeRelationsReportingController.cs`; tocados: `IPersonnelTransactionRepository.cs`+impl, 2 resx, openapi.yaml, `ApiIntegrationTests.PersonnelTransactions.cs`. Siguiente: PR-6 (consulta de disponibilidad + cierre).
- **2026-07-08** — **PR-4 COMPLETADO — amonestaciones end-to-end** sobre `feature/vacaciones-incapacidades` (espejo de PR-3 + suspensión + descuento + doble asiento + revocación integral). **(A) Feature** `Application/Features/PersonnelFiles/PersonnelTransactions/`: `DisciplinaryActions.cs` (response `PersonnelFileDisciplinaryActionResponse` con `[JsonIgnore] Id` + 32 props incl. `TypeAppliedSuspension`/`Cause*`/deducción/suspensión/`SuspensionActionPublicId`; `DisciplinaryActionInput`; 4 comandos + 2 queries + validadores; `DisciplinaryActionDecisions.{Apply,Reject}`; `DisciplinaryActionErrors`) + `DisciplinaryActions.Handlers.cs` (6 handlers + `DisciplinaryActionWriteSupport.ValidateDeclarative`) + `DisciplinaryActionDocuments.cs`/`.Handlers.cs` (5 handlers). Contratos `Api/Contracts/PersonnelFiles/DisciplinaryActionContracts.cs`; 2 controllers `PersonnelFileDisciplinaryActionsController`/`…DocumentsController` (`[AuthorizationPolicySet(ViewDisciplinaryActions, ManageDisciplinaryActions)]`, espejo reconocimientos sobre `/disciplinary-actions`). **(B) Create** (`EN_REVISION`, Manage): resuelve **tipo activo** → `DISCIPLINARY_ACTION_TYPE_INVALID` (trae `AppliesSuspension`+Name) + **causa activa** → `DISCIPLINARY_ACTION_CAUSE_INVALID` (Name + default concept); `IncidentDate ≤ hoy` → `DISCIPLINARY_ACTION_INCIDENT_DATE_IN_FUTURE`; **bloque suspensión** vía `PersonnelTransactionRules.ValidateSuspensionBlock(type.AppliesSuspension, start?, end?)` → `SUSPENSION_NOT_ALLOWED_FOR_TYPE`/`SUSPENSION_DATES_REQUIRED`/`SUSPENSION_RANGE_INVALID` (días derivados por el dominio); **bloque descuento** vía `ValidateDeduction` → `DEDUCTION_AMOUNT_REQUIRED`; el `DeductionConceptTypeCode` del input, si viaja, valida `Nature=Egreso` activo → `DEDUCTION_CONCEPT_INVALID`; snapshots `TypeNameSnapshot`/`CauseNameSnapshot`/`TypeAppliedSuspension`; `EMPLOYEE_PROFILE_RETIRED_LOCKED` en alta/edición/aplicación. **(C) Decisión** (`PATCH /decision`, **AuthorizeDisciplinaryActions** vía nuevo gate `LoadForAuthorizeDisciplinaryActionsAsync`, If-Match, re-verifica `EN_REVISION`): **anti-self DOBLE** `IsSelfDecision(LinkedUserPublicId, RegisteredByUserId, currentUser)` → 403 `DISCIPLINARY_ACTION_SELF_APPROVAL_FORBIDDEN`. **APLICAR**: si `hasSuspension` → `AcquireEmployeeRelationsLockAsync(tenant, file)` **dentro de la tx** + re-`HasOverlappingSuspensionAsync(exclude=self)` → `SUSPENSION_OVERLAP` (rollback+422); **congela el concepto de descuento** = default de la causa (`GetDisciplinaryActionCauseConceptCodeAsync`) re-validado `Nature=Egreso` activo AL APLICAR (→ 422 `DEDUCTION_CONCEPT_INVALID` si quedó inactivo/no-egreso; sin concepto en la causa → descuento sin concepto, P-14) → `Apply(..., conceptCode, conceptName)`; crea **asiento `AMONESTACION`** (`ActionDateUtc`=IncidentDate, `Amount`=DeductionAmount si `HasPayrollDeduction`) **+ asiento `SUSPENSION`** (`EffectiveFrom/ToUtc`=rango) cuando aplica — ambos `IsSystemGenerated`, misma tx, publicIds → `PersonnelActionPublicId`/`SuspensionActionPublicId`. **RECHAZAR**: `Reject` (nota obligatoria → `DECISION_NOTE_REQUIRED`). Retirado → aplicar 422 / rechazar OK. **(D) Revocación** (`PATCH /annulment` desde `APLICADA`, AuthorizeDisciplinaryActions + anti-self doble, motivo obligatorio → `ANNULMENT_REASON_REQUIRED`): `Annul(...)` + `personnelAction.Annul()` sobre **AMBOS** asientos vinculados (amonestación + suspensión) en la misma tx; desde `EN_REVISION` = Manage (retiro de trámite). **(E) Lecturas** `View OR self` (nuevo gate `LoadCompletedEmployeeForDisciplinaryActionReadAsync` → `RestrictToApplied`): self ve SOLO `APLICADA`. **(F) Adjuntos** sub-recurso Manage-only con gate de purpose `DisciplinaryActionDocument`. **(G) Repo** `IPersonnelTransactionRepository`/impl +8 métodos (`GetDisciplinaryActionResponses(onlyApplied)`/`GetDisciplinaryActionResponse`/`ResolveActiveDisciplinaryActionType`→`…TypeRef{Name,AppliesSuspension}`/`ResolveActiveDisciplinaryActionCause`→`…CauseRef{Name,DeductionConceptTypeCode}`/`ResolveActiveEgressConcept`→`EgressConceptRef{Code,Name}` [país del tenant, `Nature=Egreso` activo, molde `DisciplinaryActionCauseRepository`]/`GetDisciplinaryActionCauseConceptCode`/`GetDisciplinaryActionDocuments*`); **`DisciplinaryActionType.IsInUseAsync`/`DisciplinaryActionCause.IsInUseAsync` cableados** (ya no stub) contra la tabla `personnel_file_disciplinary_actions`. **(H) resx EN/ES** 9 códigos nuevos (`DISCIPLINARY_ACTION_TYPE_INVALID`/`_CAUSE_INVALID`/`_INCIDENT_DATE_IN_FUTURE`/`_SELF_APPROVAL_FORBIDDEN`, `SUSPENSION_NOT_ALLOWED_FOR_TYPE`/`_DATES_REQUIRED`/`_RANGE_INVALID`/`SUSPENSION_OVERLAP`, `DEDUCTION_AMOUNT_REQUIRED`); reusados `DEDUCTION_CONCEPT_INVALID`/`PERSONNEL_TRANSACTION_STATE_RULE_VIOLATION`/`DECISION_NOTE_REQUIRED`/`ANNULMENT_REASON_REQUIRED`/`EMPLOYEE_PROFILE_RETIRED_LOCKED` + `validation.message.decision_must_be_aplicar_or_rechazar`. **(I) openapi.yaml a mano**: +7 paths (disciplinary-actions ×4 + documents ×3) → 419, +8 schemas → 755; YAML válido, refs OK. **SIN migración** (dominio ya en M2). **Decisión de diseño clave**: el concepto de descuento es la **default de la causa**, congelada AL APLICAR (aclaración №5, coincide con la firma del dominio `Apply(...,conceptCode,conceptName)` — el handler resuelve el concepto, no se persiste override por registro en F1); el `DeductionConceptTypeCode` del input se valida al crear (feedback temprano/`concepto ingreso→422`). `SUSPENSION_OVERLAP` se devuelve **plano** (la infraestructura de ProblemDetails no expone un bag genérico de extensions más allá de `code`/`traceId`/`details` tipado — aclaración «si no, plano»). AMONESTACION `actionDateUtc`=IncidentDate/effective null; SUSPENSION `actionDateUtc`=now/effective=rango. **Suites: build `CLARIHR.slnx` 0 errores/0 warnings · unit `CLARIHR.Application.UnitTests` 2451/2451 (sin cambio — PR-4 es integración) · integración dirigida `~DisciplinaryAction` 6/6 verde + `~Recognition` 6/6 sin regresión** (A.4-3 tipo SUSPENSION_SIN_GOCE + causa con concepto egreso → aplicar por tercero → asientos AMONESTACION[$25]+SUSPENSION[rango]+snapshot concepto · A.4-4 fechas de suspensión sobre tipo ESCRITA→422 SUSPENSION_NOT_ALLOWED_FOR_TYPE + solape con suspensión vigente→422 SUSPENSION_OVERLAP bajo lock · A.4-5 revocar aplicada con suspensión→2 asientos ANULADA · descuento sin monto→422 + concepto de ingreso→422 DEDUCTION_CONCEPT_INVALID · anti-self doble [registrador+sujeto]→403). Suite COMPLETA de integración NO corrida (la corre el orquestador). Archivos nuevos: `DisciplinaryActions.cs`/`.Handlers.cs`, `DisciplinaryActionDocuments.cs`/`.Handlers.cs`, `DisciplinaryActionContracts.cs`, 2 controllers; tocados: `IPersonnelTransactionRepository.cs`+impl (records `DisciplinaryActionTypeRef`/`…CauseRef`/`EgressConceptRef`), `PersonnelFileEmployeeHandlerBases.cs` (3 gates nuevos), `DisciplinaryActionType/CauseRepository.cs` (IsInUse), 2 resx, openapi.yaml, `ApiIntegrationTests.PersonnelTransactions.cs`. Siguiente: PR-5 (bandejas + exportaciones + insumo de planilla).
- **2026-07-08** — **PR-3 COMPLETADO — reconocimientos end-to-end** sobre `feature/vacaciones-incapacidades`. **(A) Feature** `Application/Features/PersonnelFiles/PersonnelTransactions/`: `Recognitions.cs` (response `PersonnelFileRecognitionResponse` con `[JsonIgnore] Id`, `RecognitionInput`, 4 comandos [Add/Update/Decide/Annul] + 2 queries, validadores, `RecognitionDecisions.{Apply,Reject}`, `RecognitionErrors`) + `Recognitions.Handlers.cs` (6 handlers + `RecognitionWriteSupport.ValidateDeclarative`) + `RecognitionDocuments.cs`/`.Handlers.cs` (5 handlers, espejo medical-claims). Contratos `Api/Contracts/PersonnelFiles/RecognitionContracts.cs`. **(B) Endpoints**: `PersonnelFileRecognitionsController` (`[AuthorizationPolicySet(ViewRecognitions, ManageRecognitions)]`, SIN `[ResourceActions]`): GET/POST `/personnel-files/{id}/recognitions` · GET/PUT `/…/{recId}` · **PATCH `/…/decision`** (`{decision: APLICAR|RECHAZAR, note?}`) · PATCH `/…/annulment` (`{reason}`); `PersonnelFileRecognitionDocumentsController`: GET/POST documents · DELETE `/documents/{docId}` (`If-Match`) · GET `/documents/{docId}/read-url`. **(C) CRUD**: POST crea `EN_REVISION` (Manage; valida `RecognitionType` activo del tenant → `RECOGNITION_TYPE_INVALID`; `EventDate ≤ hoy` vía `IDateTimeProvider` → `RECOGNITION_EVENT_DATE_IN_FUTURE`; monto>0+moneda si viaja → `RECOGNITION_AMOUNT_INVALID`; snapshot `TypeNameSnapshot`); PUT solo `EN_REVISION`; `EMPLOYEE_PROFILE_RETIRED_LOCKED` en alta/edición/aplicación (nuevo `IsProfileRetiredAsync` en el repo). **(D) Decisión** (`PATCH /decision`, permiso **AuthorizeRecognitions** vía nuevo gate `LoadForAuthorizeRecognitionsAsync`, If-Match): **anti-self DOBLE** `PersonnelTransactionRules.IsSelfDecision(LinkedUserPublicId, RegisteredByUserId, currentUser)` → 403 `RECOGNITION_SELF_APPROVAL_FORBIDDEN`; re-verifica `EN_REVISION` (409/422); APLICAR → asiento `RECONOCIMIENTO` (`ActionDateUtc`=hoy, `EffectiveFromUtc`=EventDate, `Amount/CurrencyCode` si existen, `IsSystemGenerated:true`, molde `ExecuteRetirementRequest.cs:178-192`) en la misma tx + `Apply(...)` captura `PersonnelActionPublicId`; RECHAZAR → `Reject(...)` (nota obligatoria → `DECISION_NOTE_REQUIRED`). **(E) Anulación/revocación** (`PATCH /annulment`, `reason` obligatorio → `ANNULMENT_REASON_REQUIRED`): rutea por estado — desde `EN_REVISION` = Manage; desde `APLICADA` (**revocación**) = AuthorizeRecognitions + anti-self doble + `personnelAction.Annul()` (nuevo `GetPersonnelActionEntityAsync` tracked) sobre el asiento vinculado, misma tx. **(F) Lecturas** `View OR self`: nuevo gate `LoadCompletedEmployeeForRecognitionReadAsync` devuelve `RestrictToApplied` (true = rama self sin permiso) → GET lista/by-id/documentos filtran SOLO `APLICADA` del propio expediente (D-13). **(G) Adjuntos** opcionales sub-recurso Manage-only con gate de purpose `RecognitionDocument` (foreign purpose → 400 `files.invalid_purpose`, molde medical-claims — no es 422 porque reusa `FileErrors.InvalidPurpose` = Validation). **(H) Repo** `IPersonnelTransactionRepository`/impl +9 métodos (`GetRecognitionResponsesAsync(onlyApplied)`/`GetRecognitionResponseAsync`/`ResolveActiveRecognitionTypeAsync`→`RecognitionTypeRef`/`IsProfileRetiredAsync`/`GetPersonnelActionEntityAsync`/`GetRecognitionDocuments*`); `RecognitionType.IsInUseAsync` **cableado** (ya no stub) contra la tabla nueva. **(I) resx EN/ES** 8 claves nuevas (`RECOGNITION_TYPE_INVALID`/`_EVENT_DATE_IN_FUTURE`/`_AMOUNT_INVALID`/`_SELF_APPROVAL_FORBIDDEN`, `PERSONNEL_TRANSACTION_STATE_RULE_VIOLATION`, `DECISION_NOTE_REQUIRED`, `ANNULMENT_REASON_REQUIRED`, `validation.message.decision_must_be_aplicar_or_rechazar`; `EMPLOYEE_PROFILE_RETIRED_LOCKED` reusado de REQ-002). **(J) openapi.yaml a mano**: +7 paths (recognitions ×4 + documents ×3) → 412 paths, +8 schemas → 747; YAML válido, refs OK. **SIN migración** (dominio ya en M2). **Gotchas resueltos**: (1) el read gate NO acepta `ManageRecognitions` (solo View/Admin/ManageAdministration — patrón incapacidades) → managers necesitan View; en integración se usó `Admin`; (2) `/personnel-actions` GET exige Read/Admin; (3) `FileErrors.InvalidPurpose` = 400 no 422; (4) props null SÍ se serializan (no hay WhenWritingNull); (5) seeds de vuelta con query-filter de tenant → `IgnoreQueryFilters()`. **Suites: build `CLARIHR.slnx` 0 errores/0 warnings · unit `CLARIHR.Application.UnitTests` 2451/2451 (sin cambio — PR-3 es integración) · integración dirigida `~Recognition` 6/6 verde** (A.4-1 crear EN_REVISION sin asiento→aplicar por tercero→asiento RECONOCIMIENTO+self ve solo APLICADA · A.4-2 registrador→403 + sujeto→403 · rechazar sin nota→422 · revocar aplicado→asiento ANULADA+desaparece de self · adjunto sub-recurso+read-url+purpose ajeno→400 · RETIRADO alta→422/aplicar→422/rechazar→200). Suite COMPLETA de integración NO corrida (la corre el orquestador). Archivos nuevos: `Recognitions.cs`/`.Handlers.cs`, `RecognitionDocuments.cs`/`.Handlers.cs`, `RecognitionContracts.cs`, 2 controllers, `ApiIntegrationTests.PersonnelTransactions.cs`; tocados: `IPersonnelTransactionRepository.cs`+impl, `PersonnelFileEmployeeHandlerBases.cs` (3 gates), `RecognitionTypeRepository.cs`, 2 resx, openapi.yaml. Siguiente: PR-4 (amonestaciones end-to-end).
- **2026-07-08** — **PR-2 COMPLETADO — gate de la ola en verde** (dominio + reglas + M2 + mutador `Annul()`) sobre `feature/vacaciones-incapacidades`. **(A) Dominio** `Domain/PersonnelFiles/PersonnelFilePersonnelTransactions.cs`: `PersonnelFileRecognition` (FK+Bind, `RecognitionTypeId`+`TypeNameSnapshot`, `EventDate`, `Detail`(1000), `Amount?`/`CurrencyCode?` informativos, `AssignedPositionPublicId?`, `RegisteredByUserId`, `StatusCode` inicia `EN_REVISION`, decisión `DecidedByUserId/DecidedUtc/DecisionNote`, anulación `AnnulmentReason/AnnulledByUserId/AnnulledUtc`, `PersonnelActionPublicId?`, `Notes?`, `IsActive`, token; guards `Create`/`Update`(solo EN_REVISION)/`Apply(byUserId,at,actionPublicId)`/`Reject`(nota obligatoria)/`Annul`(desde EN_REVISION o APLICADA, motivo obligatorio)) + `PersonnelFileDisciplinaryAction` (+ `TypeAppliedSuspension` snapshot del flag al crear, `DisciplinaryActionCauseId`+`CauseNameSnapshot`, `IncidentDate`, `FactsDetail`(2000), descuento `HasPayrollDeduction`/`DeductionAmount?`/`CurrencyCode?`/`DeductionConceptTypeCode?`+`DeductionConceptNameSnapshot?` **congelados en `Apply`**, suspensión `SuspensionStartDate?`/`SuspensionEndDate?`+`SuspensionDays?` **derivado calendario-inclusivo** `end−start+1`, `SuspensionActionPublicId?`; `Apply(byUserId,at,actionPublicId,suspensionActionPublicId?,conceptCode?,conceptName?)`) + 2 documentos (`PersonnelFileRecognitionDocument`/`PersonnelFileDisciplinaryActionDocument`, espejo `PersonnelFileIncapacityDocument`, doc-type FK nullable). **Coherencia suspensión↔flag / descuento↔flag deliberadamente NO en el dominio** (viven en reglas/handler, plan §3.4); el dominio sí valida start≤end, ambas fechas de suspensión juntas-o-ninguna, amount>0 cuando viaja. **(B) Mutador `Annul()` en `PersonnelFilePersonnelAction`** (aclaración №4, `PersonnelFileEmployee.cs`): antes solo `Create`/`BindToPersonnelFile`; ahora custodiado — lanza si ya `ANULADA`, si no asigna `ActionStatusCode="ANULADA"` (código `-9496`, no existe const dedicada → string literal, como los handlers de retiro usan "APLICADA") y rota `ConcurrencyToken`. **(C) Reglas puras** `Application/Features/PersonnelFiles/PersonnelTransactions/PersonnelTransactionRules.cs` (estático, sin reloj): `CanTransition(from,to,via{Decision|Annulment})` · `IsSelfDecision(linkedUserId,registeredByUserId,currentUserId)` **anti-self DOBLE** (case-insensitive, ignora null/empty) · `ValidateSuspensionBlock`→enum `{Valid,NotAllowedForType,DatesRequired,RangeInvalid}` · `SuspensionDays` inclusive · `ValidateDeduction`→enum `{Valid,AmountRequired}` · `RangesOverlap` (inclusive, compartida solape RN-18 + consulta RN-15) · `BuildAvailabilityWindow`→`AvailabilityWindowResult`. **(D) EF config** `Configurations/PersonnelFiles/PersonnelFilePersonnelTransactionsConfiguration.cs` (4 configs) + 4 DbSets + DI `IPersonnelTransactionRepository`. **(E) Repositorio** `Abstractions/PersonnelFiles/IPersonnelTransactionRepository.cs` + impl `Infrastructure/PersonnelFiles/PersonnelTransactionRepository.cs`: loaders tracked por familia (`GetRecognitionEntityAsync`/`GetDisciplinaryActionEntityAsync` + internal-id + `Add*`), `HasOverlappingSuspensionAsync` (APLICADA + rango solapado, molde `RangesOverlap`), `AcquireEmployeeRelationsLockAsync` (`pg_advisory_xact_lock`, class id "ERLK", no-op default en el interface para fakes — molde `CompensatoryTimeRepository.AcquireFundLockAsync`). **Bandeja/export/consulta de disponibilidad DIFERIDOS a PR-5/PR-6** (dicho en el interface). **(F) Migración M2 `20260708231349_AddPersonnelFileRecognitionsAndDisciplinaryActions`**: 4 tablas (`personnel_file_recognitions`, `personnel_file_recognition_documents`, `personnel_file_disciplinary_actions`, `personnel_file_disciplinary_action_documents`) + 3 CHECKs (`ck_pf_recognitions__amount_positive` `amount IS NULL OR amount>0`; `ck_pf_disc_actions__deduction_amount_positive`; `ck_pf_disc_actions__suspension_dates` `start IS NULL OR end IS NULL OR start<=end`) + índices `(tenant,file,status)` en ambas + amonestaciones `(tenant,suspension_start,suspension_end)` y `(tenant,status,incident_date)` + uq public_id + FKs (personnel_file Cascade; type/cause/doc-type Restrict). Todos los nombres ≤ 63 chars; `has-pending-model-changes` **vacío (sin drift)**. **Suites: build `CLARIHR.slnx` 0 errores/0 warnings · unit `CLARIHR.Application.UnitTests` 2451/2451 (2395 base + 56: 36 golden A.4 rules + 20 dominio, incl. `PersonnelFilePersonnelAction.Annul()` desde APLICADA rota token / re-anular lanza) · migración M2 sin drift.** Suite completa de integración NO corrida (la corre el orquestador). **Golden A.4 cubiertos** (rule/dominio): A.4-2 anti-self doble, A.4-4 suspensión (4 combinaciones + `SuspensionDays(28-abr,3-may)=6`) + descuento, A.4-6 `RangesOverlap` (mes previo→intersecta / día puntual EndDate→intersecta / no toca→false / bordes inclusivos), CanTransition (Decision/Annulment), BuildAvailabilityWindow. Siguiente: PR-3 (reconocimientos end-to-end).
- **2026-07-08** — **PR-1 COMPLETADO** (configuración, M1) sobre la rama acumulada `feature/vacaciones-incapacidades`. **(A) 3 maestros governed** (`Domain/EmployeeRelations/RecognitionType`/`DisciplinaryActionType`+`AppliesSuspension`/`DisciplinaryActionCause`+`DeductionConceptTypeCode`; reusan `LeaveNormalization`): índice único filtrado `(tenant, normalized_code) WHERE is_active`, guards Create/Update/Activate/Inactivate, `[ResourceActions]`+`ISupportsAllowedActions` en DTOs; feature `Features/EmployeeRelations/*` (×3 + `.Handlers`), 3 repos, 3 controllers governed + `EmployeeRelationsConfigurationController` (load-template). **Guard de uso `…_IN_USE` DIFERIDO** (repos `IsInUseAsync` → `false` hoy; tablas de registros son M2/PR-2 — dicho en el código). **Familia governed nueva** `EmployeeRelationsConfiguration` (Read/Manage): policies + permission codes (`EmployeeRelationsConfiguration.Read/Admin` + 3 resource keys) + `IEmployeeRelationsConfigurationAuthorizationService`/impl (módulo `PersonnelFiles`) + `AllowedActionsRegistry` (3 keys) + 2 policies en Program.cs + 2 tuplas provisioning + lista canónica del governance test. **(B) `EmployeeRelationsTemplateSeeder`** idempotente por código (Anexo A.2: 5 reconocimientos, 4 tipos amonestación [solo `SUSPENSION_SIN_GOCE` con susp=true], 6 causas **sin concepto** — P-14) + hook en `CompanyProvisioningService` (tras el leave seeder) + `POST …/employee-relations/load-template`. **DevSeed OMITIDO** (PR-1 solo config; el hook de provisioning ya siembra las plantillas para tenants reales; los registros demo son de PR-2+). **(C) TPH `personnel-transaction-statuses`** (receta 8 toques): seeds SV `-9875 EN_REVISION`/`-9876 APLICADA`/`-9877 RECHAZADA`/`-9878 ANULADA` + consts `PersonnelTransactionStatuses` (Editable={EN_REVISION}, Vigentes={EN_REVISION,APLICADA}). **(D) ActionType `RECONOCIMIENTO=-9879`** (sort 230; `AMONESTACION=-9477`/`SUSPENSION=-9478` NO sembrados — se reutilizan). **(E) 7 permisos** `PersonnelFiles.View/Manage/Authorize{Recognitions,DisciplinaryActions}` + `ViewTimeAvailability`: codes+policies, 7 gates fail-closed en `IPersonnelFileAuthorizationService` + overrides, 7 tuplas provisioning, 9 policies Program.cs (View authn-only `.Combine`; Manage authn-only; **Authorize\* RequireAssertion SIN Admin** — code + `ManageAdministration`; ViewTimeAvailability RequireAssertion con fallback). **⚠️ Decisión verificada**: los 2 `Authorize*` SÍ van dentro de `CompanyAdminPermissions` (espejo EXACTO de `AuthorizeRetirement`, que también está dentro); la exclusión de Admin vive **solo en la policy** — el texto del plan «fuera de CompanyAdminPermissions» es la imprecisión ya anotada en la bitácora de REQ-007 (2026-07-06). Los GATES de handler (`LoadForManage…`/`…Read`) son de PR-3/PR-4 (no en PR-1). **(F) 2 `FilePurpose`** `RecognitionDocument`/`DisciplinaryActionDocument` (pdf/jpg/png, contenedores `clarihr-recognition-documents`/`clarihr-disciplinary-action-documents`) + bloques en appsettings **base**. **(G) Migración M1 `20260708225100_AddEmployeeRelationsConfiguration`**: 3 CreateTable de maestros + 1 CreateTable del TPH (InsertData 4 estados) + InsertData `RECONOCIMIENTO` — `has-pending-model-changes` **vacío**; índices ≤ 63 chars. **(H) resx EN/ES**: 11 códigos (`RECOGNITION_TYPE_*`/`DISCIPLINARY_ACTION_TYPE_*`/`DISCIPLINARY_ACTION_CAUSE_*` NOT_FOUND/_CODE_CONFLICT/_IN_USE + `DEDUCTION_CONCEPT_INVALID` + `EMPLOYEE_RELATIONS_CONFIGURATION_FORBIDDEN`); los validadores usan `$"…"` interpolado → sin `validation.message.*` nuevos. **(I) openapi.yaml**: 3 maestros (×4 paths) + load-template + wire key `personnel-transaction-statuses` (a mano). **Suites: build `CLARIHR.slnx` 0 errores/0 warnings · unit `CLARIHR.Application.UnitTests` 2395/2395 · migración M1 sin drift · guardrails governance+keymap+localization 17/17 verde (`--filter`).** `AllowedActionsCoverage` (integración) cubierto por construcción (3 keys registradas) — lo corre el orquestador. Sin correr la suite completa de integración. Archivos nuevos: `Domain/EmployeeRelations/*` (3) + `Domain/PersonnelFiles/PersonnelTransactionStatuses.cs`, `Features/EmployeeRelations/*` (8) + `Common/*` (3), `Abstractions/EmployeeRelations/*` (5), `Infrastructure/EmployeeRelations/*` (5), `Configurations/EmployeeRelations/*` (3), 4 controllers, migración M1. Siguiente: PR-2.
- **2026-07-05** — Análisis de negocio creado en borrador (validación contra código + GAP): semi-greenfield **con costura documental** (`ACTION_TYPE_CATALOG` ya siembra `AMONESTACION=-9477`/`SUSPENSION=-9478`/`PERMISO`/`REINTEGRO` y el asiento manual los admite hoy; falta `RECONOCIMIENTO`); no existe catálogo de "tipos de descuento" → la causa referencia conceptos país `Nature=Egreso`; fuente "fin de contratos temporales" 100 % derivable (`IsTemporary` + `EndDate`); fuente "permisos" sin módulo (REQ-001 los excluyó — hueco documentado, diseño por fuentes conectables). D-01…D-18 propuestas, P-01…P-14 abiertas; plantillas de los 3 maestros y seeds tentativos (≤ -9875) en anexos; marco legal SV verificado con fuentes (CT privado: multas prohibidas, suspensión reglamentaria ≤ 1 día · LSC público: multa y suspensión 5 días/mes, 15/año). Sin commit. Pendiente: ratificación del negocio.
- **2026-07-05** — **RATIFICADO por el negocio**: D-01…D-18 aceptadas **sin ajustes** (todas las propuestas del análisis) y P-01…P-14 respondidas con las propuestas tal cual. Única precisión: **P-14 «no hay multas»** → sin advertencia legal configurable en F1, plantilla de causas sembrada sin conceptos de descuento (coherente con P-12); la capacidad de descuento del módulo se mantiene para marcos que la permitan (P-02/P-03). Documento actualizado a RATIFICADO (§17 resuelta). Sin commit.
- **2026-07-05** — **Plan técnico escrito** (`plan-tecnico-otras-transacciones-personal.md`, PR-1…PR-6) sobre exploración quirúrgica del código: **`PersonnelFilePersonnelAction` no tiene mutadores** (solo `Create`/`Bind`, `PersonnelFileEmployee.cs:571-643`) → la revocación agrega el mutador custodiado `Annul()` (primera mutación de asientos del sistema); molde del permiso que excluye Admin verificado (`AuthorizeRetirement`: `ProvisioningConstants.cs:89` + `Program.cs:552-568`, fuera de `CompanyAdminPermissions`); decisión atómica con If-Match + re-verificación en transacción y `pg_advisory_xact_lock` solo al aplicar amonestaciones con suspensión (solapes); snapshot del concepto de egreso **al aplicar** (no al crear); consulta de disponibilidad con contrato estable + `activeSources[]` y fuentes = métodos del repositorio (conectar REQ-001/002 sin romper contrato); fuente contratos = join código normalizado + país (criterio de `PersonnelFileRepository.cs:1503`). Checklist de PRs poblado. Sin commit. Desarrollo espera merge de REQ-001 y REQ-002.

---

## REQ-004 — Tablero de gráficos e indicadores de acciones de personal

| | |
|---|---|
| **Estado** | 🔴 PENDIENTE (análisis **RATIFICADO** + plan técnico **ESCRITO** — **listo para arrancar en cualquier momento**: no depende de REQ-001…003) |
| **Análisis de negocio** | [`docs/business/analisis-tablero-acciones-personal.md`](business/analisis-tablero-acciones-personal.md) — **D-01…D-18 ratificadas por unanimidad** + **P-01…P-14 respondidas** (cierres: P-01 impresión/PDF **frontend** · P-02 catálogo país A.2 + **limpieza destructiva sin backfill** · P-03 indicadores D-04 + **mapa de gráficos A.5**) |
| **Plan técnico** | [`docs/technical/plan-tecnico-tablero-acciones-personal.md`](technical/plan-tecnico-tablero-acciones-personal.md) — **escrito 2026-07-05**: §0 aclaraciones (14) · §3 arquitectura (extensión de la familia dashboard) · §4 migración M1 (catálogo + limpieza + índice del journal) · §7 orden de PRs (PR-1…PR-5) |
| **Guía FE** | pendiente |
| **Rama** | `feature/tablero-acciones-personal` |
| **Línea base de tests** | (anotar al arrancar) |
| **Alcance (síntesis)** | **Extensión del tablero ya construido** (`personnel-files/dashboard/*`, permiso `ViewReports` — PR #52) con la **primera consulta corporativa del journal de acciones** (`PersonnelFilePersonnelAction`, hoy solo consultable por expediente): serie mensual / por tipo / por estado / origen manual-automático / por dimensión organizativa · **activación de bajas + índice de rotación diferidos** del tablero RRHH (retiro PR #55; fuente canónica = perfil, nunca el journal) + altas/neto, cobertura de entrevistas de retiro y liquidaciones por estado (conteos, **sin montos**) · filtros heredados + **mes** + **tipo de planilla** (`PAYROLL_TYPE_CATALOG` país A.2 + validación estricta + **migración de limpieza destructiva** del campo libre — sin backfill) + **centro de costo** opcional (P-12); nivel piramidal sigue diferido · **impresión/PDF desde el frontend** (P-01 — vista de impresión + mapa de gráficos A.5; cero PDF backend) · exports tabulares + bandeja corporativa de asientos (drill) · fuentes conectables con `sections[]` en metadata (REQ-001/002/003 al llegar) |
| **Dependencias** | **Ninguna dura de REQ-001…REQ-003** (read-only sobre módulos existentes — puede adelantarse si el negocio prioriza visibilidad gerencial) · el catálogo `payroll-types` queda **especificado aquí** (lo siembra el REQ que se construya primero; REQ-001 lo reutiliza para incapacidades) · seeds tentativos `-9520…-9525` (verificar al abrir PR-1 — no tocar reservas REQ-001 `-9485…-9489`/`-9850…-9862`, REQ-002 `-9865…-9871`, REQ-003 `-9875…-9879`) |

### Checklist de PRs (fuente: plan técnico §7 — detalle por PR en el plan)
- [ ] **PR-1** — Catálogo `payroll-types` (M1): entidad + seed SV `-9520…-9525` + `GeneralCatalogKeyMap` + validador de plaza (422 `PAYROLL_TYPE_INVALID`) + **migración: HasData + limpieza destructiva SQL + índice del journal** `(tenant_id, action_date_utc)` + openapi temprano — plan §3.1/§4
- [ ] **PR-2** — Capa dimensional y filtros extendidos: `EmployeeDimensionRow` (+payrollType/costCenter/categoría-motivo de retiro) + `DashboardDimensionFilter` (+month/payrollTypeCode/costCenterId) + `overview.byPayrollType` (aditivo) + metadata extendida (`sections[]`/`filters[]`/fórmula de rotación) — plan §3.2/§3.5
- [ ] **PR-3** — Sección **acciones documentales**: `GetPersonnelActionFactsAsync` (tenant-wide) + `PersonnelActionsDashboardRules` puro + endpoint `dashboard/personnel-actions` + **golden A.3 en verde (gate)** + EXPLAIN del índice — plan §3.3-3.5
- [ ] **PR-4** — Sección **movimientos**: bajas/rotación/altas-neto/cobertura de entrevistas/liquidaciones + endpoint `dashboard/movements` + fixtures retiro/reversión/liquidación + **test de contrato sin-montos** — plan §3.4/§3.5
- [ ] **PR-5** — Bandeja `personnel-actions/query` + exports (datasets + bandeja + resource async `COMPANY_PERSONNEL_ACTIONS`) + suite E2E + `openapi.yaml` final (verificar contrato publicado — aclaración №13) + **guía FE** (vista de impresión + mapa A.5 + fuentes activas) — plan §3.5/§3.6

### Pendientes de despliegue (checklist §9 del plan)
- [ ] Migración M1 aplicada: catálogo sembrado + **limpieza de `payroll_type_code` ejecutada** (registrar antes el conteo por valor en la bitácora del despliegue — es irreversible por diseño, P-02) + índice del journal
- [ ] Comunicar a la empresa: plazas quedan **sin clasificar** en tipo de planilla tras la limpieza (estado esperado; se clasifican por edición natural — "Sin dato" mientras tanto)
- [ ] Sin storage nuevo, sin configuración nueva, sin infraestructura PDF (P-01 = frontend)

### Próxima acción
> **Listo para arrancar** (no depende de REQ-001…003 — decisión de orden del negocio; si se mantiene el orden del archivo, arranca tras REQ-003). Al arrancar: crear la rama `feature/tablero-acciones-personal` desde `master`, anotar la línea base de tests, y sembrar `payroll-types` con los **IDs re-ubicados `-9890…-9895`** (⚠️ los tentativos `-9520…-9525` del plan **COLISIONAN** con `ECONOMIC_AID_TYPE_CATALOG`, verificado 2026-07-05 contra `GlobalCatalogSeedData.cs:539-544` — ver bitácora y `analisis-planilla-ingresos-ciclicos.md` A.2), verificar IDs libres contra `GlobalCatalogSeedData` y ejecutar **PR-1** siguiendo el plan §3.1/§4. Recordar: la limpieza de `payroll_type_code` es destructiva (registrar conteo previo); si REQ-001 arrancara primero y necesitara el catálogo `payroll-types`, sembrarlo con la especificación de este plan (una sola definición, IDs re-ubicados).

### Bitácora
- **2026-07-05** — Análisis de negocio creado en borrador (validación contra código + GAP). Hallazgos clave: el tablero RRHH Fase 1 está **construido y mergeado (PR #52)** — este REQ lo extiende (memoria previa que decía "no construido" quedó corregida); el journal `PersonnelFilePersonnelAction` existe con asientos automáticos (`BAJA`/`REVERSION_BAJA`/`RECONTRATACION`/`LIQUIDACION`) pero **sin consulta a nivel empresa**; el módulo de retiro (PR #55) **desbloquea las bajas/rotación diferidas** del tablero RRHH; `PayrollTypeCode` ya existe en la plaza como texto libre (sin catálogo ni filtro); nivel piramidal sigue sin relación canónica; el stack PDF es dual (QuestPDF/Gotenberg) pero **sin bloque de imagen/gráfico ni librería de charting** (impresión/PDF = decisión P-01 con opciones en Anexo A.4). D-01…D-18 propuestas, P-01…P-14 abiertas. Sin commit. Pendiente: ratificación del negocio.
- **2026-07-05** — **RATIFICADO por el negocio**: D-01…D-18 aprobadas **por unanimidad** y P-01…P-14 respondidas. Cierres que ajustan el detalle: (1) **P-01** → impresión y exportación PDF **desde el navegador/frontend** (D-12 vía (a); el reporte PDF servidor queda como referencia F2 en A.4-b; cero superficie PDF backend); (2) **P-02** → tipo de planilla = **modalidad de pago contractual del empleado** («el tipo de contrato del empleado»), **catálogo por país** con los valores A.2 confirmados y **sin backfill**: migración de **limpieza destructiva** (coincidencias exactas se normalizan a código, el resto se elimina/NULL — sin datos ni código legacy); (3) **P-03** → los 11 indicadores D-04 cubren los estándares; los tipos de gráfico/orden/prioridad quedan especificados en el **nuevo Anexo A.5** (la guía FE lo adopta); (4) P-12 → filtro de **centro de costo** entra como opcional de bajo costo. Documento actualizado a RATIFICADO (§17 resuelta, RF-003/RF-015/RN-11 y anexos ajustados). Sin commit.
- **2026-07-05** — **Plan técnico escrito** (`plan-tecnico-tablero-acciones-personal.md`, PR-1…PR-5): extensión de la familia Reporting sin permisos nuevos (todo bajo `EnsureCanViewReportsAsync`); M1 única (catálogo + limpieza SQL de dos pasos + índice `(tenant_id, action_date_utc)` del journal — los índices actuales anteponen `PersonnelFileId`); contrato 100 % aditivo (overview/hires/span/metadata intactos; `hires` NO se toca — movements calcula sus altas); `month` solo en endpoints de flujo; fuentes conectables vía `sections[]` en metadata (espejo de `activeSources[]` de REQ-003); test de contrato **sin-montos** (el asiento de liquidación lleva el neto); verificación pendiente en PR-4 del enum "completada" de `ExitInterviewSubmission`; hallazgo a resolver en PR-5: el `openapi.yaml` publicado no declara la familia dashboard (baseline desactualizado pre-existente). Sin commit. Desarrollo: listo para arrancar cuando el negocio lo priorice.
- **2026-07-05** — ⚠️ **Colisión de seeds detectada** durante la validación de REQ-005: los IDs tentativos `-9520…-9525` de `PAYROLL_TYPE_CATALOG` están **ocupados en código** por `ECONOMIC_AID_TYPE_CATALOG` (`GlobalCatalogSeedData.cs:539-544`, `-9520…-9526`). Re-ubicación acordada: **`-9890…-9895`** (especificación en `analisis-planilla-ingresos-ciclicos.md` A.2 — sigue siendo una sola definición del catálogo, la siembra el REQ que construya primero). Actualizar el plan técnico de este REQ al abrir PR-1.

---

## REQ-005 — Planilla: ingresos cíclicos (registro, autorización e historial de pago)

| | |
|---|---|
| **Estado** | 🔴 PENDIENTE (análisis **RATIFICADO** + plan técnico **ESCRITO** — listo para arrancar según orden del backlog; adelantable con la degradación del periodo, plan №13) |
| **Análisis de negocio** | [`docs/business/analisis-planilla-ingresos-ciclicos.md`](business/analisis-planilla-ingresos-ciclicos.md) — **D-01…D-18 ratificadas** (D-03/D-12 ajustadas) + **P-01…P-15 respondidas** (§17): **P-01 el motor de planilla se realizará aparte** · P-02 dos catálogos (tipos de ingresos = conceptos país; **tipos de ingresos cíclicos** = catálogo nuevo con plantilla alimentación/representación/combustible) · P-15 **centro de costo obligatorio ligado a la plaza** |
| **Plan técnico** | [`docs/technical/plan-tecnico-planilla-ingresos-ciclicos.md`](technical/plan-tecnico-planilla-ingresos-ciclicos.md) — **escrito 2026-07-05**: §0 aclaraciones (15, verificadas contra código) · §2 modelo de datos · §3 arquitectura (controller de resolución dedicado — `AuthorizationPolicySet` es class-only) · §3.5 integración liquidación (5 toques por canal de sugerencias) · §6 orden de PRs (PR-1…PR-6) |
| **Guía FE** | pendiente |
| **Rama** | `feature/planilla-ingresos-ciclicos` |
| **Línea base de tests** | (anotar al arrancar) |
| **Alcance (síntesis)** | Primera rebanada del **módulo de planilla**: ingresos cíclicos adicionales por empleado con **flujo de una decisión** (anti-autoaprobación doble, `Authorize*` sin Admin) · plan de cuotas finito/indefinido (valor + número **o** total, última cuota ajusta; proyección **derivada**, cuota **persistida al aplicar**) · aplicación manual unitaria/**lote por periodo con exclusión** (= «enviar a otro periodo») bajo lock anti-carrera · historial de pago por ingreso (aplicadas + proyectadas + saldos) · bandejas (`StatusCounts`, cuotas pendientes/**vencidas**) + exports + **insumo de planilla por periodo** (puente con nómina externa) · «acción de liquidación» (`PAGAR_SALDO`/`CANCELAR`) enganchada al motor de liquidación (líneas sugeridas, reversión simétrica) · reutiliza: conceptos país `Nature=Ingreso` («tipo ingreso»), monedas, `pay-periods` (frecuencia), `CostCenter`, catálogo `payroll-types` (spec REQ-004 re-ubicada) y maestro de periodos (REQ-001). **Sin motor de cálculo** (visión mapeada en A.1: descuentos cíclicos + endeudamiento + motor de generación = rebanadas/levantamientos futuros) |
| **Dependencias** | Ratificación del negocio: ✅ **completada** (2026-07-05, §17 del análisis) · REQ-004: definición compartida de `payroll-types` con **seeds re-ubicados `-9890…-9895`** (no bloquea: la siembra el primero que construya) · REQ-001: `PayrollPeriodDefinition` para la FK de imputación (no bloquea: degradación a etiqueta documentada, plan №13) · liquidación ya mergeada (PR #56) · seeds propios en bloque **`-9880…-9899`** (statuses `-9880…-9885` · acciones `-9886/-9887` · concepto liquidación `-9888` · holgura `-9889` · payroll-types `-9890…-9895` · tipos cíclicos `-9896…-9899`; verificar al abrir PR-1; trampa `-9490…-9496`) |

### Checklist de PRs (fuente: plan técnico §6 — detalle por PR en el plan)
- [ ] **PR-1** — Configuración (M1): 4 catálogos + seeds `-9880…-9899` (payroll-types `-9890…-9895` — omitir si REQ-004/REQ-001 ya lo sembró) + key map/categorías + verificación de conceptos `Nature=Ingreso` (P-02a: Salario/Aguinaldo/horas extras/vacaciones/bonificaciones) + 3 permisos/policies/gates (**`AuthorizeRecurringIncomes` sin Admin**) + openapi temprano — plan §3.1/§4
- [ ] **PR-2** — Dominio + reglas (M2): 2 entidades + mutadores custodiados + EF/índices/CHECKs (índice único parcial de cuotas) + `RecurringIncomeRules` puro con **golden A.3 en verde (gate)** + repositorio (idioma Add/AsNoTracking) + lock advisory — plan §2/§3.2
- [ ] **PR-3** — Flujo end-to-end: CRUD `EN_REVISION` + **controller de resolución dedicado** (autorizar/rechazar anti-self doble + revocación) + suspensión/reanudación + cierre/anulación + validaciones P-15 (plaza→centro de costo) — plan §3.3
- [ ] **PR-4** — Cuotas e historial: proyección derivada (`schedule`) + aplicación unitaria y **lote por periodo** (atómico, lock, **test de carrera**) + anulación con reapertura `FINALIZADO→VIGENTE` + par de periodo (decidir №13: FK REQ-001 vs degradación) — plan §3.4
- [ ] **PR-5** — Bandejas + exportaciones: bandeja empresa (`StatusCounts`) + cuotas pendientes/vencidas + 3 exports con rate limiting + **insumo de planilla** cuadrado — plan §3.3
- [ ] **PR-6** — Integración liquidación (5 toques: sugerencia `INGRESO_CICLICO_PENDIENTE=-9888` + finalizar al emitir + reabrir al anular) + suite E2E + test de no-escritura de ledgers + `openapi.yaml` final (skill) + **guía FE** + DevSeed — plan §3.5/§3.6

### Pendientes de despliegue (checklist §8 del plan)
- [ ] Migraciones M1–M2 aplicadas (verificar estado previo de `payroll-types` si REQ-004/REQ-001 corrió antes)
- [ ] **Sin storage nuevo** (P-07), sin jobs, sin appsettings nuevos
- [ ] Paso de adopción por empresa: revisar el catálogo de tipos de ingresos cíclicos (plantilla P-02), confirmar conceptos `Nature=Ingreso`, registrar compromisos preexistentes retroactivos (P-13)
- [ ] Comunicar la **operación manual F1** (cuotas se aplican por periodo desde la bandeja; el insumo exportado es el puente con la nómina externa — P-01) y asignar `AuthorizeRecurringIncomes` a los autorizadores

### Próxima acción
> **Esperar el turno del backlog** (orden actual: tras REQ-001…REQ-004; la integración con liquidación no espera nada — módulo mergeado). Al arrancar: crear la rama `feature/planilla-ingresos-ciclicos` desde `master`, anotar la línea base de tests, **verificar IDs de seed libres `-9880…-9899`** contra `GlobalCatalogSeedData` (trampa `-9490…-9496`; respetar reservas REQ-001/002/003; coordinar `payroll-types -9890…-9895` con REQ-004/REQ-001 — una sola definición) y ejecutar **PR-1** siguiendo el plan §3.1/§4. Si se adelanta a REQ-001: aplicar la degradación del periodo (plan №13, decidir en PR-4). Recordar: `AuthorizationPolicySet` es class-only → la decisión va en controller dedicado (plan №4).

### Bitácora
- **2026-07-05** — Análisis de negocio creado en borrador (validación contra código + GAP): la visión del levantamiento (periodos + **generar planilla** + endeudamiento + pool de pendientes) pisa la frontera «nómina externa» ratificada en cadena (D-08 plazas ingresos/egresos, RN-14 REQ-003, G-06 REQ-002) → propuesta de fasear como programa (P-01) con F1 = las dos opciones detalladas (ingreso cíclico + historial), construibles sin motor. Verificado: existe la **configuración** (`PersonnelFileCompensationConcept` + catálogos país de conceptos/monedas/frecuencias + `CostCenter` con cuentas contables + salario 3 niveles + ISSS/AFP/Renta) pero **cero** entidades de cuotas/préstamos/motor/periodos; `payroll_type_code` es texto libre (catálogo especificado en REQ-004); periodos de planilla especificados en REQ-001. **Hallazgo colateral**: colisión de seeds REQ-004 (`-9520…-9525` vs `ECONOMIC_AID_TYPE_CATALOG`) → re-ubicación `-9890…-9895` anotada en REQ-004. D-01…D-18 propuestas, P-01…P-15 abiertas; seeds propios tentativos `-9880…-9889`. Sin commit. Pendiente: ratificación del negocio.
- **2026-07-05** — **RATIFICADO por el negocio**: D-01…D-18 aceptadas y P-01…P-15 respondidas. Cierres que ajustan el diseño: (1) **P-01 «el motor se realizará aparte»** → F1 sin motor confirmada (aplicación manual de cuotas + insumo exportable; el motor de generación = levantamiento futuro); (2) **P-02 «se requieren ambos catálogos»** con semántica definida: «tipo ingreso» = catálogo general de tipos de ingreso (Salario, Aguinaldo, horas extras, vacaciones, bonificaciones… — cubierto por conceptos país `Nature=Ingreso`, verificar filas en PR-1) y «tipo» = **catálogo nuevo de tipos de ingresos cíclicos** (prestaciones permanentes independientes del salario: ayuda para alimentación, gastos de representación, combustible…) → `RECURRING_INCOME_TYPE_CATALOG` con plantilla, seeds `-9896…-9899`; (3) **P-15 centro de costo obligatorio y relacionado a la plaza** → plaza obligatoria (default principal), centro de costo derivado con snapshot, plaza sin centro de costo → 422 (D-12 ajustada); (4) P-03 `SUSPENDIDO` confirmado (6 estados) · P-06 indefinido → `CANCELAR` forzado · P-07 sin adjuntos · P-10 descuentos cíclicos + endeudamiento = **siguiente levantamiento inmediato** · resto = propuestas tal cual. Documento actualizado a RATIFICADO (§17 resuelta). Sin commit. Siguiente: plan técnico.
- **2026-07-05** — **Plan técnico escrito** (`plan-tecnico-planilla-ingresos-ciclicos.md`, PR-1…PR-6) sobre exploración quirúrgica del código: **`AuthorizationPolicySet` es class-only** (`AuthorizationPolicySetAttribute.cs:24`) → la decisión va en **controller dedicado** (molde `RetirementRequestResolutionController.cs:26`); **catálogos generales NO son TPH** (tabla propia por subclase vía `GeneralCatalogItemConfigurationBase<T>`, configs auto-descubiertas); sub-recursos en `IPersonnelFileEmployeeRepository` con idioma Add + AsNoTracking re-query + Append (`PersonnelFileEmployeeRepository.cs:1393-1403`); lock advisory sin helper compartido (constante + `ExecuteSqlRawAsync`, moldes `CompanyRepository.cs:29`/`PositionSlotRepository.cs:196` — el handler abre la transacción antes); **integración liquidación por canal de sugerencias** (no engine-spec): `GetCalculationContextAsync:126-152` + concepto nuevo `INGRESO_CICLICO_PENDIENTE=-9888` + hooks en `IssueSettlementCommandHandler:947`/`AnnulSettlementCommandHandler:1030` (finalizar al emitir / reabrir al anular, marcador `closed_by_settlement_public_id`); índice único **parcial** de cuotas `WHERE is_active` (anulación permite re-aplicar el número); `openapi.yaml` es mantenido a mano vía skill + guardrails. Única decisión abierta: №13 (FK de periodo REQ-001 vs degradación — se decide en PR-4). Checklist de PRs poblado. Sin commit. Desarrollo: espera el turno del backlog (adelantable).

---

## REQ-006 — Planilla: ingresos eventuales (registro + búsqueda avanzada)

| | |
|---|---|
| **Estado** | 🔴 PENDIENTE (análisis **RATIFICADO** + plan técnico **ESCRITO** — listo para arrancar según orden del backlog; **sinergia fuerte con REQ-005: construir contiguos**) |
| **Análisis de negocio** | [`docs/business/analisis-planilla-ingresos-eventuales.md`](business/analisis-planilla-ingresos-eventuales.md) — **D-01…D-18 RATIFICADAS + P-01…P-13 respondidas (2026-07-06, §17): todas las recomendaciones aceptadas**. Cierres: **P-01** autorización de una decisión confirmada (5 estados; alternativa sin autorización descartada) · **P-02** solicitante = expediente + trío con snapshot, **participa en anti-autoaprobación** (triple) · **P-03/P-04** dos métodos de factores, sin tarifas legales ni lookup de salario en F1 · **P-09 «no es el módulo de horas extras; ese será otro módulo»** (registro de jornadas = levantamiento aparte; costura documentada) |
| **Plan técnico** | [`docs/technical/plan-tecnico-planilla-ingresos-eventuales.md`](technical/plan-tecnico-planilla-ingresos-eventuales.md) — **escrito 2026-07-06**: §0 aclaraciones (17, verificadas contra código y contra el plan de REQ-005) · §2 modelo de datos (2 tablas; índice único parcial = **una aplicación activa por ingreso a nivel BD**) · §3 arquitectura (trío de controllers; anti-self **triple**; `groupBy` de 8 dimensiones net-new) · §3.5 integración liquidación (espejo REQ-005, concepto `-9905`) · §6 orden de PRs (PR-1…PR-6) |
| **Guía FE** | pendiente |
| **Rama** | `feature/planilla-ingresos-eventuales` |
| **Línea base de tests** | (anotar al arrancar) |
| **Alcance (síntesis)** | Segunda rebanada del **módulo de planilla** — el hermano **puntual** del REQ-005: ingresos de una sola vez por empleado (horas extras, comisiones, bonos puntuales) con **valor fijo o calculado por factores** (componentes persistidos + coherencia validada en servidor), moneda, **plaza obligatoria → centro de costo derivado** (espejo P-15), **solicitante** (trío con snapshot), **tipo de planilla + periodo destino** (par REQ-004/REQ-001 con degradación) · flujo de **una decisión** propuesto (P-01; anti-autoaprobación extendida al solicitante; `Authorize*` sin Admin) · re-imputación («enviar a otro periodo») + aplicación manual unitaria/**lote por periodo con exclusión** bajo lock + reversión con motivo · **búsqueda avanzada corporativa** (estado/empleado/tipo/fecha/valor fijo + `StatusCounts` + totales por moneda) con **agrupación por dimensión** (net-new: hoy solo existe conteo por estado) y **export Excel** (infra OpenXML existente) + bandeja de pendientes/atrasados + **insumo de planilla por periodo** · gancho de liquidación (`INGRESO_EVENTUAL_PENDIENTE`; **no** reutilizar `HORAS_EXTRAS_PENDIENTES=-9837`, reclamado por REQ-002). Reutiliza: conceptos país `Nature=Ingreso` (**`HORAS_EXTRA=-9721` y `COMISION=-9722` ya sembrados**), `currencies` (+default empresa), `CostCenter`, `payroll-types` (definición REQ-004 `-9890…-9895`), maestro de periodos (REQ-001). **Sin motor** (frontera P-01 de REQ-005 heredada), sin catálogo de tipos nuevo, sin asientos de journal, sin adjuntos ni autoservicio F1 |
| **Dependencias** | **Ratificación del negocio pendiente** (único bloqueante del plan técnico) · REQ-004/REQ-005: definición compartida `payroll-types` (no bloquea: la siembra el primero que construya) · REQ-001: maestro de periodos (no bloquea: degradación a etiqueta) · liquidación mergeada (PR #56) · **sinergia fuerte con REQ-005** — se recomienda construirlos contiguos (REQ-005 → REQ-006) · seeds propios en bloque **`-9900…-9909`** (verificado 2026-07-05: **≤ -9900 completamente libre** en código y planes; estados `-9900…-9904`, concepto liquidación `-9905`, holgura `-9906…-9909`; trampa `-9490…-9496`) |

### Checklist de PRs (fuente: plan técnico §6 — detalle por PR en el plan)
- [ ] **PR-1** — Configuración (M1): catálogo `ONE_TIME_INCOME_STATUS_CATALOG` (`-9900…-9904`) + `payroll-types` `-9890…-9895` **solo si nadie lo sembró** + key map/categorías + verificación de conceptos `Nature=Ingreso` (D-03) + 3 permisos/policies/gates (**`AuthorizeOneTimeIncomes` sin Admin**) + governance tests + openapi temprano — plan §3.1/§4
- [ ] **PR-2** — Dominio + reglas (M2): 2 entidades (ingreso + aplicación) + mutadores custodiados + EF/índices/CHECKs (**índice único parcial de aplicación**) + `OneTimeIncomeRules` puro (cálculo por factores + coherencia + ciclo) con **golden A.3 en verde (gate)** + repositorio (idioma Add/AsNoTracking) + lock advisory + decisión de base común si REQ-005 ya corrió — plan §2/§3.2
- [ ] **PR-3** — Flujo end-to-end: CRUD `EN_REVISION` (valor fijo/factores) + **controller de resolución dedicado** (autorizar/rechazar con **anti-self TRIPLE** + revocación) + anulación + **re-imputación de periodo** («enviar a otro periodo») + validaciones espejo P-15 + solicitante (trío) — plan §3.3
- [ ] **PR-4** — Aplicación: unitaria + **lote por periodo con exclusión** (atómico, locks ordenados, **test de carrera**) + reversión con reapertura a `AUTORIZADO` + bandeja de pendientes/**atrasados** + par de periodo (№13: FK REQ-001 + `end_date` vs degradación — coordinar con REQ-005) — plan §3.4
- [ ] **PR-5** — Búsqueda avanzada + exportaciones: `query` corporativo (filtros del levantamiento incl. `isFixedValue` + `StatusCounts` + totales por moneda) + **`groupBy` de 8 dimensiones (net-new, EXPLAIN)** + 3 exports con rate limiting + **insumo de planilla** cuadrado — plan §3.3/№14/№15
- [ ] **PR-6** — Integración liquidación (sugerencia `INGRESO_EVENTUAL_PENDIENTE=-9905` + `APLICADO` origen `LIQUIDACION` al emitir con línea incluida + reapertura al anular) + suite E2E + test de no-escritura + `openapi.yaml` final (skill) + **guía FE** + DevSeed — plan §3.5/§3.6

### Pendientes de despliegue (checklist §8 del plan)
- [ ] Migraciones M1–M2 aplicadas (verificar estado previo de `payroll-types` si REQ-001/004/005 corrió antes)
- [ ] **Sin storage nuevo** (P-07), sin jobs, sin appsettings nuevos
- [ ] Paso de adopción por empresa: confirmar conceptos `Nature=Ingreso` (los del levantamiento ya sembrados); registrar eventuales históricos retroactivos si se desea continuidad de reportes (P-05)
- [ ] Asignar `AuthorizeOneTimeIncomes` a los autorizadores (Admin **no** decide sin el grant)
- [ ] Comunicar la **operación manual F1** (aplicar por periodo desde la bandeja; insumo = puente con nómina externa) y la **frontera de los cuatro registros monetarios** (capacitación, Anexo A.4 del análisis)

### Próxima acción
> **Esperar el turno del backlog** — recomendación D-17: **construir contiguo a REQ-005** (inmediatamente después, comparten catálogo `payroll-types`, molde de flujo, bandejas y gancho de liquidación; decidir №13 igual en ambos). Al arrancar: crear la rama `feature/planilla-ingresos-eventuales` desde `master`, anotar la línea base de tests, **verificar IDs de seed libres `-9900…-9909`** contra `GlobalCatalogSeedData` (trampa `-9490…-9496`; respetar reservas REQ-001/002/003/005; coordinar `payroll-types -9890…-9895` — una sola definición) y ejecutar **PR-1** siguiendo el plan §3.1/§4. Recordar: `AuthorizationPolicySet` es class-only → resolución en controller dedicado y bandeja sin policy-set con gate en handler (plan №4); **NO reutilizar `HORAS_EXTRAS_PENDIENTES=-9837`** (REQ-002). Los documentos siguen **sin commit**.

### Bitácora
- **2026-07-05** — Análisis de negocio creado en borrador (validación contra código + GAP): es el **hermano puntual del REQ-005** (ratificado el mismo día) y hereda ~80 % de sus decisiones (numeración D espejo). Verificado: **cero entidades** de ingreso eventual; los dos ejemplos del levantamiento **ya están sembrados** como conceptos país (`HORAS_EXTRA=-9721`, `COMISION=-9722`, `GlobalCatalogSeedData.cs:936-952`); «solicitante» tiene **precedente exacto** (trío `RequesterFilePublicId`+`RequesterNameSnapshot`+`RequestedByUserId` en liquidación `PersonnelFileSettlement.cs:114-123` y retiro); la **agrupación por dimensión es net-new** (el único rollup existente es `StatusCounts` por estado, `SettlementRepository.cs:355-363`); el export Excel tiene infraestructura completa (OpenXML hecho a mano, `ReportExportFileWriter.cs` + `ReportExportDeliveryService`, sin librería externa); bloque de seeds **≤ -9900 verificado completamente libre** (doble pasada: piso en código `-9846`, reservas de planes hasta `-9899`). Piezas nuevas de fondo: cálculo por factores (D-07) y `groupBy` (RF-009). D-01…D-18 propuestas, P-01…P-13 abiertas. Sin commit. Pendiente: ratificación del negocio.
- **2026-07-06** — **RATIFICADO por el negocio**: D-01…D-18 aceptadas y **P-01…P-13 respondidas con todas las recomendaciones aceptadas**. Cierres que fijan el diseño: (1) **P-01 con autorización** → flujo de una decisión confirmado (ciclo de 5 estados `EN_REVISION → AUTORIZADO/RECHAZADO → APLICADO` + `ANULADO`; alternativa sin autorización descartada); (2) **P-02** solicitante = expediente de la empresa + snapshot (trío) y **participa en la anti-autoaprobación** (triple); (3) **P-03/P-04** dos métodos de cálculo por factores, sin tarifas legales ni lookup de salario en F1; (4) **P-09 con precisión literal: «no es el módulo de horas extras; ese será otro módulo»** → este módulo registra el pago, el registro de jornadas será un módulo propio (costura `overtimeRecordPublicId` de REQ-002 documentada sin construir); (5) P-06 reversión de aplicación (no contra-asiento) · P-07 sin adjuntos · P-10 sin autoservicio · P-11 permisos dedicados · P-12 sin umbral · P-13 gancho de liquidación en F1. Documento actualizado a RATIFICADO (§17 resuelta). Sin commit.
- **2026-07-06** — **Plan técnico escrito** (`plan-tecnico-planilla-ingresos-eventuales.md`, PR-1…PR-6) sobre las verificaciones de código del análisis + el plan de REQ-005 como molde (mismos puntos de anclaje verificados: controller de resolución dedicado, `Authorize*` sin Admin `Program.cs:557-575`, idioma Add/AsNoTracking `PersonnelFileEmployeeRepository.cs:1393-1403`, lock advisory, canal de sugerencias `GetCalculationContextAsync:126-152` + hooks Issue`:947`/Annul`:1030`). Piezas propias de este módulo: **anti-self triple** (pata (c) nueva contra el expediente solicitante); **índice único parcial `(tenant, income) WHERE is_active`** en la tabla de aplicaciones = una sola aplicación activa garantizada a nivel BD; **`groupBy` de 8 dimensiones** con whitelist + agregación (dimensión, moneda) — generalización del único rollup existente (`SettlementRepository.cs:355-363`); columna `payroll_period_end_date` snapshot para derivar «atrasado» (limitado en modo degradado — №13); concepto de liquidación propio `-9905` (no tocar `-9837` de REQ-002); coexistencia con REQ-005 en el contexto de liquidación verificada por diseño (aditiva). Única decisión abierta: №13 (periodo — se decide en PR-4, coordinada con REQ-005). Checklist de PRs poblado. Sin commit. Desarrollo: espera turno del backlog (contiguo a REQ-005 recomendado).

---

## REQ-007 — Horas extras del empleado (registro de jornadas, autorización y consulta de solicitudes)

| | |
|---|---|
| **Estado** | 🔴 PENDIENTE (análisis **RATIFICADO** + plan técnico **ESCRITO** — listo para arrancar según orden del backlog; recomendado **tras REQ-001 y cerca de REQ-002**, adelantable con degradaciones №1/№3 + seeder propio) |
| **Análisis de negocio** | [`docs/business/analisis-horas-extras-empleado.md`](business/analisis-horas-extras-empleado.md) — **D-01…D-21 RATIFICADAS + P-01…P-14 respondidas (2026-07-06, §17)**: 12 recomendaciones aceptadas tal cual + 2 ajustes — **P-02** también jornadas **futuras organizadas por cada área** (D-08 ajustada; aplicar exige fecha transcurrida) · **P-08** la **liquidación calcula las pendientes de pago** (**D-21 nueva** + RF-014 Alta, concepto `HORAS_EXTRAS_PENDIENTES_PAGO=-9915`) |
| **Plan técnico** | [`docs/technical/plan-tecnico-horas-extras.md`](technical/plan-tecnico-horas-extras.md) — **escrito 2026-07-06** sobre anclas frescas de código: §0 aclaraciones (18) · §2 modelo (4 tablas; índice único parcial de aplicación) · §3 arquitectura (canal portal authn-only+`isSelf` · anti-self triple molde `RetirementAuthorizerGuards` · maestros molde `CostCenter` sin DELETE) · §3.6 **integración liquidación**: línea **CALCULADA** (contexto+spec+`case` molde `SALARIO` + hooks Issue/Annul; concepto **`-9915` con `IsSystemCalculated=TRUE`** — con `false` el case nunca corre) · §6 PR-1…PR-6 · §7 **3 decisiones abiertas** (№1 periodo — PR-4 · №2 sanity-cap fecha — PR-3 · №3 horas-día — PR-6) |
| **Guía FE** | pendiente (`guia-integracion-frontend-horas-extras.md`) |
| **Rama** | `feature/horas-extras` |
| **Línea base de tests** | (anotar al arrancar) |
| **Alcance (síntesis)** | Es el **módulo dos veces anunciado**: la costura `overtimeRecordPublicId` de REQ-002 (D-21) y el «ese será otro módulo» de REQ-006 (P-09). Registro con **flujo de autorización** (pedido explícito) de las **jornadas extraordinarias**: tipo de hora extra (maestro por empresa con plantilla — factores ref. CT **2.00/2.50/4.00/5.00**, P-03) × **factor** (default del tipo, editable con nota, snapshot) × fecha pasada **o futura** (jornadas **organizadas por cada área** — P-02 ratificada; aplicar exige fecha transcurrida) × **duración en horas y minutos** (→ decimal derivado; campo sin precedente) + **solicitante** (trío, anti-self **triple** con molde `RetirementAuthorizerGuards`) + justificación (2º maestro con plantilla) + par **planilla/periodo** (4ª reutilización REQ-004 `-9890…-9895` + REQ-001 con degradación) — **SIN montos** (frontera sin-motor en cadena; el valor lo pone la nómina externa/REQ-006) · canal **portal** = autoservicio del propio empleado (precedente triple verificado: policy authn-only + `isSelf` en handler) con preferencia default-off + **origen trazado** `RRHH`/`PORTAL` (P-01) · 5 estados con **`APLICADA`** por periodo (lote con exclusión bajo lock, bandeja pendientes/atrasadas, re-imputación) · **consulta corporativa** + `StatusCounts` + totales **en horas** + **Excel** + **insumo por periodo** (horas × factor) · costura REQ-002: validar vínculo + insignia «compensada» + exclusión del insumo (**anti doble beneficio**, RF-013 condicionada) · sin asientos de journal (volumen) · **integración con liquidación (P-08 ratificada, RF-014)**: línea **calculada** `HORAS_EXTRAS_PENDIENTES_PAGO=-9915` = Σ(horas × factor) × valor-hora (salario/30 ÷ horas-día; **NO tocar `-9837`** de REQ-002 — coexisten sin doble conteo), cierre al emitir origen `LIQUIDACION` / reapertura al anular; futuras no trabajadas se anulan al retiro |
| **Dependencias** | Ratificación: ✅ **completada** (2026-07-06, §17) · ninguna dura de código: REQ-001 (maestro de periodos + plantillas/`load-template` + asuetos/`restDayOfWeek` F2) y REQ-002 (costura RF-013 + **preferencia horas-día del valor-hora de RF-014**, default 8 propio si no está) **no bloquean** — degradaciones documentadas (D-20); recomendado construir **después de REQ-001 y cerca de REQ-002** · liquidación mergeada (PR #56) — el gancho RF-014 no espera nada · `payroll-types` = definición compartida REQ-004 (la siembra el primero que construya) · seeds propios **`-9910…-9919`** (estados `-9910…-9914` · concepto liquidación `-9915` · holgura `-9916…-9919`; **banda `-9847…-9999` verificada libre en código 2026-07-06**, HEAD `62b341b`; trampa `-9490…-9496`; ojo: los aparentes `-9914`/`-9897` en `Migrations/*.Designer.cs` son fragmentos de GUID, no seeds) · maestros por empresa sin seeds globales (molde construido: `CompetencyRatingScale`) |

### Checklist de PRs (fuente: plan técnico §6 — detalle por PR en el plan)
- [ ] **PR-1 — Configuración (M1)**: 2 maestros por empresa molde `CostCenter` (sin DELETE; `activation`/`inactivation`) + `OvertimeTemplateSeeder` + `load-template` + catálogo de estados `-9910…-9914` + `payroll-types` `-9890…-9895` **solo si nadie lo sembró** + **5 policies sobre 3 codes** (registros authn-only para el portal · maestros estrictas · **`AuthorizeOvertimeRecords` sin Admin**) + 2 preferencias (vía **PUT** — el PATCH actual es scalar-only) + governance tests + openapi temprano — plan §3.1/§3.2/§4
- [ ] **PR-2 — Dominio + reglas (M2)**: 2 entidades + mutadores custodiados + configs/índices/CHECKs (**índice único parcial de aplicación**) + `OvertimeRecordRules` puro con **golden A.4 en verde (gate)** + repositorio (idioma Add/AsNoTracking) + `AcquireOvertimeRecordMutationLockAsync` — plan §2/§3.3
- [ ] **PR-3 — Flujo end-to-end**: CRUD `EN_REVISION` + **canal portal** (preferencia + `isSelf`, origen trazado, self-edit del propio `EN_REVISION`) + resolución **anti-self triple** + revocación + re-imputación + tope diario + **decidir №2** (sanity-cap de fecha futura) — plan §3.4
- [ ] **PR-4 — Aplicación**: unitaria + **lote por periodo con exclusión** (atómico, locks ordenados, **test de carrera**) + **guard fecha-transcurrida** + reversión + bandeja de pendientes/**atrasadas** + **decidir №1** (periodo FK vs degradación — coordinar con REQ-005/006) — plan §3.5
- [ ] **PR-5 — Consulta + exportaciones**: query corporativa (filtros incl. **origen `RRHH`/`PORTAL`** + `StatusCounts` + **totales en horas**) + consulta en ficha + lectura self + 3 exports con rate limiting + **insumo por periodo** cuadrado en tests — plan §3.4/№16
- [ ] **PR-6 — Integración liquidación + cierre**: 6 toques (contexto→input→spec→`case` molde `SALARIO`→hooks Issue `:992`/Annul `:1082`) + concepto **`-9915` `IsSystemCalculated=TRUE`** (M3) + **decidir №3** (horas-día: preferencia REQ-002 vs default 8) + futuras→anulación con reapertura simétrica + costura REQ-002 (RF-013, **condicional a REQ-002 mergeado**) + suite E2E + test de no-escritura + `openapi.yaml` final (skill) + **guía FE** + DevSeed — plan §3.6/§3.7/§3.8

### Pendientes de despliegue (checklist §8 del plan)
- [ ] Migraciones **M1–M3** aplicadas (verificar el estado previo de `payroll-types` si otro REQ corrió antes)
- [ ] **Sin storage nuevo**, sin jobs, sin appsettings nuevos
- [ ] **Confirmar con el contador los factores de la plantilla** (P-03: HED 2.00 / HEN 2.50 / HEDF 4.00 / HENF 5.00 + posible tipo «día de descanso» Art. 175) **antes** de ejecutar `load-template` en el tenant productivo
- [ ] Paso de adopción por empresa: `load-template` de los 2 maestros (o creación manual — factores editables), configurar preferencias (`OvertimeSelfServiceEnabled` — **piloto recomendado**, default off — y `OvertimeMaxDailyMinutes`) y asignar `AuthorizeOvertimeRecords` a los autorizadores (Admin **no** decide sin el grant)
- [ ] Comunicar la **operación por periodo** (bandeja pendientes/atrasadas + lote + insumo = puente con la nómina externa) y el **triángulo jornada/pago/compensación** (Anexo A.5 del análisis — capacitación)
- [ ] Si REQ-002 ya está en producción: verificar la costura RF-013 y la fuente de horas-día (№3)

### Próxima acción
> **Esperar el turno del backlog** (orden actual: tras REQ-001…REQ-006; recomendado construir **después de REQ-001** — plantillas/`load-template` + maestro de periodos — y **cerca de REQ-002** — costura RF-013 y preferencia horas-día; **adelantable** con seeder propio + degradación de periodo + default 8). Al arrancar: crear la rama `feature/horas-extras` desde `master`, anotar la línea base de tests, **verificar IDs de seed libres `-9910…-9919`** contra `GlobalCatalogSeedData` (trampa `-9490…-9496`; **filtrar por sufijo `L`** — los aparentes `-9914`/`-9897` en `Migrations/*.Designer.cs` son fragmentos de GUID, no seeds) y ejecutar **PR-1** siguiendo el plan §3.1/§3.2/§4. Recordatorios críticos: el concepto `-9915` va con **`isSystemCalculated=TRUE`** (con `false` el `case` del motor nunca corre — corte `ComputeIncomeLine:373-377`); **NO tocar `-9837`** (REQ-002); el canal `SuggestedItems` NO sirve para la línea calculada (produce montos manuales); las 3 decisiones abiertas del plan se cierran en PR-3 (№2 sanity-cap), PR-4 (№1 periodo — coordinada con REQ-005/006) y PR-6 (№3 horas-día). Los documentos siguen **sin commit**.

### Bitácora
- **2026-07-06** — Análisis de negocio creado en **borrador** (validación contra código + GAP, HEAD `62b341b` — el mismo de las verificaciones de REQ-005/006). Es el módulo **dos veces anunciado** (costura `overtimeRecordPublicId` de REQ-002 D-21; «no es el módulo de horas extras; ese será otro módulo» de REQ-006 P-09). **Greenfield total verificado**: solo existen `HORAS_EXTRA=-9721` (concepto país, `GlobalCatalogSeedData.cs:937`) y `HORAS_EXTRAS_PENDIENTES=-9837` (`:969`, `IsSystemCalculated=false`, **reclamado por REQ-002**); cero entidades/catálogos/flujos de jornadas; campo de duración h:m **sin precedente** en el dominio. Novedad estructural: el canal «portal» → **precedente triple de autoservicio verificado** (policy authn-only + `isSelf` en handler — reclamos médicos/ayuda económica/constancias, `PersonnelFilePolicies.cs:47-53`, `PersonnelFileEmployeeHandlerBases.cs:227+`); anti-autoaprobación del **solicitante** con precedente exacto (`RetirementAuthorizerGuards.CheckAsync`, `RetirementRequestResolution.cs:64-87`). Diseño propuesto: jornadas **sin dinero** (frontera en cadena), 2 maestros por empresa con plantilla (factores ref. CT 2.00/2.50/4.00/5.00 → **P-03 contador**), 5 estados con `APLICADA` por periodo (P-07), consulta + Excel + insumo (molde completo de reporting), costuras anti-doble-beneficio (REQ-002) y de pago (REQ-006). **Seeds `-9910…-9919` reservados** (verificación propia: banda `-9847…-9999` libre en código; trampa nueva documentada: fragmentos de GUID en `Designer.cs` aparentan IDs). **Hallazgo colateral**: la exclusión de Admin en `Authorize*` vive en la **policy** (`Program.cs:564-569` omite `PersonnelFiles.Admin`), NO en `CompanyAdminPermissions` (`ProvisioningConstants.cs:89` sí lo lista) — precisión de redacción aplicable a los planes de REQ-003/005/006 al implementarlos. D-01…D-20 propuestas, P-01…P-14 abiertas. **Sin commit**. Pendiente: ratificación del negocio.
- **2026-07-06** — **RATIFICADO por el negocio**: P-01…P-14 respondidas — **12 recomendaciones aceptadas tal cual** (P-01 canal dual RRHH+portal con preferencia default-off y origen trazado · P-03/P-04 plantilla con factores de referencia, confirmación del contador en checklist de despliegue · P-05 tope por preferencia · P-06 factor editable con nota · P-07 ciclo con `APLICADA` + bandeja/lote · P-09…P-14 tal cual, incl. RF-013 condicionada a REQ-002) y **2 ajustes de alcance**: (1) **P-02** «debe permitir crear horas extras pasadas y crear horas futuras que previamente fueron organizadas por cada área» → **D-08 ajustada** (fecha pasada o futura desde F1; salvaguardas: aplicar exige fecha transcurrida — RN-09; futuras no trabajadas se anulan al retiro; conciliación planificado-vs-trabajado = F2); (2) **P-08** «la liquidación debe calcular las horas extras pendientes de pago» → **D-16 ajustada + D-21 nueva + RF-014 (Alta)**: línea calculada por el motor Σ(horas × factor) × valor-hora (salario/30 ÷ horas-día estándar), concepto propio `HORAS_EXTRAS_PENDIENTES_PAGO=-9915` (coexiste sin doble conteo con `-9837` de REQ-002 vía RN-16 — las compensadas quedan excluidas), cierre al emitir origen `LIQUIDACION` / reapertura al anular, futuras no trabajadas → anulación. Documento actualizado a **RATIFICADO** (D-01…D-21; §17 resuelta; seeds: `-9915` asignado al concepto, holgura `-9916…-9919`). Sin commit. Siguiente: **plan técnico**.
- **2026-07-06** — **Plan técnico escrito** (`plan-tecnico-horas-extras.md`, PR-1…PR-6) sobre **anclas frescas de código** (2 agentes de exploración quirúrgica sobre HEAD `62b341b`): la integración de liquidación quedó verificada de punta a punta — el canal `SuggestedItems` existente (`SettlementRepository.cs:126-152`) **NO sirve** para líneas calculadas (produce `PlazaItems` con `IsManual:true` → monto manual); la vía es **campo nuevo de contexto + spec del motor + `case`** (molde `SALARIO` `SettlementCalculation.Rules.cs:382-389`; `derived.DailySalary` ya existe = salario/30 `:193`); el concepto **`-9915` DEBE sembrarse `isSystemCalculated=TRUE`** (param 11 de `CreateSettlementConceptSeed :1184-1221`; el corte `:373-377` mata el case con `false`) y con `true` el anti-duplicado viene gratis (`Settlements.Handlers.cs:622-627` rechaza líneas manuales de conceptos de sistema); hooks: emitir = tras `MarkIssued :992`, anular = tras `Annul :1082` (hoy sin efectos — molde de cascada `RevertRetirementRequest.cs:140-196`). Moldes secundarios verificados: maestros = `CostCenter` (**sin DELETE**; `activation`/`inactivation`; UQ `(tenant, normalized_code)` + catch de constraint), catálogo de estados = cadena completa subclase→config auto-descubierta→KeyMap→`GeneralCatalogsController`, preferencias = **el PATCH actual es scalar-only** (los setters ricos van por PUT `CompanyPreferenceAdministration.cs:157-158`), 5 policies sobre 3 codes (dual authn-only/estricto — precedente retiro). **⚠️ Hallazgo cross-REQ anotado en REQ-002**: su plan §3.11 dice «el seed de `-9837` no cambia», pero el corte `:373-377` exige `IsSystemCalculated=true` para llegar a su case — resolver al implementar su PR-6. 3 decisiones abiertas (№1 periodo · №2 sanity-cap · №3 horas-día) con PR asignado. Checklist de PRs y pendientes de despliegue poblados. Sin commit. Desarrollo: espera turno del backlog.

---

## Plantilla para nuevos requerimientos

```markdown
## REQ-0NN — <Nombre del requerimiento>

| | |
|---|---|
| **Estado** | 🔴 PENDIENTE |
| **Análisis de negocio** | `docs/business/analisis-<modulo>.md` (estado: borrador / ratificado) |
| **Plan técnico** | `docs/technical/plan-tecnico-<modulo>.md` (pendiente / escrito) |
| **Guía FE** | pendiente |
| **Rama** | `feature/<modulo>` |
| **Línea base de tests** | (anotar al arrancar) |
| **Alcance (síntesis)** | 1-3 líneas |

### Checklist de PRs
- [ ] PR-1 — …

### Pendientes de despliegue
- [ ] …

### Próxima acción
> (autocontenida: qué hacer exactamente al retomar)

### Bitácora
- **AAAA-MM-DD** — …
```
